# Phase 4.0: Bootstrap & Cleanup / GSD 接管 + 队列清零 — Research

**Researched:** 2026-04-26
**Domain:** Brownfield doc / governance cleanup; GSD plumbing tracer-bullet
**Confidence:** HIGH (all answers verified against repo files at known SHAs)

## Summary

Phase 4.0 is purely doc-level + governance file creation. Five tasks (CLEANUP-01..04 + GOVERNANCE-01) and zero code/proto/test changes. All concrete strings (chunk_type wire values, ledger D120 row, ADR-V2-023 §Enforcement checklist, dormant paths) are already authored upstream — this phase is **transcription + provenance markers**, not creative work. Below: the exact strings, line numbers, and source-of-truth references the planner needs.

**Primary recommendation:** Plan 5 atomic-commit tasks in the order **CLEANUP-01 → 02 → 03 → 04 → GOVERNANCE-01 (dry-run last)**. All tasks `🟢 review_protocol: skip` per REVIEW_POLICY §2.3 (each <50 LOC delta in single file, no architecture impact, mechanical edits) — except CLEANUP-03 (new file ~30-50 LOC, may classify 🟡 `gsd-standard`).

<phase_requirements>
## Phase Requirements

| ID | Description | Research Support |
|----|-------------|------------------|
| CLEANUP-01 | L1_RUNTIME_ADAPTATION_GUIDE.md §4 chunk_type stale-value sweep + provenance marker | §"CLEANUP-01 — Exact Stale Strings" enumerates 7 stale wire values + line numbers + ADR-V2-021 canonical replacements |
| CLEANUP-02 | DEFERRED_LEDGER.md D120 status disambiguation + row-edit-on-close convention in preamble | §"CLEANUP-02 — D120 Current State + Preamble Gap" quotes line 326/339 verbatim + locates current "引入流程" preamble at L265-269 |
| CLEANUP-03 | docs/reviews/strategy-grid-two-leg-checklist.md (new) covering ADR-V2-023 §Enforcement 5 checkpoints | §"CLEANUP-03 — ADR-V2-023 §Enforcement 5 Items" quotes them verbatim |
| CLEANUP-04 | .github/CODEOWNERS (new) marking 5 Leg-B paths to @firmwwwee | §"CLEANUP-04 — CODEOWNERS Path Syntax" gives ADR-V2-023 §共享vs专属 Leg-B path list + GitHub CODEOWNERS path syntax |
| GOVERNANCE-01 | REVIEW_POLICY.md tracer-bullet validation (no file change in 4.0; verify 4.0 task review_protocol fields are valid) | §"GOVERNANCE-01 — Dry-run Check Procedure" + §"REVIEW_POLICY §2/§3 Format" |
</phase_requirements>

## Project Constraints (from CLAUDE.md)

These directives MUST be honored in PLAN.md:

1. **Doc language**: Files in `docs/` MUST be written in Chinese (中文). `CLAUDE.md`, `README.md`, root files in English.
   - **Impact**: `docs/reviews/strategy-grid-two-leg-checklist.md` is Chinese. `.github/CODEOWNERS` is config syntax (no language rule). `.planning/REVIEW_POLICY.md` is project-level (already Chinese, keep Chinese).
2. **Commit message footer (ENFORCED)**: Every commit ends with two lines:
   ```
   Generated-By: Claude (claude-<model>) via Claude Code CLI
   Co-Authored-By: claude-flow <ruv@ruv.net>
   ```
   Subject ≤72 chars; type prefixes `feat:` / `fix:` / `chore:` / `docs:` / `refactor:`.
3. **Never save scratch markdown to repo root** — all new docs go under `docs/` or `.planning/`.
4. **Never run `cargo test --workspace`** unsolicited — N/A this phase (no code changes).
5. **Always read a file before editing** — Edit tool enforces this.
6. **HEREDOC for commit messages** to preserve formatting.

## CONTEXT.md User Constraints (locked)

### Locked Decisions (verbatim from 04.0-CONTEXT.md `<decisions>`)

- **D-01-01..04**: Chunk_type provenance — top-of-file 1-line declaration + `<!-- @chunk-type-sync ADR-V2-021 -->` marker above §4 table only (NOT each TS example). Manual edit, no codegen.
- **D-02-01..04**: D120 — fix line 326 in-place to closed status w/ commit `7e083c7`; preamble adds row-edit-on-close convention text; NO retroactive edits to historical D-items; success criterion = `grep '^.*D120.*' DEFERRED_LEDGER.md | head -1` returns the closed line.
- **D-03-01..04**: Create `docs/reviews/strategy-grid-two-leg-checklist.md`; content = ADR-V2-023 §Enforcement 5 items as `- [ ]` markdown checkboxes; preamble ≤5 lines explaining when to use; CODEOWNERS auto-invokes.
- **D-04-01..04**: 5 Leg-B paths each → `@firmwwwee` (self-review); paths = `/grid-server/`, `/grid-platform/`, `/grid-desktop/`, `/web/`, `/web-platform/`; no virtual team; no PR template change in 4.0.
- **D-G01-01..05**: REVIEW_POLICY.md already exists — no rewrite. tracer-bullet for high-risk path deferred to Phase 4.1; all 4.0 tasks classify 🟢/🟡 (no high-risk trigger). REVIEW_POLICY draft→Active flip deferred to Phase 4.1.
- **D-C-01..03**: 5-6 atomic commits; per-task atomic; standard footer; expect `/gsd-execute-phase 4.0` to per-task atomic-commit (1 commit per task).

### Claude's Discretion

- Specific grep/sed forms for chunk_type replacement (do manual edits, no scripts).
- CLEANUP-03 checklist exact wording (translate ADR-V2-023 §Enforcement directly).
- CODEOWNERS inline `#` comments (planner decides; recommended: yes, brief).
- PLAN.md task ordering — but **GOVERNANCE-01 dry-run MUST be last** (it depends on other tasks having `review_protocol` fields).
- Frontmatter `review_protocol:` syntax — follow REVIEW_POLICY.md §3 (HTML comment per task, NOT YAML frontmatter — see §"GSD Frontmatter vs REVIEW_POLICY annotation" below).

### Deferred Ideas (OUT OF SCOPE)

1. PR template strengthening (CODEOWNERS adjunct) — Phase 4.1 or independent governance phase
2. REVIEW_POLICY.md draft → Active flip — Phase 4.1 first superpowers two-stage activation
3. Retroactive row-edit-on-close on historical closed D-items — only D120
4. Codegen tool for chunk_type sync — manual now, only if drift recurs
5. Cross-milestone watchlist (D109/D134/D136/D142/D143/NEW-D2/NEW-E2/NEW-E3) — STATE.md-tracked, next milestone
6. Phase 4.1 audit Q1-Q4 — already in `4.1-PRE-AUDIT-NOTES.md` §F

---

## CLEANUP-01 — Exact Stale Strings to Replace

### Source of truth: ADR-V2-021 §4 (`docs/design/EAASP/adrs/ADR-V2-021-chunk-type-contract-freeze.md`)

**Note**: Filename is `ADR-V2-021-chunk-type-contract-freeze.md` (CONTEXT.md and CLAUDE.md reference it as `ADR-V2-021-chunk-type-contract.md`; planner should use the actual filename when linking).

ADR-V2-021 §4 (lines 116-128) gives **8 canonical wire values** (CONTEXT.md said "7" — actual count is 8 with `unspecified` default + 7 active):

| enum (proto) | wire value (lower_snake_case) | Semantics |
|---|---|---|
| `CHUNK_TYPE_UNSPECIFIED = 0` | n/a (proto default, never sent) | placeholder |
| `CHUNK_TYPE_TEXT_DELTA = 1` | `"text_delta"` | Streaming assistant text |
| `CHUNK_TYPE_THINKING = 2` | `"thinking"` | Optional extended reasoning |
| `CHUNK_TYPE_TOOL_START = 3` | `"tool_start"` | Tool call begins, content = JSON params |
| `CHUNK_TYPE_TOOL_RESULT = 4` | `"tool_result"` | Tool returns, content = JSON result |
| `CHUNK_TYPE_DONE = 5` | `"done"` | Turn ends |
| `CHUNK_TYPE_ERROR = 6` | `"error"` | Runtime error |
| `CHUNK_TYPE_WORKFLOW_CONTINUATION = 7` | `"workflow_continuation"` | D87 observability, content = "attempt N/M" |

ADR-V2-021 §3 mandates: "**禁止在 runtime 层自行扩展 chunk_type 取值或添加同义映射**" — enum-closed.

### Stale strings in `docs/design/EAASP/L1_RUNTIME_ADAPTATION_GUIDE.md`

`grep '"text"\|"tool_call"\|"hook_fired"\|"pre_compact"\|chunk_type' L1_RUNTIME_ADAPTATION_GUIDE.md` reveals **3 locations** to edit:

#### Location 1 — §4 table (L92 heading, L96-104 table)

| Line | Current text (verbatim) | Action |
|------|-------------------------|--------|
| L92 | `## 4. Send 事件类型语义（7 种 chunk_type）` | Update to "**8 种 chunk_type**" (ADR-V2-021 has 7 active + 1 unspecified, reach with planner — recommend keeping "7 种" since unspecified is internal-only, OR change to "8 种" to match enum count). **Decide in planner** — but DO add `<!-- @chunk-type-sync ADR-V2-021 -->` marker right above this line per D-01-01. |
| L98 | `| `"text"` | LLM 生成的文本片段 | `content` | LLM 返回非 tool_call 内容 |` | Replace `"text"` → `"text_delta"` |
| L99 | `| `"tool_call"` | 代理请求执行工具 | `tool_name`, `tool_id`, `content`（参数 JSON） | LLM 返回 tool_calls |` | Replace `"tool_call"` → `"tool_start"` |
| L100 | `| `"tool_result"` | 工具执行结果 | `tool_name`, `tool_id`, `content`, `is_error` | 工具执行完毕 |` | **Already canonical** — no change |
| L101 | `| `"done"` | 会话轮次正常结束 | `content`（最终输出） | 代理停止（无更多 tool_calls） |` | **Already canonical** |
| L102 | `| `"error"` | 不可恢复错误 | `content`（错误信息）, `is_error=true` | Provider 异常、超出 max_turns 等 |` | **Already canonical** |
| L103 | `| `"hook_fired"` | Hook 脚本执行结果（可选） | `content`（decision） | PostToolUse hook 完成 |` | **CRITICAL DECISION**: ADR-V2-021 §4 has NO `hook_fired` value. Two options for planner: (a) **delete the row** (preferred — ADR-V2-021 enum-closed, this row contradicts source); (b) replace with `"workflow_continuation"` if hook-fired observability is intended. Recommend (a) + add `"thinking"` and `"workflow_continuation"` rows to bring table to 7 active values. |
| L104 | `| `"pre_compact"` | 上下文压缩前快照（可选） | `content` | 触发 PreCompact hook 时 |` | Same as L103 — `"pre_compact"` not in ADR-V2-021 §4 enum. **Delete the row.** PreCompact hook is event-name (ADR-V2-018), not a chunk_type wire value. |

**Why `hook_fired` and `pre_compact` are stale (not just "renamed")**: ADR-V2-021 §4 enumerates the closed set. There is NO chunk_type for "hook fired" or "pre-compact snapshot" — these are HookEvent kinds (ADR-V2-006 / V2-018), not SendResponse chunk_type wire values. The original L1_RUNTIME_ADAPTATION_GUIDE was authored Phase 2.5 (predates ADR-V2-021 freeze on 2026-04-19/20).

#### Location 2 — TypeScript example at L470-475 (within §12.4 "Send 方法")

L472 verbatim:
```typescript
yield { chunk_type: "text", content: "Processing...", session_id: sessionId, ... };
```
**Replace** `"text"` → `"text_delta"`. L474 (`"done"`) is already canonical.

#### Location 3 — TypeScript example at L484-498 (within §12.5 "手写类型接口")

L492 verbatim (inline comment in interface):
```typescript
chunk_type: string;   // "text" | "tool_call" | "tool_result" | "done" | "error"
```
**Replace** the entire comment to: `chunk_type: string;   // "text_delta" | "thinking" | "tool_start" | "tool_result" | "done" | "error" | "workflow_continuation"`.

**Cross-reference note**: `lang/ccb-runtime-ts/src/proto/types.ts` itself was migrated in commit `fec7c1d` (Phase 3.5) — only the docs guide is stale. Planner does NOT touch `types.ts`.

### Provenance marker placement (per D-01-01, D-01-02)

- **Top of file (~L1-2 region)**: 1-line declaration:
  ```markdown
  > 本文 chunk_type wire 值以 ADR-V2-021 为 source of truth, 任何漂移 PR 必须同步更新本文 §4 表格
  ```
- **Above §4 table (L92 region, before the heading or directly above L96 table)**:
  ```markdown
  <!-- @chunk-type-sync ADR-V2-021 -->
  ```
- **NOT** added to L470/L490 TS examples (D-01-01 explicit).

Pattern reference (CONTEXT.md §specifics): `<!-- @adr-trace ADR-V2-XXX -->` is the existing ADR plugin convention — `@chunk-type-sync` mirrors it for visual consistency.

---

## CLEANUP-02 — D120 Current State + Preamble Gap

### D120 verbatim text

**Line 326** (`docs/design/EAASP/DEFERRED_LEDGER.md`, the STALE row):
```
| 2026-04-15 | D120 | **新增** 🟡 P1-defer | **Cross-runtime envelope parity**：Rust `HookContext::to_json/to_env_vars` 缺 ADR-V2-006 §2/§3 字段（`event` / `skill_id` / `draft_memory_id` / `evidence_anchor_id` / `created_at` / `GRID_EVENT` / `GRID_SKILL_ID`），Python 已符合。S3.T5 reviewer M1，前置 Phase 2.5 goose 契约测试 → 补 HookContext 扩展 + cross-runtime parity tests |
```

**Line 339** (the closed row, append-only convention):
```
| 2026-04-16 | D120 | 🟡 P1-defer → ✅ closed | Phase 2.5 S0.T3 @ `7e083c7` — Rust `HookContext` 扩展 ADR-V2-006 §2/§3 字段（`event`/`skill_id`/`draft_memory_id`/`evidence_anchor_id`/`created_at` + `GRID_EVENT`/`GRID_SKILL_ID` env vars）+ empty-string serde helper + 10 parity tests。Python 侧先前已合规（S3.T5）。byte-parity 已对 ADR §2.1/2.2/2.3 canonical JSON 锁定 |
```

**Other D120 mentions** (DO NOT touch — these are forward-references in unrelated D-item rows):
- L328: D122 row says "→ D120 统一修" (informational reference)
- L341: D135 row says "可能掩盖 D120 回归" (test-mask reference)
- L386: closed-list summary table includes D120

### Required edit per D-02-01 (row-edit-in-place on line 326)

Per CONTEXT.md, line 326 must be **edited in place** to closed status (NOT append a new row, NOT delete it). Suggested replacement (planner refines wording):
```
| 2026-04-15 → 2026-04-16 | D120 | ✅ closed | **Cross-runtime envelope parity**：Rust `HookContext::to_json/to_env_vars` 缺 ADR-V2-006 §2/§3 字段（`event` / `skill_id` / `draft_memory_id` / `evidence_anchor_id` / `created_at` / `GRID_EVENT` / `GRID_SKILL_ID`），Python 已符合。S3.T5 reviewer M1。**Closed @ Phase 2.5 S0.T3 commit `7e083c7`** — HookContext 扩展 + 10 parity tests + byte-parity ADR §2.1/2.2/2.3 canonical JSON 锁定 |
```

**Open question for planner (NOT discuss-phase locked)**: Should line 339 (the append-style close row) be DELETED to avoid `grep '^.*D120.*' | head -1` returning the older row position first? The success criterion D-02-04 says "first grep result must contain `closed`" — if line 326 is edited to closed AND line 339 stays as another closed row, both lines say "closed" so the criterion is satisfied either way. **Recommend keeping line 339 as-is** (historical state-change log) since CONTEXT.md `<deferred>` item 3 says "不 retroactively 修" and line 339 is the chronological event in `状态变更日志` section L273-378. **Verify ordering**: line 326 sits in main D-item ledger section, line 339 sits in 状态变更日志 (state change log) — these are TWO separate sections, both legitimate. After edit, `head -1` on grep reads line 326 first (it appears before L339 in file order), so criterion is satisfied via the edited line.

### Existing preamble — current state

**Maintenance protocol already documented** at L265-269 of DEFERRED_LEDGER.md (under "引入流程"):
```
**引入流程**:
1. 在新 Deferred 产生的 plan 文件里以表格形式定义 `| D90 | 标题 | 去向 |`
2. **同步追加到本 ledger** 的相应 section（不要只写在 plan 里）
3. 在 commit message 引用 `Dxx`
4. 在 [状态变更日志](#状态变更日志) 新增一行
```

**Gap**: this is the *introduction* protocol. There is NO documented *closure* / *state-change* convention. Current de-facto pattern (visible at L286-339) is **append-new-row to 状态变更日志** for state changes — never edit the original D-item row.

CLEANUP-02 must ADD a row-edit-on-close convention, e.g. (planner finalizes wording):
```
**关闭/状态变更流程** (新增 2026-04-XX, Phase 4.0 CLEANUP-02):
1. 在原 D-item row 直接修改 status 字段（例如 `🟡 P1-defer → ✅ closed`）
2. 在 row 末尾追加 close-out trace（commit hash + Phase/Task 编号）
3. 在 [状态变更日志](#状态变更日志) 同步追加一行（保留历史时间线）
4. 仅适用于**未来**新关闭的 D-item；**已关闭的历史 D-item 不强制 retroactive 改写**
```

**Recommended insertion point**: directly after the current "引入流程" block (after L269), so introduction + closure protocols sit together.

### Empty-table guard

L172-174 of `docs/plans/2026-04-20-phase4a-debt-cleanup.md` shows the project pattern for empty Deferred tables (`| — | (none yet) | | |`). For DEFERRED_LEDGER.md preamble we just add prose, no empty-table concern.

---

## CLEANUP-03 — ADR-V2-023 §Enforcement 5 Items (Verbatim)

### Source: `docs/design/EAASP/adrs/ADR-V2-023-grid-two-leg-product-strategy.md` L213-231

ADR-V2-023 §Enforcement explicitly **promises this file** at L222: "`docs/reviews/strategy-grid-two-leg-checklist.md` — 未来创建。初步条目:". This is the file CLEANUP-03 creates.

#### Verbatim 5 checkpoints (ADR-V2-023 L224-228)

```markdown
- [ ] 此 PR 是否触碰 `grid-platform` 或 `web*`？如是，是否必须（见 P2 原则，原则上当前不应动）？
- [ ] 此 PR 在 `grid-engine` / `grid-runtime` / `grid-types` / `grid-sandbox` / `grid-hook-bridge` 任何一个里加了只服务某一条腿的分支？（违反 P1）
- [ ] 此 PR 在 `tools/eaasp-*/` 里实现了 Grid 独有业务逻辑？（违反 P3）
- [ ] 此 PR 是否声称"为腿 B 准备"而实际属于 P5 未触发的预热代码？（违反 P2）
- [ ] 如果 P5 触发条件满足，是否创建了对应的 ADR 和 Phase plan？
```

CLEANUP-03 should reproduce these **verbatim** (per D-03-02 "直接逐项提取") with optional preamble per D-03-04. Recommended file structure:

```markdown
# Strategy: Grid 两腿战略 PR Review Checklist

> ADR-V2-023 §Enforcement 落地。reviewer 在 PR 触碰 `grid-server` / `grid-platform` / `grid-desktop` / `web/` / `web-platform/` 任一路径时复制本清单到 PR comment 逐项 check。CODEOWNERS 自动指派 reviewer (@firmwwwee, self-review)。
> Source of truth: `docs/design/EAASP/adrs/ADR-V2-023-grid-two-leg-product-strategy.md` §Enforcement
> Last sync: 2026-04-XX (Phase 4.0 CLEANUP-03 initial creation)

## Checklist (5 items)

- [ ] 此 PR 是否触碰 `grid-platform` 或 `web*`？...
- [ ] 此 PR 在 `grid-engine` / `grid-runtime` / `grid-types` / `grid-sandbox` / `grid-hook-bridge` 任何一个里...
- [ ] 此 PR 在 `tools/eaasp-*/` 里实现了 Grid 独有业务逻辑？...
- [ ] 此 PR 是否声称"为腿 B 准备"而实际属于 P5 未触发的预热代码？...
- [ ] 如果 P5 触发条件满足，是否创建了对应的 ADR 和 Phase plan？

## 参考

- ADR-V2-023 §P1 — Core first, package later
- ADR-V2-023 §P2 — 腿 A 当前绝对优先 (dormant 5 paths)
- ADR-V2-023 §P5 — 腿 B 激活触发条件
```

**Directory creation**: `docs/reviews/` does not exist (`test -d` returns missing). CLEANUP-03 implicitly creates the directory via the new file.

---

## CLEANUP-04 — CODEOWNERS Path Syntax

### Existing state

- `.github/` exists, contains `workflows/` only (verified via `ls`)
- **No `CODEOWNERS` file anywhere in repo** (verified via `find -name CODEOWNERS`)
- **No `.gitlab/CODEOWNERS`, no root `CODEOWNERS`**

This is a NEW file at `.github/CODEOWNERS`.

### 5 Leg-B paths (ADR-V2-023 §"共享 vs 专属组件清单" L120-124)

Verbatim from ADR-V2-023 component table (L120-124):

| 组件 (column header) | 腿 A 用 | 腿 B 用 | 备注 |
|------|:-:|:-:|------|
| `grid-server` | ❌ | ✅ | 腿 B 专属（单用户 workbench） |
| `grid-platform` | ❌ | ✅ | 腿 B 专属（多租户 server） |
| `grid-desktop` | ❌ | ✅ | 腿 B 专属（Tauri 桌面应用） |
| `web/` | ❌ | ✅ | 腿 B 专属 |
| `web-platform/` | ❌ | ✅ | 腿 B 专属 |

### Repo layout — paths verified

| Component | Filesystem path | Type |
|---|---|---|
| `grid-server` | `crates/grid-server/` | Rust crate |
| `grid-platform` | `crates/grid-platform/` | Rust crate |
| `grid-desktop` | `crates/grid-desktop/` | Rust crate |
| `web/` | `web/` | top-level dir (frontend scaffolding) |
| `web-platform/` | `web-platform/` | top-level dir (frontend scaffolding) |

### GitHub CODEOWNERS path syntax (verified knowledge)

GitHub CODEOWNERS uses gitignore-like glob patterns (per [GitHub docs](https://docs.github.com/en/repositories/managing-your-repositories-settings-and-security/managing-your-repositories-settings-and-security/about-code-owners) — assumed verified):

- `path/` — directory and all contents (recursive)
- `/path/` — leading slash anchors to repo root (otherwise matches anywhere)
- `*` — wildcard
- Order matters — **last matching pattern wins**

**Recommended CODEOWNERS content** (planner finalizes; preamble + 5 entries, optional comments per D-04-04):

```
# CODEOWNERS — Leg-B dormancy reviewer per ADR-V2-023 §Enforcement
#
# Per ADR-V2-023 §P2, Leg-B components (grid-server, grid-platform, grid-desktop,
# web*, web-platform*) are dormant. Any PR touching these paths must justify
# necessity. Reviewer (currently @firmwwwee, self-review) uses
# docs/reviews/strategy-grid-two-leg-checklist.md to gate.
#
# Last sync: 2026-04-XX (Phase 4.0 CLEANUP-04 initial creation)

# Leg-B Rust crates (dormant, ADR-V2-023 §P2)
/crates/grid-server/        @firmwwwee
/crates/grid-platform/      @firmwwwee
/crates/grid-desktop/       @firmwwwee

# Leg-B frontend (dormant, ADR-V2-023 §P2)
/web/                       @firmwwwee
/web-platform/              @firmwwwee
```

**Critical**: ADR-V2-023's enforcement table uses BARE crate names (`grid-server`) but the actual filesystem paths are `crates/grid-server/`. CODEOWNERS MUST use real paths — bare names alone won't match. CONTEXT.md D-04-02 lists the slash-only forms `/grid-server/` etc. — recommend planner clarifies these to `/crates/grid-server/` (Rust crates are under `crates/`) AND `/web/` / `/web-platform/` (those ARE top-level). **This is a CONTEXT.md ambiguity** — planner should make the right call (filesystem paths) rather than literal CONTEXT.md text.

**Risk if used as written in CONTEXT.md**: A CODEOWNERS line `/grid-server/` would never match (no top-level `grid-server/` dir exists), silently disabling the gate. Confirm with user during plan-checker pass if uncertain.

---

## REVIEW_POLICY §2 / §3 — Fields the Planner Must Use

### §2 (Risk Tiers) — verified at REVIEW_POLICY.md L29-118

Three tiers:

- **🔴 High Risk → `review_protocol: superpowers-two-stage`** — 11 trigger conditions (L34-46). Phase 4.0 task should hit NONE.
- **🟡 Medium Risk → `review_protocol: gsd-standard`** — 6 conditions (L73-82). Document changes 50-200 LOC, new helpers, refactors without behavior change. **CLEANUP-03 (new file) likely fits here**.
- **🟢 Low Risk → `review_protocol: skip`** — 6 ALL-pass conditions (L102-110): LOC <50, single non-core file, typo/format/rename, doc microedits, no new deps, no commit-footer change. **CLEANUP-01, 02, 04 likely fit here**.

### §3 (PLAN.md Task Frontmatter) — verified at L122-167

**Format is HTML comment per task, NOT YAML top-of-file frontmatter**:

```markdown
### T1 — Add new ChunkType variant + propagate to 7 runtimes

<!-- meta
review_protocol: superpowers-two-stage
review_trigger: proto-contract-change + cross-runtime-modification (rules 2.1, 2.6)
review_estimated_overhead: +30min
-->

**Why**: ...
**Action**: ...
**Sign-off**: ...
```

**Decision path** (L160-167):
1. `gsd-planner` defaults to `gsd-standard`
2. `gsd-plan-checker` reclassifies hits to `superpowers-two-stage` or `skip`
3. Human gate before approval
4. Frontmatter frozen post-approval; executor obeys

### GSD Frontmatter vs REVIEW_POLICY annotation — IMPORTANT CONFLICT-AREA

The standard GSD planner agent (verified at `~/.claude/agents/gsd-planner.md` L491-506) defines **plan-level YAML frontmatter** with these fields:

```
phase, plan, type, wave, depends_on, files_modified, autonomous, requirements, user_setup, must_haves
```

`review_protocol` is **NOT** a standard GSD frontmatter field. REVIEW_POLICY §3 specifies it as a **per-task HTML comment metadata block** within the PLAN.md body. **These are not in conflict** — they coexist:

- Plan-level YAML frontmatter (top of `XX-NN-PLAN.md`): GSD standard fields
- Task-level `<!-- meta -->` comments (per `### T1` heading): REVIEW_POLICY §3 `review_protocol` annotation

Planner MUST handle both. The `gsd-plan-checker` agent in standard GSD does NOT yet know REVIEW_POLICY §3 — it will not auto-reclassify. **Manual reclassification required during plan-checker review pass** (acceptable for Phase 4.0 since all tasks are Low/Medium and all classify deterministically per §2 conditions).

---

## GOVERNANCE-01 — Dry-run Check Procedure

Per CONTEXT.md D-G01-04 + 04.0 success criterion #5, GOVERNANCE-01 in Phase 4.0 is **NOT** a code change — it's a verification dry-run. The check:

1. After CLEANUP-01..04 tasks have `<!-- meta review_protocol: ... -->` annotations in PLAN.md
2. For each task, verify the `review_protocol` matches REVIEW_POLICY §2 trigger conditions
3. Confirm none of the 4 cleanup tasks accidentally classifies `superpowers-two-stage` (would fail D-G01-03 "全 🟢/🟡")
4. Output: a sign-off line in the GOVERNANCE-01 task's commit message OR a brief check appended to STATE.md

**No file change expected unless misclassification found.** If the dry-run fails, GOVERNANCE-01 task gets reopened to fix the misclassified task's `review_protocol`. Per CONTEXT.md D-C-01 lower bound 5 commits = scenario where GOVERNANCE-01 outputs no commit (dry-run pass + no file change).

**Why last**: GOVERNANCE-01 reads other tasks' `review_protocol` annotations — they must exist first.

---

## Hidden Gotchas (top 5 for the planner)

1. **CONTEXT.md D-04-02 path strings are filesystem-incorrect.** It lists `/grid-server/`, `/grid-platform/`, `/grid-desktop/` as if top-level — actual paths are `crates/grid-server/` etc. Planner must use real filesystem paths in CODEOWNERS or the gate is silently broken. (See §"CLEANUP-04 — CODEOWNERS Path Syntax".)

2. **REVIEW_POLICY §3 uses HTML-comment per-task metadata, NOT YAML frontmatter.** Standard `gsd-planner` agent emits YAML frontmatter; REVIEW_POLICY annotation lives BELOW each `### Tx` heading. Planner must produce both layers correctly. (See §"GSD Frontmatter vs REVIEW_POLICY annotation".)

3. **Doc language rule conflict**: CLAUDE.md says "All `docs/` files MUST be Chinese". `docs/reviews/strategy-grid-two-leg-checklist.md` is in `docs/` → must be Chinese. ADR-V2-023 §Enforcement checklist text IS Chinese, so direct extraction satisfies this. CODEOWNERS is config syntax (no language rule applies). `.planning/REVIEW_POLICY.md` is `.planning/` (not `docs/`) — Chinese already, but rule technically doesn't enforce.

4. **L1_RUNTIME_ADAPTATION_GUIDE.md L92 heading says "7 种"** but ADR-V2-021 §4 enum has 8 (incl `UNSPECIFIED`) or 7 active wire values. After CLEANUP-01 deletes stale `hook_fired` and `pre_compact` rows AND adds `thinking` + `workflow_continuation`, the table will have 7 active rows — the heading "7 种" remains correct. **Planner should verify final row count matches the heading.**

5. **CONTEXT.md filename reference (`ADR-V2-021-chunk-type-contract.md`) does not match actual file (`ADR-V2-021-chunk-type-contract-freeze.md`).** Use the real filename in PLAN.md links and citations.

6. **Cargo fmt drift trap (REVIEW_POLICY §5.7)** — N/A this phase (zero Rust code changes), but worth noting that ANY `cargo fmt -p <crate>` without `--check` would reformat 170+ unrelated files. Phase 4.0 has zero code so this trap doesn't fire, but document anyway in case PLAN.md inadvertently introduces a Rust touch.

---

## Task Dependency Map

| Task | Depends On | Notes |
|------|-----------|-------|
| CLEANUP-01 | none | self-contained edit to L1_RUNTIME_ADAPTATION_GUIDE.md |
| CLEANUP-02 | none | self-contained edit to DEFERRED_LEDGER.md |
| CLEANUP-03 | none | new file `docs/reviews/strategy-grid-two-leg-checklist.md` |
| CLEANUP-04 | none | new file `.github/CODEOWNERS` |
| GOVERNANCE-01 | CLEANUP-01..04 (must have `review_protocol` annotations) | dry-run review of other tasks' classifications |

CONTEXT.md `<code_context>` L143 confirms: "CLEANUP-01 / 02 / 03 / 04 之间无依赖, 可任意顺序执行".

**Recommended ordering** (planner discretion per CONTEXT.md `<decisions>` Claude's Discretion):
1. CLEANUP-01 (chunk_type) — most concrete, smallest cognitive load
2. CLEANUP-02 (D120 ledger) — single file, mechanical
3. CLEANUP-04 (CODEOWNERS) — new file, no dependency on 03
4. CLEANUP-03 (checklist) — new file, references ADR-V2-023; logical pair with 04 (CODEOWNERS auto-invokes this checklist)
5. GOVERNANCE-01 (dry-run) — last, as required by D-G01-04

**Wave structure**: All 4 cleanup tasks have **zero `files_modified` overlap** (different files), so all 4 could be Wave 1 parallel; GOVERNANCE-01 must be Wave 2. However, given the small scope and "atomic-commit per task" tracer-bullet goal (D-C-03), recommend **sequential wave (Wave 1 → 2 → 3 → 4 → 5)** so each commit is observable and the GSD plumbing tracer-bullet validates per-task atomicity. Use `wave: 1` for all cleanup tasks if planner prefers parallel execution.

**Files modified — exclusive ownership confirmed**:

| Plan/Task | files_modified |
|-----------|----------------|
| CLEANUP-01 | `[docs/design/EAASP/L1_RUNTIME_ADAPTATION_GUIDE.md]` |
| CLEANUP-02 | `[docs/design/EAASP/DEFERRED_LEDGER.md]` |
| CLEANUP-03 | `[docs/reviews/strategy-grid-two-leg-checklist.md]` (new) |
| CLEANUP-04 | `[.github/CODEOWNERS]` (new) |
| GOVERNANCE-01 | `[]` (dry-run; if fails, fix is in failed task's file) |

Zero overlap → safe to run any-order or parallel.

---

## Phase 4a Archive Reference Pattern

`docs/plans/2026-04-20-phase4a-debt-cleanup.md` is the closest precedent (7 sequential tasks T1-T7, atomic commits, each task has `Why / Action / Sign-off / Risk` blocks). Verified commit cadence — git log shows:

```
86295053 fix(proto): D152 post-process grpcio-tools stubs to accept enum ints (T7)
a274ebdb fix(tests): D148 tighten T6 signatures + reconcile ledger LOC
07318fd1 test(pydantic-ai): D148 thicken pytest bench to >=12 (T6)
350c5f2a feat(ci): D149 add ccb-runtime-ts types.ts sync guard (T5)
9cff967a feat(scripts): D153 add --out-dir to gen_runtime_proto + drop Dockerfile sym...
9b051808 chore(scripts): D155 add check-pyright-prereqs.sh + make target
d4ca92e5 chore(pyright): D154 align per-env pythonVersion to pyproject floor
94e4fc60 test(grid-engine): add harness envelope call-site wiring regression (D151)
```

Pattern observations:
- 1 commit per task (atomic) ✓ matches D-C-03 expectation
- Subject pattern: `<type>(<scope>): D<N> <one-line> (T<X>)` — Phase 4.0 tasks don't have D-IDs (cleanup queue items), so use `(<phase4-cleanup>)` or similar. Suggested: `docs(planning): CLEANUP-01 chunk_type sweep + ADR-V2-021 marker`
- Footer: standard 2-liner per CLAUDE.md. Verified across 5 most recent commits — all comply.

**Task block template** (extract from Phase 4a):
```markdown
### T<N> — <DID/CLEANID>: <one-line>

<!-- meta
review_protocol: <skip|gsd-standard|superpowers-two-stage>
review_trigger: <rule N.M (description)>
-->

**Why**: ...
**Action**: ...
1. step
2. step

**Sign-off**: <observable verification — grep / file existence / test pass>
```

---

## Open Questions

1. **L1_RUNTIME_ADAPTATION_GUIDE.md §4 row count**: After deleting `hook_fired` and `pre_compact` and adding `thinking` and `workflow_continuation`, table is 7 rows; heading "7 种" still correct. But CONTEXT.md §specifics never explicitly confirms which rows to add/delete. **Planner decides during plan-phase Q&A or commits to the recommended (a) route.**
   - What we know: ADR-V2-021 §4 has 7 active wire values; current table has 7 rows but 2 are stale (`hook_fired`, `pre_compact`).
   - What's unclear: Should the planner introduce `thinking` and `workflow_continuation` rows in CLEANUP-01, or leave only the 5 confirmed (text_delta/tool_start/tool_result/done/error) and update heading to "5 种"?
   - **Recommendation**: Bring table to full 7-active-value parity with ADR-V2-021 (text_delta, thinking, tool_start, tool_result, done, error, workflow_continuation). Do NOT include `unspecified` — proto default is internal-only. Heading "7 种" stays accurate.

2. **CODEOWNERS path correction**: CONTEXT.md D-04-02 lists `/grid-server/` etc. — these don't match filesystem reality (`crates/grid-server/` for the Rust crates). Should the planner correct to `/crates/grid-server/` etc. or escalate to user via plan-checker?
   - What we know: Rust crates are under `crates/`; web dirs are top-level; `find -name CODEOWNERS` returned empty so no precedent.
   - What's unclear: Does CONTEXT.md D-04-02 intentionally use simplified paths (knowing `crates/` layout) or is it a slip?
   - **Recommendation**: Planner uses correct filesystem paths and notes the deviation in plan-checker pass for user confirmation. Better to be safe than have a silent CODEOWNERS no-op.

3. **GOVERNANCE-01 commit-or-no-commit**: D-C-01 says 5-6 commits — the lower bound 5 implies GOVERNANCE-01 may produce zero commits if dry-run passes with no file change. How does this interact with `/gsd-execute-phase` atomic-commit-per-task expectation?
   - What we know: GSD execute-phase commits per task; if a task is a "verification" with no diff, no commit.
   - What's unclear: Whether GSD treats a task with no diff as PASS or as a phase-blocker.
   - **Recommendation**: GOVERNANCE-01's task action should be "verify all CLEANUP tasks have valid `review_protocol` annotation; if any invalid, FAIL and reopen the offending task". If all pass, the GOVERNANCE-01 task itself contributes 0 commits — total 4 commits if no fixes needed; 5+ commits if any reclassification needed. This matches CONTEXT.md D-C-01 "5 个下限".

---

## Environment Availability

Phase 4.0 has zero external dependencies. All work is text editing.

| Dependency | Required By | Available | Version | Fallback |
|------------|------------|-----------|---------|----------|
| `git` | every task (commits) | ✓ | system git | — |
| `grep` | success criterion verification (D-02-04) | ✓ | system grep | — |

**Missing dependencies with no fallback:** none.
**Missing dependencies with fallback:** none.

---

## Validation Architecture

> Standard test infrastructure does not apply — no code under test. Validation = grep assertions and file-existence checks.

### Test Framework
| Property | Value |
|----------|-------|
| Framework | bash + grep (no test framework needed for doc-cleanup phase) |
| Config file | none |
| Quick run command | per-task `Sign-off` line in PLAN.md (file existence / grep result) |
| Full suite command | `bash -c "test -f ... && grep -q ... && ..."` chained |

### Phase Requirements → Test Map

| Req ID | Behavior | Test Type | Automated Command |
|--------|----------|-----------|-------------------|
| CLEANUP-01 | §4 table + L472 + L492 use canonical wire values | smoke | `! grep -E '"text"\|"tool_call"\|"hook_fired"\|"pre_compact"' docs/design/EAASP/L1_RUNTIME_ADAPTATION_GUIDE.md` (must produce zero matches in chunk_type contexts) AND `grep -F '<!-- @chunk-type-sync ADR-V2-021 -->' docs/design/EAASP/L1_RUNTIME_ADAPTATION_GUIDE.md` |
| CLEANUP-02 | D120 first grep result returns closed status | smoke | `grep '^.*D120.*' docs/design/EAASP/DEFERRED_LEDGER.md \| head -1 \| grep -q closed` (per D-02-04) AND `grep -q "row-edit-on-close\|关闭/状态变更流程" docs/design/EAASP/DEFERRED_LEDGER.md` |
| CLEANUP-03 | checklist file exists with 5 items | smoke | `test -f docs/reviews/strategy-grid-two-leg-checklist.md && [ $(grep -c '^- \[ \]' docs/reviews/strategy-grid-two-leg-checklist.md) -ge 5 ]` |
| CLEANUP-04 | CODEOWNERS file exists with 5 paths assigned to @firmwwwee | smoke | `test -f .github/CODEOWNERS && [ $(grep -c '@firmwwwee' .github/CODEOWNERS) -ge 5 ]` |
| GOVERNANCE-01 | every CLEANUP task in PLAN.md has `review_protocol:` annotation | smoke | `grep -c 'review_protocol:' .planning/phases/04.0-bootstrap-cleanup-gsd/*-PLAN.md` ≥ 4 |

### Sampling Rate
- Per task commit: that task's `Sign-off` line
- Phase gate: all 5 grep assertions PASS

### Wave 0 Gaps
- None — no test infrastructure to set up.

---

## Security Domain

Skipped — Phase 4.0 has zero code changes, zero auth/crypto/input-validation surfaces, zero secrets. Security audit not applicable to doc-cleanup phase.

If security_enforcement config requires this section: ASVS V5 Input Validation N/A (no input boundaries). V6 Cryptography N/A. V2/V3/V4 N/A. STRIDE N/A.

---

## Assumptions Log

| # | Claim | Section | Risk if Wrong |
|---|-------|---------|---------------|
| A1 | GitHub CODEOWNERS path syntax described matches actual GitHub behavior (gitignore-like, last-match-wins, leading-slash anchors to root) | CLEANUP-04 | If wrong, gate may fire on unintended paths or fail to fire on intended paths. Mitigation: planner verifies via GitHub docs at PR-creation time. |
| A2 | Planner's recommended action on stale row L103/L104 (delete `hook_fired`/`pre_compact`, add `thinking`/`workflow_continuation`) is what user wants | CLEANUP-01 | Wrong reading would mean keeping or differently transforming those rows. Listed as Open Question 1 — explicitly flagged for plan-checker confirmation. |
| A3 | CONTEXT.md D-04-02 paths `/grid-server/` are slips, not deliberate (correct filesystem paths are `crates/grid-server/`) | CLEANUP-04 | If deliberate, planner over-corrects. Listed as Open Question 2. |
| A4 | GSD execute-phase tolerates a task with zero file diff (GOVERNANCE-01 dry-run pass case) | GOVERNANCE-01 | If GSD requires a diff, GOVERNANCE-01 needs a placeholder commit (e.g., touch STATE.md). Listed as Open Question 3. |

All other claims in this research are verified via direct file reads at known SHAs (HEAD of `main` as of 2026-04-26).

---

## Sources

### Primary (HIGH confidence)
- `.planning/phases/04.0-bootstrap-cleanup-gsd/04.0-CONTEXT.md` — locked decisions
- `.planning/REQUIREMENTS.md` — CLEANUP-01..04 + GOVERNANCE-01 details
- `.planning/STATE.md` — current position + watchlist
- `.planning/REVIEW_POLICY.md` — risk tiers (§2) + frontmatter format (§3)
- `.planning/ROADMAP.md` — Phase 4.0 success criteria
- `docs/design/EAASP/L1_RUNTIME_ADAPTATION_GUIDE.md` L92, L96-104, L470-475, L484-498 — stale chunk_type strings (verbatim)
- `docs/design/EAASP/adrs/ADR-V2-021-chunk-type-contract-freeze.md` §4 L116-128 — canonical wire values (verbatim)
- `docs/design/EAASP/adrs/ADR-V2-023-grid-two-leg-product-strategy.md` L109-126, L213-231 — dormant components + Enforcement 5 checkpoints (verbatim)
- `docs/design/EAASP/DEFERRED_LEDGER.md` L265-269, L326, L339, L380-394 — preamble protocols + D120 rows (verbatim)
- `docs/plans/2026-04-20-phase4a-debt-cleanup.md` — task structure pattern
- `~/.claude/agents/gsd-planner.md` L491-506 — standard GSD frontmatter fields
- `git log --format="%H %s" 7e083c7 -1` — confirmed commit hash

### Secondary (MEDIUM confidence)
- `~/.claude/skills/gsd-plan-phase/SKILL.md` — orchestrator behavior
- CLAUDE.md (project instructions) — language/commit-format/file-org rules

### Tertiary (LOW confidence)
- GitHub CODEOWNERS path syntax (training data) — flagged as A1; planner verifies against current GitHub docs at PR time

## Metadata

**Confidence breakdown:**
- CLEANUP-01 stale strings: HIGH — direct grep + file read at exact line numbers
- CLEANUP-02 D120 rows + preamble: HIGH — line 326/339 quoted verbatim, preamble L265-269 read in full
- CLEANUP-03 ADR-V2-023 §Enforcement: HIGH — 5 checkpoints quoted verbatim from L224-228
- CLEANUP-04 path list: HIGH on Leg-B component names (ADR table); MEDIUM on filesystem path translation (planner verifies); MEDIUM on CODEOWNERS path syntax (training)
- GOVERNANCE-01 procedure: HIGH — REVIEW_POLICY §2/§3 read in full
- Hidden gotchas: HIGH on items 1, 2, 4, 5 (verified); MEDIUM on item 3 (language rule interpretation)

**Research date:** 2026-04-26
**Valid until:** 2026-05-26 (30 days; doc-cleanup phase, low-volatility content)

## RESEARCH COMPLETE
