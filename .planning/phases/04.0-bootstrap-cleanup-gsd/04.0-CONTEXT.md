# Phase 4.0: Bootstrap & Cleanup / GSD 接管 + 队列清零 - Context

**Gathered:** 2026-04-27
**Status:** Ready for planning
**Source:** Discuss-phase (5 gray areas analyzed, all 5 user-accepted Claude recommendations)

<domain>
## Phase Boundary

Phase 4.0 是 milestone "Phase 4 — Product Scope Decision" 的**第一个 GSD-managed phase**, 目标三件并行:

1. **GSD 治理底座落地** —— `.planning/REVIEW_POLICY.md` 已存在 draft (Phase 4a 实战起草), 本 phase 验证它在真实 task 拆分中可被引用 + tracer-bullet
2. **Phase 4a 遗留 cleanup 队列归零** —— 4 项 mechanical doc-level cleanup (CLEANUP-01..04), 每项有具体的"文件存在 + 内容 grep-断言" success criteria
3. **GSD 端到端 plumbing 验证** —— 通过本 phase 的 plan → execute → review → end-phase 跑通, 验证 brownfield repo 适配 GSD 没有 plumbing-level 的卡点

**Scope anchor**: ROADMAP §Phase 4.0 5 个 success criteria 是硬边界, discuss-phase 不扩展 scope, 只锁定**实施风格** (style of execution)。

**与上下文的耦合**:
- Phase 4.1 (Discuss & Audit) 依赖本 phase 的 REVIEW_POLICY.md 走 risk classification
- Phase 4.1 success criteria #3 要求 "GSD discuss → plan → execute → review 至少跑通一次" —— 本 phase **不需要在 4.0 内验证 superpowers two-stage 激活路径**, 留给 4.1 第一个真实 audit task 自然验证 (gray area #3 决策)

</domain>

<decisions>
## Implementation Decisions

### CLEANUP-01: chunk_type 引用对齐 + provenance marker 放置

- **D-01-01**: `<!-- @chunk-type-sync ADR-V2-021 -->` provenance marker 加在 `docs/design/EAASP/L1_RUNTIME_ADAPTATION_GUIDE.md` **§4 表格上方一处** (不每个 TypeScript 示例都加, 避免冗余)
- **D-01-02**: 文件**顶部**加 README-style 1 行声明 `> 本文 chunk_type wire 值以 ADR-V2-021 为 source of truth, 任何漂移 PR 必须同步更新本文 §4 表格`
- **D-01-03**: §4 表格内容用 ADR-V2-021 §4 canonical 8 个 wire 值替换现有 stale 字符串 (`text` → `text_delta`, `tool_call` → `tool_start` 等); 包括 line 92 表格 + line 472 / 492 TypeScript 示例
- **D-01-04**: 替换工作**手写**, 不写 codegen 工具 (规模小, 一次性, 加 codegen 是 over-engineering)

### CLEANUP-02: D120 status ambiguity 解决 + row-edit-on-close convention

- **D-02-01**: 解决方式 = **C 选项** —— Ledger preamble 加入 row-edit-on-close 约定文字, 同时把 D120 line 326 ("P1-defer → Phase 2.5 W1") 改为 closed 状态 + commit hash `7e083c7` 引用 (而非删除/废弃 stale 行)
- **D-02-02**: Convention 文字应当显式说: 当 D-item 状态变化时, **回填到原 row** (status 字段直接改, 不另起一行), 同时在 row 末尾追加 close-out trace (commit / phase 编号); 这条 convention 适用于**未来所有 D-item**
- **D-02-03**: **不 retroactively 修历史所有 closed D-item** —— 只修 D120 (因为它是 trigger), 历史已 closed 的 D-item 保留多 row 时间线作为历史记录, 不强制改写
- **D-02-04**: Success criteria #3 "单行 grep 即可断定 D120 真实状态" 的判定方法: `grep '^.*D120.*' DEFERRED_LEDGER.md | head -1` 应当返回包含 `closed` 的行, 不再返回 stale "P1-defer" 行

### CLEANUP-03: strategy-grid-two-leg-checklist.md 创建

- **D-03-01**: 新建 `docs/reviews/strategy-grid-two-leg-checklist.md` (目录不存在, 一并创建)
- **D-03-02**: 内容覆盖 ADR-V2-023 §Enforcement 5 个检查点 —— 由 planner 在 PLAN.md 拆 task 时从 ADR-V2-023 §Enforcement 段直接逐项提取 (Phase 4.1 audit 可能修订 ADR-V2-023, 但 4.0 cleanup 落地时按现有 §Enforcement 即可, 不预测未来变更)
- **D-03-03**: Checklist 形态 = markdown 复选框 `- [ ]`, reviewer 在 PR review 时复制到 PR comment 逐项 check
- **D-03-04**: 文件应有简短 preamble 说明用途 (≤ 5 行), reviewer 何时拿来用 (触碰 grid-platform / grid-server / grid-desktop / web / web-platform 任一路径), CODEOWNERS 自动 invoke 它

### CLEANUP-04: .github/CODEOWNERS 创建

- **D-04-01**: 颗粒度 = **A 选项** —— 5 条 Leg-B 路径每条标 `@firmwwwee` (user 自己), 实质 self-review, 但 review 标识在 + GitHub PR 流程会显式提示 dormancy-justification
- **D-04-02**: 路径列表: `/grid-server/`, `/grid-platform/`, `/grid-desktop/`, `/web/`, `/web-platform/` (5 条, 与 ADR-V2-023 §P2 dormant 列表对齐)
- **D-04-03**: 不引入虚拟 GitHub team (`@org/leg-b-reviewers` 不存在, fallback 个人), 不强制 multi-reviewer (会 unmergeable)
- **D-04-04**: PR template 强制 dormancy-justification check **暂不在 4.0 做**, 标为 deferred (gray area #2 我的建议: "留 PR template TODO 给后续") —— 加到 §deferred 不阻塞 4.0

### GOVERNANCE-01: REVIEW_POLICY.md tracer-bullet 验证策略

- **D-G01-01**: REVIEW_POLICY.md **已存在** (`.planning/REVIEW_POLICY.md`, 8 sections, ~150 LOC, draft @ commit 165cab4) —— 不重写, 但 GSD planner 应当**核对 4.0 自己拆出的 task 是否能依据 REVIEW_POLICY §2 三档 trigger condition 正确分级**
- **D-G01-02**: tracer-bullet 验证 = **B 选项** —— **不在 Phase 4.0 内人为制造 high-risk task 跑 superpowers two-stage**, 留给 Phase 4.1 第一个真实 audit task (那是 design-level work, 自然 high-risk)
- **D-G01-03**: 4.0 自己 task 全部应当落到 🟢 Low Risk (`review_protocol: skip`) 或 🟡 Medium Risk (`review_protocol: gsd-standard`) —— 都是 doc-level cleanup, 没有 proto / agent loop / hooks / security policy 改动, 不命中 §2 High Risk 11 条 trigger 任何一条
- **D-G01-04**: Success criteria #5 "任何 task 失败时 superpowers two-stage 机制能正确激活" 的判定: 不验证 4.0 task (会 contrived), 用 ROADMAP §Phase 4.1 success criteria #3 作为 milestone-level 验证锚 —— 4.1 第一次跑 audit 任务时验证激活路径
- **D-G01-05**: REVIEW_POLICY.md draft → Active 状态翻转**不在 4.0 做**, 等 Phase 4.1 第一次激活验证后再翻 Active (符合文档头 "Draft 2026-04-26 (...Phase 4.0 验证后转 Active)" 但 user 接受推迟到 4.1 第一次 superpowers 验证)

### Commit 策略

- **D-C-01**: 每个 CLEANUP 独立 commit + GOVERNANCE-01 (如有改动) 独立 commit = **6 个 commits 上限**, 5 个下限 (若 GOVERNANCE-01 不需要改 REVIEW_POLICY.md, 只跑 dry-run 检查则不出 commit, 总数变 5)
- **D-C-02**: 每个 commit message 遵守用户 global CLAUDE.md commit 规则 (subject ≤72 chars, body 答 why, 末尾两行 Generated-By + Co-Authored-By)
- **D-C-03**: tracer-bullet 用 GSD plumbing 验证: 期望 `/gsd-execute-phase 4.0` 路径 (或 plan 内 task 顺序逐个跑) 应当能逐 task atomic commit, 不出现一次 commit 多 task 的情况

### Claude's Discretion

下述事项 planner / executor 可自主决定, 不需要再问 user:
- CLEANUP-01 中 chunk_type 替换的具体 grep / sed 命令形式 (一次性手工 edit, 不写 script)
- CLEANUP-03 checklist 内每条具体措辞 (从 ADR-V2-023 §Enforcement 直译即可)
- CLEANUP-04 CODEOWNERS 行内是否加 # 注释 (建议加但 planner 决定)
- 如何在 PLAN.md frontmatter 写 `review_protocol:` 字段 (按 REVIEW_POLICY.md §3 格式即可)
- 各 task 在 PLAN.md 内的排序 (但 GOVERNANCE-01 dry-run check 应当**最后**跑, 因为它要求其他 4 个 cleanup 的 task 已经标好 review_protocol 字段)

</decisions>

<specifics>
## Specific Ideas

- **Phase 4a 实证基线**: REVIEW_POLICY.md §1 表格列了 T1-T7 实测结果, 4.0 task 拆分时可对照 (T2/T3/T4 配置改动 = 直接落; T5/T6 新组件 = two-stage)。4.0 全部 task 性质类似 T2/T3 (doc 修改 + 配置文件创建), 应当全 🟢/🟡, 无 🔴。
- **D120 ambiguity 实例**: 当前 `grep D120 docs/design/EAASP/DEFERRED_LEDGER.md` 返回 line 326 (P1-defer stale) + line 339 (closed) + line 328/341 (D122/D135 衍生) —— 需要修后 grep 第一行就读到 closed 状态。
- **Provenance marker 类似先例**: ADR plugin 已经有 `<!-- @adr-trace ADR-V2-XXX -->` marker 模式, chunk_type sync marker 沿用相同风格保持视觉一致。
- **CODEOWNERS 5 路径与 ADR-V2-023 §P2 一致**: ADR 列出的 dormant 列表是 `grid-server` / `grid-platform` / `grid-desktop` / `web/` / `web-platform/`, CODEOWNERS 用相同的 5 条, 维护时单点更新 ADR 即可同步推 CODEOWNERS。

</specifics>

<canonical_refs>
## Canonical References

**Downstream agents (researcher / planner) MUST read these before planning or implementing.**

### ROADMAP / GSD 上游
- `.planning/ROADMAP.md` §Phase 4.0 — phase goal + 5 success criteria + Requirements 列表 (CLEANUP-01..04 + GOVERNANCE-01)
- `.planning/REQUIREMENTS.md` — CLEANUP-01..04 / GOVERNANCE-01 详细描述 + traceability table
- `.planning/STATE.md` — 当前 phase position + cross-milestone watchlist (D109/D134/D136/D142/D143/NEW-D2/NEW-E2/NEW-E3, 不在 4.0 处理)

### Review policy 与 enforcement
- `.planning/REVIEW_POLICY.md` — 8 sections 完整 policy 草稿 (Phase 4a T1-T7 实测起草)
- `.planning/REVIEW_POLICY.md` §2 — 三档 risk tiers + 11 条 high-risk trigger conditions (planner 用以判定每个 4.0 task 的 review_protocol 字段)
- `.planning/REVIEW_POLICY.md` §3 — PLAN.md task frontmatter `review_protocol:` 字段格式

### ADR 锚点
- `docs/design/EAASP/adrs/ADR-V2-021-chunk-type-contract.md` §4 — canonical 8 个 chunk_type wire 值清单 (CLEANUP-01 替换的 source of truth)
- `docs/design/EAASP/adrs/ADR-V2-023-grid-two-leg-product-strategy.md` §P2 — Leg-B 5 条 dormant 路径 (CODEOWNERS 路径列表)
- `docs/design/EAASP/adrs/ADR-V2-023-grid-two-leg-product-strategy.md` §Enforcement — 5 个检查点 (CLEANUP-03 checklist 内容来源)

### Cleanup 目标文件 (要修改)
- `docs/design/EAASP/L1_RUNTIME_ADAPTATION_GUIDE.md` §4 (line 92 表格) + line 472 / 492 TypeScript 示例 (CLEANUP-01 替换目标)
- `docs/design/EAASP/DEFERRED_LEDGER.md` line 326 (D120 stale row) + line 339 (D120 closed row) + preamble (CLEANUP-02 修改目标)

### Phase 4 baseline / agenda
- `.planning/phases/4.1-PRE-AUDIT-NOTES.md` §A-§F — Phase 4.1 baseline (规范评估 §F 已加, 但 4.0 不依赖 §F 内容)
- `docs/external-review/2026-04-26-eaasp-skill-spec-coverage-internal.md` — EAASP 平台对调度 skill 规范 v0.1 承接评估 (commit c6dadbc, 喂 4.1 audit, 不影响 4.0)

### 不要修改的 SSOT 例外
- `docs/design/EAASP/DEFERRED_LEDGER.md` — 是 SSOT, 4.0 只解 D120 row 与 preamble convention, 不要顺手清理其他 D-item
- `docs/dev/WORK_LOG.md` — Phase 4a archive, 不在 4.0 触碰

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets

- **`.planning/REVIEW_POLICY.md`**: 已存在 8 sections 完整 draft, planner 直接引用 §2 / §3, 不需要重写
- **ADR-V2-021 §4 canonical 8 个 wire 值**: 已在 ADR 内, CLEANUP-01 直接 reference + 抄表头
- **ADR-V2-023 §Enforcement 检查点**: 已写, CLEANUP-03 checklist 直接基于此 5 项展开
- **ADR-V2-023 §P2 dormant 路径列表**: 已写, CLEANUP-04 CODEOWNERS 直接对齐
- **GSD `/gsd-execute-phase` 自带 atomic commit logic**: 不需要在 4.0 内自定义 commit 工具, 用 GSD 默认即可

### Constraints (从 socratic 浮现)

- **Phase 4.0 任务全 doc-level**, 不命中 REVIEW_POLICY §2 任何 high-risk trigger —— 全部 task 应当落 🟡 / 🟢 review_protocol
- **ROADMAP success criteria #5 中 "任何 task 失败时 superpowers two-stage 机制能正确激活" 不在 4.0 实测**, 推迟到 4.1 第一个真实 audit task —— 4.0 完成不代表 #5 已硬验证, 但完成 #5 的**前置 (REVIEW_POLICY.md 字段格式 + risk tier 判定逻辑)**
- **D-C-03 atomic commit 期望**: GSD plumbing 验证不只是"phase 跑通", 还要"每 task 一个 commit", 4.0 实测产出 5-6 个 commits 是 plumbing 健康的信号

### Implementation Notes

- CLEANUP-01 / 02 / 03 / 04 之间无依赖, 可任意顺序执行
- GOVERNANCE-01 dry-run check 应当**最后**跑 (D-G01-04), 但若它发现某 task 标错 review_protocol, 应当能把那个 task 状态改回 in_progress 让 fix
- 整个 4.0 不引入新 dependency, 不动 Cargo.toml / pyproject.toml, 不写 Rust / Python 代码

</code_context>

<deferred>
## Deferred Ideas

显式不在本 phase 解决, 留给后续 phase 或独立时点:

1. **PR template 强制 dormancy-justification check** (CODEOWNERS 配套增强) —— 推迟到 Phase 4.1 或独立 governance phase
2. **REVIEW_POLICY.md draft → Active 翻转** —— 推迟到 Phase 4.1 第一次 superpowers two-stage 实战激活后翻转 (D-G01-05)
3. **Retroactive 修改历史 closed D-item 用新 row-edit-on-close convention** —— 仅修 D120, 历史 closed item 保留时间线 (D-02-03)
4. **Codegen 工具自动同步 chunk_type wire 值到 L1_RUNTIME_ADAPTATION_GUIDE** —— 当前一次性手改, 未来若漂移多次再考虑写工具 (D-01-04)
5. **跨 milestone watchlist** D109 / D134 / D136 / D142 / D143 / NEW-D2 / NEW-E2 / NEW-E3 —— STATE.md 已记, 不在 4.0 处理 (cross-milestone)
6. **Phase 4.1 audit agenda Q1-Q4** —— 已写入 4.1-PRE-AUDIT-NOTES.md §F, 等 Phase 4.1 跑 audit 时回答, 不在 4.0 范围

</deferred>

---

*Phase: 04.0-bootstrap-cleanup-gsd*
*Context gathered: 2026-04-27 via discuss-phase (5 gray areas, all Claude recommendations accepted by user)*
*Next step: re-enter `/gsd-plan-phase 4.0` to spawn researcher (--research selected) + planner*
