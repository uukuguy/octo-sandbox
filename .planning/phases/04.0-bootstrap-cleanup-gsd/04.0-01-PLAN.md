---
phase: 04.0
slug: bootstrap-cleanup-gsd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/design/EAASP/L1_RUNTIME_ADAPTATION_GUIDE.md
  - docs/design/EAASP/DEFERRED_LEDGER.md
  - docs/reviews/strategy-grid-two-leg-checklist.md
  - .github/CODEOWNERS
autonomous: true
requirements:
  - CLEANUP-01
  - CLEANUP-02
  - CLEANUP-03
  - CLEANUP-04
  - GOVERNANCE-01
must_haves:
  truths:
    - "L1_RUNTIME_ADAPTATION_GUIDE.md §4 chunk_type 表使用 ADR-V2-021 canonical wire 值（7 行 active），无 stale `text` / `tool_call` / `hook_fired` / `pre_compact`"
    - "L1_RUNTIME_ADAPTATION_GUIDE.md 顶部 blockquote 含 ADR-V2-021 source-of-truth 声明 + §4 heading 上方有 `<!-- @chunk-type-sync ADR-V2-021 -->` provenance marker"
    - "DEFERRED_LEDGER.md D120 row（line 326）首个 grep 命中含 `closed` 字样 + 引用 commit `7e083c7`"
    - "DEFERRED_LEDGER.md preamble 含 row-edit-on-close convention 段（紧接 `引入流程` 段后）"
    - "docs/reviews/strategy-grid-two-leg-checklist.md 文件存在，含 ADR-V2-023 §Enforcement 5 个 verbatim checkpoints（markdown 复选框）"
    - ".github/CODEOWNERS 文件存在，5 条 Leg-B filesystem-correct 路径（crates/grid-server/, crates/grid-platform/, crates/grid-desktop/, web/, web-platform/）全标 @firmwwwee"
    - "本 PLAN.md 含 ≥4 个 task 带 `review_protocol:` HTML-comment 注释（GOVERNANCE-01 dry-run pass 前提）"
  artifacts:
    - path: "docs/design/EAASP/L1_RUNTIME_ADAPTATION_GUIDE.md"
      provides: "L1 runtime 适配指南 §4 chunk_type 表 ADR-V2-021 对齐 + provenance marker"
      contains: "<!-- @chunk-type-sync ADR-V2-021 -->"
    - path: "docs/design/EAASP/DEFERRED_LEDGER.md"
      provides: "D120 closed status 显式化 + row-edit-on-close convention 引入"
      contains: "关闭/状态变更流程"
    - path: "docs/reviews/strategy-grid-two-leg-checklist.md"
      provides: "ADR-V2-023 §Enforcement 5 检查点 PR reviewer checklist"
      contains: "Grid 两腿战略"
    - path: ".github/CODEOWNERS"
      provides: "5 条 Leg-B 路径 dormancy reviewer routing"
      contains: "@firmwwwee"
  key_links:
    - from: "docs/design/EAASP/L1_RUNTIME_ADAPTATION_GUIDE.md §4"
      to: "ADR-V2-021 §4 (canonical 7 wire 值)"
      via: "<!-- @chunk-type-sync ADR-V2-021 --> + 顶部 source-of-truth 声明"
      pattern: "<!-- @chunk-type-sync ADR-V2-021 -->"
    - from: "docs/design/EAASP/DEFERRED_LEDGER.md L326"
      to: "Phase 2.5 S0.T3 commit 7e083c7"
      via: "row 末尾 close-out trace 字符串"
      pattern: "Closed @ Phase 2.5 S0.T3 commit"
    - from: ".github/CODEOWNERS"
      to: "docs/reviews/strategy-grid-two-leg-checklist.md"
      via: "GitHub PR auto-routing → reviewer 拷贝 checklist"
      pattern: "@firmwwwee"
---

<objective>
Phase 4.0 Bootstrap & Cleanup — 一次性扫掉 Phase 4a 遗留 4 项 doc-level cleanup（CLEANUP-01..04）+ GOVERNANCE-01 dry-run，作为 GSD 治理底座的 tracer-bullet 验证。

Purpose:
- 关 Phase 4a project review 浮现的 P0/P1 cleanup 队列（D120 ambiguity / chunk_type 漂移 / strategy checklist 缺位 / CODEOWNERS 不存在）
- 通过 5 个 atomic-commit-per-task 验证 GSD `/gsd-execute-phase` plumbing 在 brownfield repo 工作
- 为 Phase 4.1 audit 提供干净起点（4.1 第一个真实 audit task 才会激活 superpowers two-stage 验证）

Output:
- 4 个文件改动 + 1 个 dry-run 验证（zero-diff if pass）= 4-5 commits
- ROADMAP §Phase 4.0 5 个 success criteria 全部命中（VALIDATION.md Phase Gate composite check 全 PASS）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/REVIEW_POLICY.md
@.planning/phases/04.0-bootstrap-cleanup-gsd/04.0-CONTEXT.md
@.planning/phases/04.0-bootstrap-cleanup-gsd/04.0-RESEARCH.md
@.planning/phases/04.0-bootstrap-cleanup-gsd/04.0-PATTERNS.md
@.planning/phases/04.0-bootstrap-cleanup-gsd/04.0-VALIDATION.md
@docs/design/EAASP/adrs/ADR-V2-021-chunk-type-contract-freeze.md
@docs/design/EAASP/adrs/ADR-V2-023-grid-two-leg-product-strategy.md
@docs/design/EAASP/L1_RUNTIME_ADAPTATION_GUIDE.md
@docs/design/EAASP/DEFERRED_LEDGER.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 (CLEANUP-01): L1_RUNTIME_ADAPTATION_GUIDE.md §4 chunk_type sweep + ADR-V2-021 provenance marker</name>

  <!-- meta
  review_protocol: skip
  review_trigger: REVIEW_POLICY §2.3 rules 1+3+4 (单文件 doc microedit, <50 LOC delta, 不引入新依赖)
  review_estimated_overhead: 0
  -->

  <files>docs/design/EAASP/L1_RUNTIME_ADAPTATION_GUIDE.md</files>

  <read_first>
    - docs/design/EAASP/L1_RUNTIME_ADAPTATION_GUIDE.md (整个文件,但重点是 L1-7 顶部 blockquote / L92-104 §4 表格 / L470-475 TS 示例 / L484-498 接口示例)
    - docs/design/EAASP/adrs/ADR-V2-021-chunk-type-contract-freeze.md §4 (canonical 7 active wire 值 source of truth)
  </read_first>

  <action>
本任务实现 CLEANUP-01 per CONTEXT.md D-01-01..04 + RESEARCH.md "Stale strings" 段 + RESEARCH OQ1 (user 选 A，全采纳 ADR-V2-021 §4 7 行 active 集)。

**所有改动手写 Edit, 不写 sed/codegen 工具** (D-01-04)。文件中文 (CLAUDE.md doc lang rule)。

### Step 1.1 — 顶部 blockquote 加 ADR-V2-021 引用 + sync 声明 (per D-01-02)

修改 L3-5 现有 blockquote 块，**追加两行**(保持现 3 行不变,在 L5 后插入 L6-7 两行)：

修改前 (L3-5):
```markdown
> **版本**: v1.0 (Phase 2.5 S2.T1)
> **适用范围**: 任何希望成为 EAASP L1 Runtime 的代理执行运行时
> **参考 ADR**: ADR-V2-006 (Hook 信封协议), ADR-V2-017 (L1 生态策略), ADR-V2-019 (部署模型)
```

修改后 (L3-7,在原 L5 末尾追加 ", **ADR-V2-021 (chunk_type 契约)**" + 新增 L6 sync 声明,保留 L7 空行):
```markdown
> **版本**: v1.0 (Phase 2.5 S2.T1)
> **适用范围**: 任何希望成为 EAASP L1 Runtime 的代理执行运行时
> **参考 ADR**: ADR-V2-006 (Hook 信封协议), ADR-V2-017 (L1 生态策略), ADR-V2-019 (部署模型), **ADR-V2-021 (chunk_type 契约)**
> **chunk_type 同步**: 本文 chunk_type wire 值以 ADR-V2-021 为 source of truth, 任何漂移 PR 必须同步更新本文 §4 表格
```

### Step 1.2 — §4 heading 上方插入 provenance marker (per D-01-01)

在 L92 `## 4. Send 事件类型语义（7 种 chunk_type）` 之前插入 1 行：
```markdown
<!-- @chunk-type-sync ADR-V2-021 -->
```

**Heading 文本保持 "7 种"** (per RESEARCH "Hidden Gotchas #4": 删除 hook_fired / pre_compact 后，新增 thinking / workflow_continuation，最终表格 7 active rows，heading "7 种" 仍准确)。

### Step 1.3 — §4 表格行替换 (per RESEARCH "Stale strings" 段 verbatim)

**当前表格 L96-104 共 9 行 (1 表头 + 1 分隔 + 7 数据)**。逐行修改：

- L98 (text → text_delta):
  当前: `| `"text"` | LLM 生成的文本片段 | `content` | LLM 返回非 tool_call 内容 |`
  改为: `| `text_delta` | LLM 生成的文本片段（流式 increment） | `content` | LLM 返回非 tool_call 内容 |`

- L99 (tool_call → tool_start):
  当前: `| `"tool_call"` | 代理请求执行工具 | `tool_name`, `tool_id`, `content`（参数 JSON） | LLM 返回 tool_calls |`
  改为: `| `tool_start` | 代理请求执行工具 | `tool_name`, `tool_id`, `content`（参数 JSON） | LLM 返回 tool_calls |`

- L100 (tool_result, 已 canonical, 仅去引号):
  当前: `| `"tool_result"` | 工具执行结果 | `tool_name`, `tool_id`, `content`, `is_error` | 工具执行完毕 |`
  改为: `| `tool_result` | 工具执行结果 | `tool_name`, `tool_id`, `content`, `is_error` | 工具执行完毕 |`

- L101 (done, 已 canonical, 仅去引号):
  当前: `| `"done"` | 会话轮次正常结束 | `content`（最终输出） | 代理停止（无更多 tool_calls） |`
  改为: `| `done` | 会话轮次正常结束 | `content`（最终输出） | 代理停止（无更多 tool_calls） |`

- L102 (error, 已 canonical, 仅去引号):
  当前: `| `"error"` | 不可恢复错误 | `content`（错误信息）, `is_error=true` | Provider 异常、超出 max_turns 等 |`
  改为: `| `error` | 不可恢复错误 | `content`（错误信息）, `is_error=true` | Provider 异常、超出 max_turns 等 |`

- L103 (`hook_fired` row): **DELETE 整行**（ADR-V2-021 §4 enum 不含此值；hook_fired 是 HookEvent kind 而非 chunk_type，原文档跨契约混淆）

- L104 (`pre_compact` row): **DELETE 整行**（同上，PreCompact 是 HookEvent ADR-V2-018，不是 chunk_type wire）

**插入 2 个新行** (per ADR-V2-021 §4 active 集补全)：在 L102 (error) 之后、原 L103/L104 删除位置：

新行 A (thinking):
```markdown
| `thinking` | 思考链（推理过程，可选） | `content` | LLM 内部推理过程（extended reasoning） |
```

新行 B (workflow_continuation):
```markdown
| `workflow_continuation` | 跨段工作流延续 | `content`（"attempt N/M"），`event` | 长任务跨 turn 续航（D87 observability） |
```

最终表格 7 数据行顺序: text_delta / thinking / tool_start / tool_result / done / error / workflow_continuation （与 ADR-V2-021 §4 active 集一致，匹配 heading "7 种"）。

### Step 1.4 — TS 示例 L472 修复 (per RESEARCH §"Location 2")

修改前:
```typescript
yield { chunk_type: "text", content: "Processing...", session_id: sessionId, ... };
```

修改后:
```typescript
yield { chunk_type: "text_delta", content: "Processing...", session_id: sessionId, ... };
```

L474 (`"done"`) 已是 canonical, 不动。

### Step 1.5 — TS 接口注释 L492 修复 (per RESEARCH §"Location 3")

修改前:
```typescript
chunk_type: string;   // "text" | "tool_call" | "tool_result" | "done" | "error"
```

修改后:
```typescript
chunk_type: string;   // "text_delta" | "thinking" | "tool_start" | "tool_result" | "done" | "error" | "workflow_continuation"
```

### Commit

`docs(planning): CLEANUP-01 chunk_type sweep + ADR-V2-021 marker`

Body 答 why: chunk_type wire 值漂移会让新 L1 runtime 作者跟旧文档实现 → contract test 必 fail；引入 provenance marker 锁未来漂移。Footer per CLAUDE.md global commit rule。
  </action>

  <acceptance_criteria>
    自动验证（VALIDATION.md §"Per-Task Verification Map" T1 verbatim）:

    1. **Stale strings 完全清除** (must produce zero matches):
       ```bash
       ! grep -nE '\| *`?"(text|tool_call|hook_fired|pre_compact)"?` *\|' docs/design/EAASP/L1_RUNTIME_ADAPTATION_GUIDE.md
       ```

    2. **Provenance marker 存在**:
       ```bash
       grep -F '<!-- @chunk-type-sync ADR-V2-021 -->' docs/design/EAASP/L1_RUNTIME_ADAPTATION_GUIDE.md
       ```

    3. **顶部 source-of-truth 声明存在**:
       ```bash
       grep -F 'chunk_type 同步' docs/design/EAASP/L1_RUNTIME_ADAPTATION_GUIDE.md
       grep -F 'ADR-V2-021 (chunk_type 契约)' docs/design/EAASP/L1_RUNTIME_ADAPTATION_GUIDE.md
       ```

    4. **§4 表格 7 active rows + ADR-V2-021 canonical 全集** (text_delta, thinking, tool_start, tool_result, done, error, workflow_continuation 各出现至少一次于表格段):
       ```bash
       for v in text_delta thinking tool_start tool_result done error workflow_continuation; do
         grep -q "\`$v\`" docs/design/EAASP/L1_RUNTIME_ADAPTATION_GUIDE.md || { echo "MISSING: $v"; exit 1; }
       done
       echo "all 7 canonical wire values present"
       ```

    5. **TS 示例 L472 已修**:
       ```bash
       grep -F 'chunk_type: "text_delta"' docs/design/EAASP/L1_RUNTIME_ADAPTATION_GUIDE.md
       ```

    6. **TS 接口注释 L492 已修** (新 union list 完整出现):
       ```bash
       grep -F '"text_delta" | "thinking" | "tool_start" | "tool_result" | "done" | "error" | "workflow_continuation"' docs/design/EAASP/L1_RUNTIME_ADAPTATION_GUIDE.md
       ```

    7. **Heading 仍是 "7 种"** (Hidden Gotcha #4 防回归):
       ```bash
       grep -F '## 4. Send 事件类型语义（7 种 chunk_type）' docs/design/EAASP/L1_RUNTIME_ADAPTATION_GUIDE.md
       ```

    全部 7 项 PASS = 任务完成。
  </acceptance_criteria>

  <verify>
    <automated>bash -c 'set -e; F=docs/design/EAASP/L1_RUNTIME_ADAPTATION_GUIDE.md; ! grep -nE "\| *\`?\"(text|tool_call|hook_fired|pre_compact)\"?\` *\|" $F && grep -F "<!-- @chunk-type-sync ADR-V2-021 -->" $F && grep -F "chunk_type 同步" $F && for v in text_delta thinking tool_start tool_result done error workflow_continuation; do grep -q "\`$v\`" $F; done && grep -F "chunk_type: \"text_delta\"" $F && grep -F "## 4. Send 事件类型语义（7 种 chunk_type）" $F && echo CLEANUP-01 PASS'</automated>
  </verify>

  <done>
    - L3-7 blockquote 块 4 行（含 ADR-V2-021 引用 + sync 声明）
    - L92 上方 provenance marker
    - §4 表格 7 数据行（ADR-V2-021 canonical 全集）
    - L472 TS 示例使用 `text_delta`
    - L492 TS 接口注释含完整 7 wire 值 union
    - heading 仍 "7 种"
    - Commit subject `docs(planning): CLEANUP-01 chunk_type sweep + ADR-V2-021 marker` ≤72 chars
    - Commit footer 含 Generated-By + Co-Authored-By 两行
  </done>
</task>

<task type="auto">
  <name>Task 2 (CLEANUP-02): DEFERRED_LEDGER.md D120 row edit-in-place + row-edit-on-close convention</name>

  <!-- meta
  review_protocol: skip
  review_trigger: REVIEW_POLICY §2.3 rules 1+3+4 (单文件 doc microedit, <50 LOC delta)
  review_estimated_overhead: 0
  -->

  <files>docs/design/EAASP/DEFERRED_LEDGER.md</files>

  <read_first>
    - docs/design/EAASP/DEFERRED_LEDGER.md (重点 L260-271 引入流程 preamble + L326 D120 stale row + L339 D120 closure log row)
    - .planning/phases/04.0-bootstrap-cleanup-gsd/04.0-RESEARCH.md §"CLEANUP-02 — D120 Current State + Preamble Gap" (verbatim 模板)
    - .planning/phases/04.0-bootstrap-cleanup-gsd/04.0-PATTERNS.md §"CLEANUP-02" (preamble pattern + D-item row 4-column 模板 + closure-marker bold pattern)
  </read_first>

  <action>
本任务实现 CLEANUP-02 per CONTEXT.md D-02-01..04 + RESEARCH "CLEANUP-02" 段。

**核心约束** (CONTEXT.md D-02-03):
- 仅 D120 row edit-in-place + 加 preamble convention
- **不 retroactively 修历史所有 closed D-item**（保留多 row 时间线）
- L339 (状态变更日志中的 D120 closure 行) **不动**（append-only 日志）

文件中文 (CLAUDE.md doc lang rule)。

### Step 2.1 — D120 row edit-in-place (L326) per D-02-01

**当前 L326 stale row**:
```
| 2026-04-15 | D120 | **新增** 🟡 P1-defer | **Cross-runtime envelope parity**：Rust `HookContext::to_json/to_env_vars` 缺 ADR-V2-006 §2/§3 字段（`event` / `skill_id` / `draft_memory_id` / `evidence_anchor_id` / `created_at` / `GRID_EVENT` / `GRID_SKILL_ID`），Python 已符合。S3.T5 reviewer M1，前置 Phase 2.5 goose 契约测试 → 补 HookContext 扩展 + cross-runtime parity tests |
```

**改为** (verbatim, RESEARCH.md §"CLEANUP-02" + PATTERNS.md §"Edit-in-place template" 已 vetted):
```
| 2026-04-15 → 2026-04-16 | D120 | ✅ closed | **Cross-runtime envelope parity**：Rust `HookContext::to_json/to_env_vars` 缺 ADR-V2-006 §2/§3 字段（`event` / `skill_id` / `draft_memory_id` / `evidence_anchor_id` / `created_at` / `GRID_EVENT` / `GRID_SKILL_ID`），Python 已符合。S3.T5 reviewer M1。**Closed @ Phase 2.5 S0.T3 commit `7e083c7`** — HookContext 扩展 + 10 parity tests + byte-parity ADR §2.1/2.2/2.3 canonical JSON 锁定 |
```

**关键变化**:
1. 日期: `2026-04-15` → `2026-04-15 → 2026-04-16`（引入到关闭跨度）
2. 状态: `**新增** 🟡 P1-defer` → `✅ closed`
3. 备注末尾: 原 "前置 Phase 2.5 goose 契约测试 → 补 HookContext 扩展 + cross-runtime parity tests" 替换为 "**Closed @ Phase 2.5 S0.T3 commit `7e083c7`** — HookContext 扩展 + 10 parity tests + byte-parity ADR §2.1/2.2/2.3 canonical JSON 锁定"

**不动的位置**:
- L328: D122 row 中 "→ D120 统一修" 字样（forward-reference, 信息性）
- L339: D120 closure 行在 状态变更日志（append-only history）
- L341: D135 row 中 "可能掩盖 D120 回归" 字样（test-mask reference）

### Step 2.2 — Preamble 加 row-edit-on-close convention (per D-02-02)

在现有 `**引入流程**` 块（L265-269）**之后**、`---` 分隔线（L271）**之前**，插入新 sibling 块（mirror "引入流程" bold-header + numbered-list shape per PATTERNS.md §"Preamble pattern"）：

**插入位置**: L269 后（`4. 在 [状态变更日志](#状态变更日志) 新增一行` 行后）。先插入 1 个空行，再插入：

```markdown

**关闭/状态变更流程** (新增 2026-04-27, Phase 4.0 CLEANUP-02):
1. 在原 D-item row 直接修改 status 字段（例如 `🟡 P1-defer → ✅ closed`）
2. 在 row 末尾追加 close-out trace（commit hash + Phase/Task 编号 + 一句话 closure summary）
3. 在 [状态变更日志](#状态变更日志) 同步追加一行（保留历史时间线，append-only）
4. 仅适用于**未来**新关闭的 D-item；**已关闭的历史 D-item 不强制 retroactive 改写**
```

(注意首行带前导空行以与上一个 numbered list 视觉分离。)

### Commit

`docs(planning): CLEANUP-02 D120 row-edit + close-out convention`

Body 答 why: D120 状态在 ledger 中歧义（L326 仍说 P1-defer，L339 显示已 closed），单行 grep 无法判定真实状态；引入 row-edit-on-close convention 防 future D-item 复发同样歧义。Footer per CLAUDE.md.
  </action>

  <acceptance_criteria>
    自动验证（VALIDATION.md §"Per-Task Verification Map" T2 verbatim）:

    1. **D120 first grep 命中含 `closed`** (D-02-04 success criterion):
       ```bash
       grep '^.*D120.*' docs/design/EAASP/DEFERRED_LEDGER.md | head -1 | grep -q 'closed'
       ```

    2. **L326 row 含 commit hash 引用**:
       ```bash
       grep -F 'Closed @ Phase 2.5 S0.T3 commit `7e083c7`' docs/design/EAASP/DEFERRED_LEDGER.md
       ```

    3. **Preamble convention 段已加**:
       ```bash
       grep -qE '关闭/状态变更流程' docs/design/EAASP/DEFERRED_LEDGER.md
       ```

    4. **Convention 段 4 个 numbered items 完整**:
       ```bash
       awk '/关闭\/状态变更流程/,/^---/' docs/design/EAASP/DEFERRED_LEDGER.md | grep -cE '^[0-9]\. ' | xargs test 4 -eq
       ```

    5. **历史日志未被破坏** (L339 仍含 D120 closure 行):
       ```bash
       grep -c '🟡 P1-defer → ✅ closed' docs/design/EAASP/DEFERRED_LEDGER.md | xargs test 1 -le
       ```
       （状态变更日志 L339 的 closure 行保留，不动）

    全部 5 项 PASS = 任务完成。
  </acceptance_criteria>

  <verify>
    <automated>bash -c 'set -e; F=docs/design/EAASP/DEFERRED_LEDGER.md; grep "^.*D120.*" $F | head -1 | grep -q closed && grep -F "Closed @ Phase 2.5 S0.T3 commit \`7e083c7\`" $F && grep -qE "关闭/状态变更流程" $F && [ "$(awk "/关闭\/状态变更流程/,/^---/" $F | grep -cE "^[0-9]\. ")" -ge 4 ] && echo CLEANUP-02 PASS'</automated>
  </verify>

  <done>
    - L326 row 状态字段改为 `✅ closed`，备注末尾追加 commit `7e083c7` 引用
    - L269 后插入 `**关闭/状态变更流程**` 4-step numbered list 块
    - L339 (状态变更日志 D120 closure 行) **未动**
    - L328 / L341 (forward-references) **未动**
    - Commit subject `docs(planning): CLEANUP-02 D120 row-edit + close-out convention` ≤72 chars
    - Commit footer 含 Generated-By + Co-Authored-By 两行
  </done>
</task>

<task type="auto">
  <name>Task 3 (CLEANUP-03): 创建 docs/reviews/strategy-grid-two-leg-checklist.md 含 ADR-V2-023 §Enforcement 5 verbatim checkpoints</name>

  <!-- meta
  review_protocol: gsd-standard
  review_trigger: REVIEW_POLICY §2.2 rule 3 (新增 doc 30-50 LOC, 需要跨 ADR 语义提取保真度审视)
  review_estimated_overhead: +10min
  -->

  <files>docs/reviews/strategy-grid-two-leg-checklist.md</files>

  <read_first>
    - docs/design/EAASP/adrs/ADR-V2-023-grid-two-leg-product-strategy.md §Enforcement (L213-228, 5 个 verbatim checkpoints + §P5 触发条件 + §P2 dormancy 原则)
    - .planning/REVIEW_POLICY.md L1-5 (preamble blockquote pattern weak analog A per PATTERNS.md §"CLEANUP-03")
    - .planning/phases/04.0-bootstrap-cleanup-gsd/04.0-RESEARCH.md §"CLEANUP-03" (5 verbatim checkpoints 已枚举)
  </read_first>

  <action>
本任务实现 CLEANUP-03 per CONTEXT.md D-03-01..04 + RESEARCH "CLEANUP-03" 段 verbatim 5 checkpoints。

**核心约束**:
- 5 个 checkpoint 文本**直接从 ADR-V2-023 L224-228 verbatim copy**, 不 paraphrase (D-03-02)
- Checklist 形态用 markdown `- [ ]` 复选框 (D-03-03), reviewer 拷贝到 PR comment 逐项 check
- Preamble ≤5 行 (D-03-04), 说明触发条件 + CODEOWNERS 自动 invoke
- 文件**中文** (CLAUDE.md doc lang rule, ADR-V2-023 §Enforcement 已是中文，verbatim 即合规)
- 目录 `docs/reviews/` **不存在**, Write 文件时同时创建目录 (Write 工具自动 mkdir -p)

### Step 3.1 — 创建文件 docs/reviews/strategy-grid-two-leg-checklist.md

完整内容（PATTERNS.md §"CLEANUP-03" weak analog A blockquote-tagged metadata 风格 + RESEARCH §"CLEANUP-03" verbatim 5 checkpoints）：

```markdown
# Strategy: Grid 两腿战略 PR Review Checklist

> **触发条件**: PR 触碰 `crates/grid-server/` / `crates/grid-platform/` / `crates/grid-desktop/` / `web/` / `web-platform/` 任一路径时，reviewer 复制本清单到 PR comment 逐项 check（CODEOWNERS 自动指派 @firmwwwee）
> **Source of truth**: `docs/design/EAASP/adrs/ADR-V2-023-grid-two-leg-product-strategy.md` §Enforcement L213-228
> **Last sync**: 2026-04-27 (Phase 4.0 CLEANUP-03 initial creation)

---

## Checklist (5 items, ADR-V2-023 §Enforcement L224-228 verbatim)

- [ ] 此 PR 是否触碰 `grid-platform` 或 `web*`？如是，是否必须（见 P2 原则，原则上当前不应动）？
- [ ] 此 PR 在 `grid-engine` / `grid-runtime` / `grid-types` / `grid-sandbox` / `grid-hook-bridge` 任何一个里加了只服务某一条腿的分支？（违反 P1）
- [ ] 此 PR 在 `tools/eaasp-*/` 里实现了 Grid 独有业务逻辑？（违反 P3）
- [ ] 此 PR 是否声称"为腿 B 准备"而实际属于 P5 未触发的预热代码？（违反 P2）
- [ ] 如果 P5 触发条件满足，是否创建了对应的 ADR 和 Phase plan？

## Reviewer 工作流

1. PR 触碰 5 路径之一 → CODEOWNERS auto-assign @firmwwwee 为 reviewer
2. Reviewer 在 PR 描述中确认任务背景属于 ADR-V2-023 §P5 4 条触发条件之一（否则按 dormancy 拒绝）
3. 复制本 5 条 checklist 到 PR comment, 逐项 ☑
4. 任一未 ☑ → block merge, 推动作者补 dormancy-justification
5. 全部 ☑ → approve

## 参考

- ADR-V2-023 (`docs/design/EAASP/adrs/ADR-V2-023-grid-two-leg-product-strategy.md`)
  - §P1 — Core first, package later
  - §P2 — 腿 A 当前绝对优先 (5 条 dormant 路径)
  - §P5 — 腿 B 激活触发条件
  - §Enforcement — 本 checklist 的 source of truth
- `.github/CODEOWNERS` (本文件 trigger 入口)
- `.planning/REVIEW_POLICY.md` (Grid 整体 review tier; 本 checklist 与 high-risk superpowers two-stage **正交不重叠**)
```

### Step 3.2 — 验证 5 个 checkpoint 与 ADR-V2-023 L224-228 byte-equal

执行 `diff <(grep '^- \[ \]' docs/reviews/strategy-grid-two-leg-checklist.md) <(sed -n '224,228p' docs/design/EAASP/adrs/ADR-V2-023-grid-two-leg-product-strategy.md)` 应当 zero output (5 行完全一致)。

### Commit

`docs(reviews): CLEANUP-03 add Grid two-leg PR review checklist`

Body 答 why: ADR-V2-023 §Enforcement 引用此文件但磁盘上不存在; CODEOWNERS (CLEANUP-04) 自动 invoke 它做 dormancy gate. Footer per CLAUDE.md.
  </action>

  <acceptance_criteria>
    自动验证（VALIDATION.md §"Per-Task Verification Map" T3 + 加强）:

    1. **文件存在**:
       ```bash
       test -f docs/reviews/strategy-grid-two-leg-checklist.md
       ```

    2. **≥5 个 markdown checkbox**:
       ```bash
       [ "$(grep -c '^- \[ \]' docs/reviews/strategy-grid-two-leg-checklist.md)" -ge 5 ]
       ```

    3. **5 个 checkpoint 与 ADR-V2-023 L224-228 byte-equal** (verbatim 保真):
       ```bash
       diff <(grep '^- \[ \]' docs/reviews/strategy-grid-two-leg-checklist.md | head -5) <(sed -n '224,228p' docs/design/EAASP/adrs/ADR-V2-023-grid-two-leg-product-strategy.md)
       ```
       (zero output = byte-equal)

    4. **Preamble blockquote 含触发条件**:
       ```bash
       grep -F 'crates/grid-server/' docs/reviews/strategy-grid-two-leg-checklist.md
       grep -F 'crates/grid-platform/' docs/reviews/strategy-grid-two-leg-checklist.md
       grep -F 'crates/grid-desktop/' docs/reviews/strategy-grid-two-leg-checklist.md
       grep -F 'web/' docs/reviews/strategy-grid-two-leg-checklist.md
       grep -F 'web-platform/' docs/reviews/strategy-grid-two-leg-checklist.md
       ```

    5. **Source of truth 引用**:
       ```bash
       grep -F 'ADR-V2-023' docs/reviews/strategy-grid-two-leg-checklist.md
       grep -F '§Enforcement' docs/reviews/strategy-grid-two-leg-checklist.md
       ```

    全部 5 组 PASS = 任务完成。
  </acceptance_criteria>

  <verify>
    <automated>bash -c 'set -e; F=docs/reviews/strategy-grid-two-leg-checklist.md; A=docs/design/EAASP/adrs/ADR-V2-023-grid-two-leg-product-strategy.md; test -f $F && [ "$(grep -c "^- \[ \]" $F)" -ge 5 ] && diff <(grep "^- \[ \]" $F | head -5) <(sed -n "224,228p" $A) && grep -F "crates/grid-server/" $F && grep -F "ADR-V2-023" $F && grep -F "§Enforcement" $F && echo CLEANUP-03 PASS'</automated>
  </verify>

  <done>
    - 文件 `docs/reviews/strategy-grid-two-leg-checklist.md` 存在
    - 含 5 个 ADR-V2-023 §Enforcement L224-228 verbatim 复制的 markdown 复选框
    - Preamble blockquote 4 行（触发条件 + Source of truth + Last sync）
    - "Reviewer 工作流" 段 5 步
    - "## 参考" 段引用 ADR-V2-023 §P1/P2/P5/Enforcement + CODEOWNERS + REVIEW_POLICY
    - Commit subject `docs(reviews): CLEANUP-03 add Grid two-leg PR review checklist` ≤72 chars
    - Commit footer 含 Generated-By + Co-Authored-By 两行
  </done>
</task>

<task type="auto">
  <name>Task 4 (CLEANUP-04): 创建 .github/CODEOWNERS 5 条 Leg-B filesystem-correct 路径</name>

  <!-- meta
  review_protocol: skip
  review_trigger: REVIEW_POLICY §2.3 rules 1+5 (单 config 文件, 无新依赖, GitHub-standard syntax 无逻辑判断)
  review_estimated_overhead: 0
  -->

  <files>.github/CODEOWNERS</files>

  <read_first>
    - docs/design/EAASP/adrs/ADR-V2-023-grid-two-leg-product-strategy.md §"共享 vs 专属组件清单" (L109-126, Leg-B 5 条路径来源) + §P2 (dormancy 原则) + §Enforcement (review checklist 引用)
    - .planning/phases/04.0-bootstrap-cleanup-gsd/04.0-RESEARCH.md §"CLEANUP-04" (filesystem path 验证 + GitHub CODEOWNERS syntax + Hidden Gotcha #1)
    - .github/workflows/phase4a-ccb-types-sync.yml L1-10 (PATTERNS.md §"CLEANUP-04" `#`-comment header 风格弱 analog)
  </read_first>

  <action>
本任务实现 CLEANUP-04 per CONTEXT.md D-04-01..04 + RESEARCH "CLEANUP-04" 段 + RESEARCH OQ2 user 选 A (filesystem-correct 路径，已写入 CONTEXT.md D-04-02)。

**核心约束**:
- 5 路径**用 filesystem-correct 形式** `crates/grid-server/`, `crates/grid-platform/`, `crates/grid-desktop/`, `web/`, `web-platform/` (NOT 顶层 `/grid-server/`, RESEARCH Hidden Gotcha #1: 顶层 `/grid-server/` 不会匹配任何文件，gate 静默失效)
- 每条标 `@firmwwwee` (D-04-01 self-review)
- 不引入虚拟 GitHub team (D-04-03)
- PR template 强制 dormancy-justification 不在 4.0 做（CONTEXT.md §deferred item 1）
- 文件**英文** + `#` 注释（GitHub CODEOWNERS syntax 不接受语言规则）

### Step 4.1 — 创建文件 .github/CODEOWNERS

完整内容（PATTERNS.md §"CLEANUP-04" `#`-comment header 风格 + RESEARCH §"Recommended CODEOWNERS content" verified template + 8-space alignment for readability）：

```
# CODEOWNERS — Leg-B dormancy reviewer per ADR-V2-023 §Enforcement
#
# Per ADR-V2-023 §P2, Leg-B components (grid-server, grid-platform, grid-desktop,
# web, web-platform) are dormant. Any PR touching these paths must justify
# necessity via docs/reviews/strategy-grid-two-leg-checklist.md.
#
# Reviewer: currently @firmwwwee (self-review during Leg-B dormancy; will be
# rerouted to a team handle when ADR-V2-023 §P5 activation triggers fire).
#
# Path syntax: gitignore-like, last-match-wins, leading-slash anchors to repo
# root. Trailing slash matches all under directory.
#
# Last sync: 2026-04-27 (Phase 4.0 CLEANUP-04 initial creation)

# Leg-B Rust crates (dormant, ADR-V2-023 §P2)
/crates/grid-server/        @firmwwwee
/crates/grid-platform/      @firmwwwee
/crates/grid-desktop/       @firmwwwee

# Leg-B frontend scaffolding (dormant, ADR-V2-023 §P2)
/web/                       @firmwwwee
/web-platform/              @firmwwwee
```

### Step 4.2 — 验证 5 路径与 ADR-V2-023 §P2 dormant 列表对齐

CONTEXT.md D-04-02 明确 (RESEARCH OQ2 修正后): `crates/grid-server/`, `crates/grid-platform/`, `crates/grid-desktop/`, `web/`, `web-platform/`. CODEOWNERS 文件中 5 条非注释行应当一一对应。

### Commit

`chore(github): CLEANUP-04 add CODEOWNERS for Leg-B dormancy`

Body 答 why: ADR-V2-023 §Enforcement 要求 Leg-B 路径变更必须 dormancy-justification reviewer; 此前无 CODEOWNERS,GitHub PR 自动指派机制不工作; 引入后 reviewer 自动获 ping，强制查 strategy-grid-two-leg-checklist.md (CLEANUP-03 创建). Footer per CLAUDE.md.
  </action>

  <acceptance_criteria>
    自动验证（VALIDATION.md §"Per-Task Verification Map" T4 + 加强）:

    1. **文件存在**:
       ```bash
       test -f .github/CODEOWNERS
       ```

    2. **≥5 条 @firmwwwee 行**:
       ```bash
       [ "$(grep -cE '@firmwwwee' .github/CODEOWNERS)" -ge 5 ]
       ```

    3. **5 路径全部 filesystem-correct** (RESEARCH Hidden Gotcha #1 防回归 — 顶层 `/grid-server/` 错):
       ```bash
       grep -qE '^/crates/grid-server/' .github/CODEOWNERS
       grep -qE '^/crates/grid-platform/' .github/CODEOWNERS
       grep -qE '^/crates/grid-desktop/' .github/CODEOWNERS
       grep -qE '^/web/' .github/CODEOWNERS
       grep -qE '^/web-platform/' .github/CODEOWNERS
       ```

    4. **NO bare top-level `/grid-server/` 等 (silent-fail trap)**:
       ```bash
       ! grep -qE '^/grid-(server|platform|desktop)/' .github/CODEOWNERS
       ```
       (must produce zero match — 否则 gate 失效)

    5. **Header 段含 ADR-V2-023 引用**:
       ```bash
       grep -qE 'ADR-V2-023' .github/CODEOWNERS
       grep -qE 'strategy-grid-two-leg-checklist' .github/CODEOWNERS
       ```

    全部 5 组 PASS = 任务完成。
  </acceptance_criteria>

  <verify>
    <automated>bash -c 'set -e; F=.github/CODEOWNERS; test -f $F && [ "$(grep -cE "@firmwwwee" $F)" -ge 5 ] && grep -qE "^/crates/grid-server/" $F && grep -qE "^/crates/grid-platform/" $F && grep -qE "^/crates/grid-desktop/" $F && grep -qE "^/web/" $F && grep -qE "^/web-platform/" $F && ! grep -qE "^/grid-(server|platform|desktop)/" $F && grep -qE "ADR-V2-023" $F && grep -qE "strategy-grid-two-leg-checklist" $F && echo CLEANUP-04 PASS'</automated>
  </verify>

  <done>
    - 文件 `.github/CODEOWNERS` 存在
    - 5 条 filesystem-correct 路径（`/crates/grid-server/` / `/crates/grid-platform/` / `/crates/grid-desktop/` / `/web/` / `/web-platform/`）全标 `@firmwwwee`
    - Header 段含 ADR-V2-023 §P2 dormancy 解释 + strategy-grid-two-leg-checklist.md 引用
    - 无顶层 bare path（`/grid-server/` 等 silent-fail trap）
    - Commit subject `chore(github): CLEANUP-04 add CODEOWNERS for Leg-B dormancy` ≤72 chars
    - Commit footer 含 Generated-By + Co-Authored-By 两行
  </done>
</task>

<task type="auto">
  <name>Task 5 (GOVERNANCE-01): Dry-run validate review_protocol annotations across CLEANUP-01..04</name>

  <!-- meta
  review_protocol: skip
  review_trigger: REVIEW_POLICY §2.3 rules 1+3 (verification dry-run, 零 file diff in pass case)
  review_estimated_overhead: 0
  -->

  <files></files>

  <read_first>
    - .planning/phases/04.0-bootstrap-cleanup-gsd/04.0-01-PLAN.md (本 PLAN.md 自身, 验证 4 个 CLEANUP task `<!-- meta review_protocol: ... -->` 注释存在 + 与 REVIEW_POLICY §2 trigger 对齐)
    - .planning/REVIEW_POLICY.md §2 (三档 risk tiers + 11 high-risk triggers + 6 medium-risk + 6 low-risk all-pass)
    - .planning/REVIEW_POLICY.md §3 (HTML-comment per-task `review_protocol:` 字段格式)
  </read_first>

  <action>
本任务实现 GOVERNANCE-01 per CONTEXT.md D-G01-01..05 + RESEARCH §"GOVERNANCE-01 — Dry-run Check Procedure" + planning_context.md OQ3 user 选 A (verify all CLEANUP plans have valid review_protocol; 零 diff dry-run, 不为凑 commit 数加 placeholder; 总 commits 4-5).

**核心约束**:
- 不在 Phase 4.0 内人为制造 high-risk task 跑 superpowers two-stage (D-G01-02, 留给 4.1 第一个真实 audit task)
- 4.0 自己 task 全部应当落 🟢 skip 或 🟡 gsd-standard (D-G01-03, 都 doc-level cleanup)
- REVIEW_POLICY draft → Active 翻转**不在 4.0 做** (D-G01-05, 等 4.1 第一次激活)
- **零 file diff in pass case** — 不创建 placeholder STATE.md update 凑 commit 数

### Step 5.1 — 检查 PLAN.md 4 个 CLEANUP task 全部带 review_protocol

执行：
```bash
PLAN_FILE=.planning/phases/04.0-bootstrap-cleanup-gsd/04.0-01-PLAN.md
TASKS_WITH_RP=$(grep -c "review_protocol:" "$PLAN_FILE" || echo 0)
if [ "$TASKS_WITH_RP" -ge 4 ]; then
  echo "GOVERNANCE-01 PASS — $TASKS_WITH_RP review_protocol annotations found"
  exit 0
else
  echo "GOVERNANCE-01 FAIL — only $TASKS_WITH_RP review_protocol annotations (≥4 required)"
  exit 1
fi
```

预期: 5 个 review_protocol 出现（T1-T5 各一个），check PASS。

### Step 5.2 — 验证每个 review_protocol 与 REVIEW_POLICY §2 trigger 对齐

逐 task 检查 (CONTEXT.md D-G01-03 要求 4.0 全 🟢/🟡, 无 🔴 superpowers-two-stage):

| Task | Expected `review_protocol` | Trigger justification |
|------|---------------------------|----------------------|
| T1 CLEANUP-01 | `skip` | §2.3 rules 1+3+4: 单文件 doc microedit, <50 LOC, 不引入新依赖 |
| T2 CLEANUP-02 | `skip` | §2.3 rules 1+3+4: 单文件 doc microedit, <50 LOC |
| T3 CLEANUP-03 | `gsd-standard` | §2.2 rule 3: 新增 doc 30-50 LOC, 跨 ADR 语义提取需保真度审视 |
| T4 CLEANUP-04 | `skip` | §2.3 rules 1+5: 单 config 文件, GitHub-standard syntax, 无逻辑 |
| T5 GOVERNANCE-01 | `skip` | §2.3 rules 1+3: verification dry-run, 零 file diff in pass case |

执行 grep 检验每个 task 的 review_protocol 字段命中 expected:
```bash
PLAN_FILE=.planning/phases/04.0-bootstrap-cleanup-gsd/04.0-01-PLAN.md
# Should find: 4 × "review_protocol: skip" + 1 × "review_protocol: gsd-standard"
SKIP_COUNT=$(grep -cE "^[[:space:]]*review_protocol: skip" "$PLAN_FILE")
STANDARD_COUNT=$(grep -cE "^[[:space:]]*review_protocol: gsd-standard" "$PLAN_FILE")
SUPERPOWERS_COUNT=$(grep -cE "^[[:space:]]*review_protocol: superpowers-two-stage" "$PLAN_FILE")
echo "skip=$SKIP_COUNT, gsd-standard=$STANDARD_COUNT, superpowers-two-stage=$SUPERPOWERS_COUNT"

# Per CONTEXT.md D-G01-03: 4.0 全 🟢/🟡, 无 🔴
[ "$SUPERPOWERS_COUNT" -eq 0 ] && echo "PASS — no superpowers-two-stage (D-G01-03 satisfied)"
```

### Step 5.3 — Pass case 无 file diff, **不创建 commit**

per CONTEXT.md D-C-01 lower bound 5 commits 含 GOVERNANCE-01 = 0 commits 场景:
- 如 Step 5.1 + 5.2 全 PASS → GOVERNANCE-01 不产生 commit (零 file diff)
- 总 commits = 4 (T1-T4 各一)
- 这正是 RESEARCH §"Open Question 3" 推荐 + planning_context.md OQ3 user 选 A 的预期

### Step 5.4 — Fail case fix-pass

如果 Step 5.1 或 5.2 失败 (e.g. 某 task 漏 review_protocol 或标 superpowers-two-stage):
1. **重开本 task** (gsd-execute-phase task status → in_progress)
2. **回到对应 CLEANUP task 修 PLAN.md** (在该 task 的 `<!-- meta -->` 块改 `review_protocol`)
3. 单独 commit: `fix(planning): GOVERNANCE-01 reclassify Tx review_protocol (fix-pass)` (ETIQUETTE 5 commit upper bound = 4 cleanup + 1 fix-pass)
4. 重跑 Step 5.1 + 5.2 直到 PASS

### Output

GOVERNANCE-01 task 完成时, 输出 stdout:
```
GOVERNANCE-01 dry-run PASS — N review_protocol annotations found, 0 superpowers-two-stage (D-G01-03 satisfied)
Phase 4.0 ready for /gsd-verify-work + /gsd-end-phase
```

**不创建 commit** (零 file diff in pass case)。
  </action>

  <acceptance_criteria>
    自动验证（VALIDATION.md §"Per-Task Verification Map" T5 + 加强）:

    1. **PLAN.md 含 ≥4 个 review_protocol 注释**:
       ```bash
       PLAN_FILE=.planning/phases/04.0-bootstrap-cleanup-gsd/04.0-01-PLAN.md
       [ "$(grep -c 'review_protocol:' $PLAN_FILE)" -ge 4 ]
       ```

    2. **零 superpowers-two-stage** (CONTEXT.md D-G01-03):
       ```bash
       [ "$(grep -cE '^[[:space:]]*review_protocol: superpowers-two-stage' $PLAN_FILE)" -eq 0 ]
       ```

    3. **Mix 落 skip + gsd-standard** (PATTERNS.md §"Per-task review_protocol decision matrix" 4×skip + 1×gsd-standard 期望):
       ```bash
       SKIP_COUNT=$(grep -cE '^[[:space:]]*review_protocol: skip' $PLAN_FILE)
       STANDARD_COUNT=$(grep -cE '^[[:space:]]*review_protocol: gsd-standard' $PLAN_FILE)
       [ "$SKIP_COUNT" -ge 3 ] && [ "$STANDARD_COUNT" -ge 1 ]
       ```

    4. **Pass case 零 file diff** (本 task 不应产 commit):
       ```bash
       git status --porcelain | grep -v "^?? " | wc -l | xargs test 0 -eq
       ```
       (在 Task 5 执行前后 working tree status 无 stage/modify 变化)

    全部 4 组 PASS = 任务完成 (无 commit)。
    若任一 FAIL → 重开 offending CLEANUP task fix `review_protocol` 字段，再重跑本 task。
  </acceptance_criteria>

  <verify>
    <automated>bash -c 'set -e; PLAN_FILE=.planning/phases/04.0-bootstrap-cleanup-gsd/04.0-01-PLAN.md; TOTAL=$(grep -c "review_protocol:" $PLAN_FILE); SUPERPOWERS=$(grep -cE "^[[:space:]]*review_protocol: superpowers-two-stage" $PLAN_FILE); SKIP=$(grep -cE "^[[:space:]]*review_protocol: skip" $PLAN_FILE); STD=$(grep -cE "^[[:space:]]*review_protocol: gsd-standard" $PLAN_FILE); [ "$TOTAL" -ge 4 ] && [ "$SUPERPOWERS" -eq 0 ] && [ "$SKIP" -ge 3 ] && [ "$STD" -ge 1 ] && echo "GOVERNANCE-01 PASS — total=$TOTAL skip=$SKIP gsd-standard=$STD superpowers-two-stage=$SUPERPOWERS (D-G01-03 satisfied, no commit needed)"'</automated>
  </verify>

  <done>
    - PLAN.md 5 个 task 全部带 `<!-- meta review_protocol: ... -->` HTML-comment 注释
    - 4 个 task 标 `skip` (T1, T2, T4, T5) + 1 个标 `gsd-standard` (T3)
    - 0 个 task 标 `superpowers-two-stage` (CONTEXT.md D-G01-03)
    - **不产生 commit** (zero diff in pass case, RESEARCH §"Open Question 3" + planning_context.md OQ3 user 选 A 一致)
    - stdout 输出 PASS 标记
    - Phase 4.0 ready for `/gsd-verify-work` + `/gsd-end-phase`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 4.0 has zero attack surface — doc-only mechanical cleanup. No user input boundary, no auth/crypto surface, no network calls, no proto changes. ASVS L1 N/A; STRIDE N/A.

| Boundary | Description |
|----------|-------------|
| (none) | Phase 4.0 changes 4 doc/config files, all read-only at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.0-01 | Tampering | `.github/CODEOWNERS` | accept | GitHub-side gate; tamper requires push permission to main; out of phase scope |
| T-04.0-02 | Information Disclosure | docs/reviews/* | accept | Public repo by design; checklist content is verbatim from already-public ADR-V2-023 |

(All 6 STRIDE categories N/A for actual content changes — CLEANUP-01..04 only modify documentation describing pre-existing protocols, do not alter runtime behavior.)
</threat_model>

<verification>
**Per-Task verification** (each task has `<verify><automated>` running independently before commit):
- Task 1 (CLEANUP-01): 7 grep assertions on L1_RUNTIME_ADAPTATION_GUIDE.md
- Task 2 (CLEANUP-02): 5 grep assertions on DEFERRED_LEDGER.md
- Task 3 (CLEANUP-03): 5 assertions on docs/reviews/strategy-grid-two-leg-checklist.md (incl byte-equal diff vs ADR-V2-023 L224-228)
- Task 4 (CLEANUP-04): 5 assertions on .github/CODEOWNERS (incl Hidden Gotcha #1 防回归 — 顶层 path silent-fail trap)
- Task 5 (GOVERNANCE-01): 4 assertions on this PLAN.md (review_protocol distribution + zero-diff)

**Phase Gate composite check** (VALIDATION.md §"Phase Gate", run after Task 5):
```bash
bash -c '
  set -e
  echo "Phase 4.0 gate check:"
  grep -F "<!-- @chunk-type-sync ADR-V2-021 -->" docs/design/EAASP/L1_RUNTIME_ADAPTATION_GUIDE.md > /dev/null && echo "✓ CLEANUP-01 marker"
  ! grep -nE "\| *\"(text|tool_call|hook_fired|pre_compact)\" *\|" docs/design/EAASP/L1_RUNTIME_ADAPTATION_GUIDE.md > /dev/null && echo "✓ CLEANUP-01 stale strings removed"
  first_d120=$(grep "D120" docs/design/EAASP/DEFERRED_LEDGER.md | head -1)
  echo "$first_d120" | grep -q "closed" && echo "✓ CLEANUP-02 D120 first-line closed"
  grep -qE "row-edit-on-close|关闭/状态变更" docs/design/EAASP/DEFERRED_LEDGER.md && echo "✓ CLEANUP-02 preamble convention"
  test -f docs/reviews/strategy-grid-two-leg-checklist.md && echo "✓ CLEANUP-03 file"
  [ $(grep -c "^- \[ \]" docs/reviews/strategy-grid-two-leg-checklist.md) -ge 5 ] && echo "✓ CLEANUP-03 ≥5 items"
  test -f .github/CODEOWNERS && echo "✓ CLEANUP-04 file"
  [ $(grep -cE "@firmwwwee" .github/CODEOWNERS) -ge 5 ] && echo "✓ CLEANUP-04 ≥5 reviewer entries"
  grep -qE "^/crates/grid-server/" .github/CODEOWNERS && echo "✓ CLEANUP-04 filesystem-correct path"
  [ $(grep -c "review_protocol:" .planning/phases/04.0-bootstrap-cleanup-gsd/04.0-01-PLAN.md) -ge 4 ] && echo "✓ GOVERNANCE-01 review_protocol present"
  echo "── Phase 4.0 gate PASS ──"
'
```

**Atomic commit verification** (D-C-03 GSD plumbing tracer-bullet):
- 4-5 atomic commits expected (T1-T4 produce 1 each = 4; T5 produces 0 in pass case = total 4; 5 if T5 finds misclass needing fix)
- Each commit subject ≤72 chars, footer 含 Generated-By + Co-Authored-By
- `git log --oneline | head -5` 验证 cadence 与 Phase 4a precedent (RESEARCH §"Phase 4a Archive Reference Pattern") 一致
</verification>

<success_criteria>
ROADMAP §Phase 4.0 5 个 success criteria 全部命中:

1. ✓ `.planning/REVIEW_POLICY.md` 已存在 (Phase 4a draft @ 165cab4); 本 phase 通过 task 5 验证 §2 / §3 字段格式可被引用 (D-G01-01 + D-G01-03)
2. ✓ `docs/design/EAASP/L1_RUNTIME_ADAPTATION_GUIDE.md` §4 + L472 + L492 全部使用 ADR-V2-021 canonical wire 值, 含 `<!-- @chunk-type-sync ADR-V2-021 -->` provenance marker (Task 1 完成后)
3. ✓ `docs/design/EAASP/DEFERRED_LEDGER.md` D120 status ambiguity 已解决 — `grep '^.*D120.*' DEFERRED_LEDGER.md | head -1` 返回 closed 行 + commit `7e083c7`; preamble 加入 row-edit-on-close convention (Task 2 完成后)
4. ✓ `docs/reviews/strategy-grid-two-leg-checklist.md` + `.github/CODEOWNERS` 双双存在; checklist 5 个 ADR-V2-023 §Enforcement verbatim checkpoints; CODEOWNERS 5 条 filesystem-correct Leg-B 路径标 @firmwwwee (Task 3 + Task 4 完成后)
5. ✓ CLEANUP-01..04 在 GSD plan 内分 task 走完 (4 atomic commits per Phase 4a precedent); superpowers two-stage 激活路径在 4.0 不实测但 4.0 task 的 review_protocol annotation 工作 — Task 5 dry-run validate 之, 留给 Phase 4.1 第一个真实 audit task 自然激活 (D-G01-04)

Phase Gate composite check (verification 段) 全 11 行 PASS = 整体完成。
</success_criteria>

<output>
After completion, create `.planning/phases/04.0-bootstrap-cleanup-gsd/04.0-01-SUMMARY.md` containing:

- 5 task outcomes (T1-T5: PASS/FAIL + commit hash if applicable)
- Total commits produced (expected 4 in zero-fix-pass case, 5 if Task 5 finds misclass)
- Phase Gate composite check stdout (11 ✓ markers all PASS)
- Cross-references to ROADMAP §Phase 4.0 5 success criteria (mark each ✓)
- Pointer to next phase: Phase 4.1 Discuss & Audit (依赖本 phase REVIEW_POLICY.md 字段已 dry-run validated)
- Any new Deferred items filed (expect 0 — phase scope 不扩展; if found, add to DEFERRED_LEDGER.md per CLEANUP-02 引入流程)
</output>
