---
phase: 04.2
slug: decide-document
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md
  - docs/design/EAASP/adrs/ADR-V2-023-grid-two-leg-product-strategy.md
  - docs/design/EAASP/adrs/decisions/2026-04-27-leg-decision-audit.md
  - CLAUDE.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - docs/dev/WORK_LOG.md
  - .planning/REVIEW_POLICY.md
  - .planning/phases/04.2-decide-document/04.2-01-SUMMARY.md
autonomous: true
requirements:
  - DECIDE-02
  - DECIDE-03
must_haves:
  truths:
    - "ADR-V2-024 Decision 段为 layered 写法 — top-line `>` blockquote pin (一句采纳 §5 双轴模型 + §4.2 推荐为产品形态实例) + ### §1 双轴模型 substance H3 (audit §5.5 verbatim) + ### §2 三选一推荐 H3 (audit §4.2 verbatim) (per CONTEXT.md D-B-01..03; ROADMAP SC#1+SC#2)"
    - "ADR-V2-024 Alternatives Considered 段 4 个 Option (A/B/C/D) 各含 Positive/Negative/Risks/Verdict/Evidence-chain 5 子标 + 每子标 ≥2 evidence-anchored bullets (per CONTEXT.md D-D-02 T2; PATTERNS.md T2 template)"
    - "ADR-V2-024 Enforcement 段 frontmatter `enforcement.level: strategic` + `trace: []` + `review_checklist:` non-empty + 在 body 段 ## Implementation §Phase 4.2 子段下追加 review_checklist 与 ADR governance hand-off 文字 (per CONTEXT.md D-F-04 + PATTERNS.md T3.A)"
    - "audit doc §6 Open Items 5 项全部 backfill — 5 子段 (#1..#5) 每段含 Verdict (yes/no/partial 收敛) + Answer (CONTEXT.md `<open_items_resolved>` verbatim) + Rationale + Phase 4.2 影响 4 行; §6.1 优先级矩阵 §P5.2 行 ADR-block 状态从 yes 改为 已解除/解除 (per CONTEXT.md D-A-03 + open_items_resolved §1 P0)"
    - "ADR-V2-024 frontmatter `supersedes:` 改为 multi-line YAML list 含 `- ADR-V2-023` 一行 + `status: Accepted` + `accepted_at: 2026-04-XX` non-null + `phase:` 改为 `Phase 4.2 — Decide & Document` (per CONTEXT.md D-C-01 + PATTERNS.md T4.A)"
    - "ADR-V2-023 frontmatter `status: Superseded` + `superseded_by: ADR-V2-024` (string scalar) + `deprecated_at: 2026-04-XX` non-null + `deprecated_reason:` non-null 含 `双轴模型替代 Leg A/B 二元框架` 字样 + body **Status:** 行同步更新 (per CONTEXT.md D-C-02 + PATTERNS.md T4.B; first supersede in repo history)"
    - "F1+F2+F3 lint 在 ADR-V2-024 + ADR-V2-023 上 exit 0 — `.adr-plugin/scripts/adr_lint.py --id ADR-V2-024 --check F1,F2,F3 --ci` exit 0 + 同样 V2-023 (per CONTEXT.md D-D-02 T5 + PATTERNS.md T5; F4 advisory WARN-only OK)"
    - "T6 sweep 后 in-scope 5 文件 (CLAUDE.md / PROJECT.md / REQUIREMENTS.md / ROADMAP.md / WORK_LOG.md) 中所有 `Leg A` / `Leg B` body 文本被替换为双轴/中性措辞 + 每文件首次出现位置含 supersede footnote (`ADR-V2-024 supersedes ADR-V2-023`); DEFERRED_LEDGER.md 0 occurrence 无操作 (per CONTEXT.md D-C-04 + D-D-03 + PATTERNS.md §Sweep grep pattern reference)"
    - "GOVERNANCE-03 hard gate (REVISION per cross-AI HIGH#2 fix) — T6 step 6.2 完成 CLAUDE.md sweep + commit 后, executor 在 step 6.2.5 创建 .continue-here.md 并显式 PAUSE (`exit 78`); user 必须手动 /clear + /gsd-resume-work + 在 SUMMARY.md §T6 GOVERNANCE-03 实测结果 段贴出 a/b/c 三档判据输出, 然后重新 invoke /gsd-execute-phase 4.2 才能 step 6.2.6 继续 sweep 剩余 4 文件; 不允许 trigger gate 创建即视为通过 (per CONTEXT.md D-E-01..04 + REVIEWS.md HIGH#2 strict mode)"
    - "PROJECT.md §Active 中 `Phase 4 主决策` 行被划掉/删除 + §Validated 顶部插入 Phase 4.2 entry 含 ADR-V2-024 commit hash 引用 + 7 必要 substance (Decision 锁定 + supersede chain + audit Open Items backfill + sweep + GOVERNANCE-03 + REVIEW_POLICY patch + 5 SC ✅) (per CONTEXT.md D-D-02 T7 + PATTERNS.md T7 §Validated 模板)"
    - "ROADMAP.md §Phase 4.2 5 SC 全部从 `1.` 改为 `1. ✅` (5 行) + §Progress 表 Phase 4.2 行从 `0/0 (TBD)` 改为 `1/1 ✅ Complete (5/5 SC, X/Y must-haves PASS)` + Coverage 表保持 (DECIDE-02 + DECIDE-03 已 ✅ 通过 §Progress 行体现) (per ROADMAP §Phase 4.2 + PATTERNS.md T7)"
    - "REVIEW_POLICY.md §3.5 Review Modality Table (4 行 modality) 在 §3 §4 之间 (current L171 之前) inline patch + §2.12 Strategic ADR creation trigger row append 到 §2 末尾; 两段 patch 均含 'Phase 4.1 §7 Lesson 1' 引用 字样 + 'Phase 4.2 实证' 字样 (per CONTEXT.md D-F-01..03 + PATTERNS.md T7 §3.5/§2.12 模板)"
    - "Phase 4.2 commits 在 12 commits 总数 区间 (T1+T2+T3+T4+T5 = 5 single-file commits + T6 5 sub-commits 跨 5 sweep files + T7 single multi-file commit + T8 single commit incl. T7 hash backfill) — 每 commit subject ≤72 chars + 末尾两行 Generated-By + Co-Authored-By footer (per CONTEXT.md D-G-01..03 + PATTERNS.md §Commit subject convention; REVISION per cross-AI HIGH#1 5-file sweep)"
    - "SUMMARY.md 落盘 — frontmatter mirror Phase 4.1 SUMMARY (phase/plan/subsystem/tags/dependency_graph/tech_stack/key_files/decisions/metrics) + body 12 段 sequence (one-paragraph + Task Outcomes table + Total Commits + Phase Gate composite + ROADMAP SC cross-ref + Audit Open Items closure + Supersede chain validation + T6 GOVERNANCE-03 实测 + REVIEW_POLICY patch summary + Pointer to Next Milestone + Deferred Items + Self-Check) (per PATTERNS.md T8)"
  artifacts:
    - path: "docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md"
      provides: "Phase 4 product scope 决策 ADR Accepted — Decision 段 layered + Alternatives 4 Options 展开 + Enforcement 填实 + frontmatter supersedes V2-023"
      contains: "status: Accepted"
    - path: "docs/design/EAASP/adrs/ADR-V2-023-grid-two-leg-product-strategy.md"
      provides: "ADR-V2-023 Superseded 双向锁链 (frontmatter superseded_by V2-024 + status Superseded + deprecated_at + deprecated_reason)"
      contains: "status: Superseded"
    - path: "docs/design/EAASP/adrs/decisions/2026-04-27-leg-decision-audit.md"
      provides: "audit doc §6 Open Items 5 行 backfill — verdict yes/no 全部收敛 + ADR-block:yes 解除 (§P5.2)"
      contains: "ADR-block**: 解除"
    - path: ".planning/PROJECT.md"
      provides: "§Active Phase 4 主决策行删除 + §Validated 顶部 Phase 4.2 entry"
      contains: "Phase 4.2 Decide & Document"
    - path: ".planning/ROADMAP.md"
      provides: "§Phase 4.2 5 SC ✅ + §Progress 表 1/1 ✅ Complete"
      contains: "4.2 Decide & Document | 1/1 ✅"
    - path: ".planning/REVIEW_POLICY.md"
      provides: "§3.5 Review Modality Table inline patch + §2.12 Strategic ADR creation trigger row append (Phase 4.1 §7 Lesson 1 实施)"
      contains: "### 3.5 Review Modality Table"
    - path: ".planning/phases/04.2-decide-document/04.2-01-SUMMARY.md"
      provides: "Phase 4.2 close-phase SUMMARY — 8 task outcome + GOVERNANCE-03 实测 + Supersede chain + REVIEW_POLICY patch"
      contains: "Phase 4.2 ✅ COMPLETE"
  key_links:
    - from: "docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md ## Decision 段"
      to: "docs/design/EAASP/adrs/decisions/2026-04-27-leg-decision-audit.md §4.2 + §5.5"
      via: "Decision 段 layered 写法 §1 H3 verbatim 引用 audit §5.5 双轴 substance + §2 H3 verbatim 引用 audit §4.2 推荐措辞"
      pattern: "audit §5|双轴模型|两腿都推进"
    - from: "docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md frontmatter supersedes:"
      to: "docs/design/EAASP/adrs/ADR-V2-023-grid-two-leg-product-strategy.md frontmatter superseded_by:"
      via: "双向锁链 — V2-024 supersedes V2-023 (multi-line list) + V2-023 superseded_by V2-024 (string scalar) + V2-023 status Superseded + deprecated_at/reason 全 non-null"
      pattern: "supersedes:|superseded_by: ADR-V2-024"
    - from: "CLAUDE.md / PROJECT.md / REQUIREMENTS.md / ROADMAP.md / WORK_LOG.md"
      to: "ADR-V2-024 双轴模型 (engine vs data/integration)"
      via: "T6 sweep — 'Leg A' / 'Leg B' 措辞替换 + supersede footnote 链回 ADR-V2-024 Decision 段"
      pattern: "ADR-V2-024 supersedes ADR-V2-023|engine vs data/integration"
    - from: ".planning/REVIEW_POLICY.md §3.5 / §2.12"
      to: "REVIEW_POLICY §7 Lesson 1 (commit d4ba2b8 经验 1+3)"
      via: "Phase 4.2 inline patch 实施 Phase 4.1 Lesson 1 三经验中两条 — modality table + audit-fidelity-grep trigger"
      pattern: "audit-fidelity-grep|§7 Lesson 1"
    - from: ".planning/PROJECT.md §Validated"
      to: "ADR-V2-024 commit hash + Phase 4.2 close-phase commit hash"
      via: "T7 §Validated entry 引用 commit hash, 镜像 Phase 4.0 (commit c12f425) 模板"
      pattern: "commit `[0-9a-f]{7}`"
review_protocol:
  default: gsd-standard
  per_task:
    T1: audit-fidelity-grep
    T2: audit-fidelity-grep
    T3: gsd-standard
    T4: gsd-standard
    T5: gsd-standard
    T6: skip
    T7: gsd-standard
    T8: skip
---

<objective>
Phase 4.2 Decide & Document / 决策落定 — Phase 4.1 audit doc 推荐 (两腿都推进 + §5 双轴模型 co-equal output) 落到三件具体动作 + 关闭 Phase 4 milestone:

Purpose:
- DECIDE-02: PLAN.md (本文件) 反映 Phase 4.1 audit 决策方向 — 8 atomic task 决策-locking + 1 mechanical sweep + close-phase cascade, 不预排下个 milestone engine/data backlog (那是下个 milestone roadmap 的事)
- DECIDE-03: ADR-V2-024 Proposed → Accepted, Decision 段 layered (双轴 + 三选一 H3) + Alternatives 4 Options 展开 + Enforcement 填实 + 完全 supersede ADR-V2-023 (双向锁链 + ADR-V2-023 status Superseded)
- 顺势: audit Open Items 5 条 backfill (CONTEXT.md `<open_items_resolved>` verbatim) + 仓库 "Leg A/B" 措辞 sweep (≥5 文件) + GOVERNANCE-03 mid-T6 /clear 实测 + REVIEW_POLICY §3.5/§2.12 inline patch (Phase 4.1 Lesson 1 实施) + PROJECT.md §Active 行划入 §Validated + ROADMAP §Phase 4.2 5 SC ✅ + close milestone

Output:
- 8 atomic commits (T1-T8) + 1 close-phase cascade commit (PROJECT/ROADMAP/REVIEW_POLICY/STATE 由 close-phase cascade 一起落) ≈ 9 commits 总数 (per CONTEXT.md D-G-01)
- ROADMAP §Phase 4.2 5 SC 全部 ✅
- Milestone "Phase 4 — Product Scope Decision" CLOSED, 下个 milestone 起点清洁不背 Leg A/B 包袱
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/REVIEW_POLICY.md
@.planning/phases/04.2-decide-document/04.2-CONTEXT.md
@.planning/phases/04.2-decide-document/04.2-PATTERNS.md
@.planning/phases/04.1-discuss-audit-p5/04.1-01-PLAN.md
@.planning/phases/04.1-discuss-audit-p5/04.1-01-SUMMARY.md
@docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md
@docs/design/EAASP/adrs/ADR-V2-023-grid-two-leg-product-strategy.md
@docs/design/EAASP/adrs/decisions/2026-04-27-leg-decision-audit.md
@docs/design/EAASP/adrs/ADR-V2-019-l1-runtime-deployment-model.md
</context>

<interfaces>
<!-- Audit doc §4.2 + §5.5 verbatim source phrases (T1 executor 在 T1 时读取, NOT plan-hardcoded — per Phase 4.1 Fix #3 substitution principle).
     Acceptance criteria 用 grep 验证 Decision 段含 audit §4.2 + §5.5 的 anchor phrases, 但具体引用文本由执行 T1 时直接 awk 引用 audit 落盘版本. -->

audit §4.2 anchor phrases (位于 audit doc L284-286):
- "两腿都推进"
- "据 audit §3.4 Q4 verdict yes (Grid 优先 + 并行可行)"
- "Phase 4.2+ 子任务必须按 §5 双轴模型 (engine vs data/integration) 切, 而非按 Leg A/B 切"

audit §5.5 anchor phrases (位于 audit doc L347-352):
- "engine vs data/integration"
- "user 投 engine + 他人投 data/integration, Grid 全栈作为产品 leg 自然 active 但内部职责切清晰"
- "co-equal"

audit §5.1 双轴模型 substance anchor phrases (位于 audit doc L308-319):
- "轴 1: engine vs data/integration"
- "engine = 可复用的核心组件, user 主战场"
- "data/integration = 场景特定的横切关注, 客户/厂商/他人接手"

ADR-V2-024 frontmatter delta (T4.A — Proposed → Accepted + supersedes V2-023):
```yaml
status: Accepted                             # was: Proposed
accepted_at: 2026-04-XX                      # was: null  (X=commit 当天)
phase: "Phase 4.2 — Decide & Document"       # was: "Phase 4.1 — Leg Decision Audit (Phase 4.2 graduates to Accepted)"
supersedes:                                  # was: []
  - ADR-V2-023
```

ADR-V2-023 frontmatter delta (T4.B — Accepted → Superseded):
```yaml
status: Superseded                           # was: Accepted
superseded_by: ADR-V2-024                    # was: null  (string scalar, NOT list)
deprecated_at: 2026-04-XX                    # was: null
deprecated_reason: "Superseded by ADR-V2-024 — 双轴模型替代 Leg A/B 二元框架, 详见 ADR-V2-024 Decision 段 + audit doc §5"   # was: null
```

ADR-V2-023 body L38 同步更新 (T4.B 附带):
```markdown
**Status:** Superseded by ADR-V2-024 (2026-04-XX)        # was: **Status:** Proposed (历史落盘 bug, frontmatter 一直是 Accepted)
```

Open Items 5 backfill payload (T3.B — verbatim from CONTEXT.md `<open_items_resolved>`):

| Open Item | Verdict 收敛 | Answer | Rationale | Phase 4.2 影响 |
|-----------|------------|--------|-----------|--------------|
| #1 §P5.2 ≥2 客户 Grid-only | unknown → **no** + ADR-block:yes 解除 | 0 家 | 公司在 EAASP 同仓孵化期, 客户还没被外部试压 | ADR-V2-024 Accepted 路径无 §P5.2 hard-stop; audit §6 backfill "0 家 (2026-04-27 user 直陈)" |
| #2 §P5.4 工时分配精确百分比 | partial → **yes** | Grid 全栈 ≈60% / EAASP 引擎 ≈30% / 元工作 ≈10% | 与 PRE-AUDIT-NOTES §B.2 baseline 一致, 精确化为 60/30/10 | ADR Decision/Consequences 引用 60/30/10 baseline |
| #3 §F.Q4 五者发力顺序 | partial → **yes** | grid-cli + grid-server 优先 (其他 dormant) | grid-cli 已 active, grid-server 单租户 × EAASP L1 双身份 | ADR Consequences 引用此优先级; 下个 milestone roadmap 起点 |
| #4 §F.Q1 字段切分 | partial → **yes** | 5+3 切分 (engine: priority_tier/risk_class/latency_class/audit_trace/model_lock; vendor: SCADA/KPI/合规表) + L3/L4 extension hook 架构承诺 | 跨行业可复用 → engine; 行业 specific → 厂商; 但 engine 必须支持扩展机制 | ADR Decision 段提"5+3 切分 + L3/L4 extension hook 架构承诺"; Consequences 提 Phase 4.3+ 独立 ADR |
| #5 框架修订 + supersede | unknown → **yes** (完整 supersede) | 采纳 §5 双轴模型主框架 + ADR-V2-024 完全 supersede ADR-V2-023 | 双轴模型职责切清楚; 下个 milestone 起点清洁; T6 sweep ≤2h 可承受 | T1 Decision 段 layered + T4 supersede 双向锁链 + T6 sweep 全仓库 |

T6 sweep replacement table (per PATTERNS.md T6, 已用 grep -lc 验证目标文件 occurrence count):

| Original 字面 | Replacement 推荐 | Rationale |
|--------------|----------------|---------|
| `Leg A` (and `Leg A —`) | `EAASP 集成 (engine 接入面)` 或 `engine 子任务` | per audit §5 双轴一极 |
| `Leg B` (and `Leg B —`) | `Grid 独立 (data/integration 边界)` 或 `data/integration 接入面` | per audit §5 另一极 |
| `Leg A vs Leg B` | `engine vs data/integration` | 双轴 framing 替代二元 |
| `走 Leg A 硬化 / 走 Leg B 激活 / 两条腿` | `双轴模型实例化 + 两腿都推进 (engine vs data/integration)` | 三选一 → 双轴 + audit §4.2 推荐 |
| `**Leg A — EAASP integration (CURRENT MAIN FOCUS).**` (CLAUDE.md L9) | `**Engine 子任务 — EAASP 集成 (PRIMARY FOCUS).**` | 保留 EAASP 集成 substance |
| `**Leg B — Grid independent product (DORMANT, planned).**` (CLAUDE.md L11) | `**Data/integration 接入面 — Grid 独立 (DORMANT, planned).**` | 保留 dormancy semantic |

T6 sweep target file occurrence counts (pre-sweep 验证, executor 用 grep 在 T6 开始前重测):
- CLAUDE.md: 13 occurrences
- .planning/PROJECT.md: 4 occurrences
- .planning/REQUIREMENTS.md: 8 occurrences
- .planning/ROADMAP.md: 12 occurrences (含 §Phase Details 多处 framing 文字)
- docs/dev/WORK_LOG.md: 2 occurrences (L117 + L123)
- docs/design/EAASP/DEFERRED_LEDGER.md: 0 occurrences (sweep no-op)

T6 supersede footnote 模板 (每文件首次 'Leg A/B' 出现位置上方 prepend; mid-paragraph 出现则在该段首插入):
```markdown
> **Note**: ADR-V2-024 (2026-04-XX) supersedes ADR-V2-023 — Leg A/B 二元框架替换为双轴模型 (engine vs data/integration). 此文件中"Leg A/B" 历史措辞已统一替换. 详见 ADR-V2-024 Decision 段。
```

T6 EXCLUDE list (sweep MUST NOT touch):
- `docs/design/EAASP/adrs/ADR-V2-023*` (V2-023 自身保留措辞作为历史 ADR, T4 仅改 frontmatter + body L38 Status 行)
- `.planning/phases/04.1-*` (Phase 4.1 audit + discuss-phase context 历史快照)
- `.planning/phases/04.2-*` (本 phase metadocs)
- `docs/design/EAASP/adrs/decisions/2026-04-27-leg-decision-audit.md` (audit 历史快照, T3 仅改 §6 Open Items)

REVIEW_POLICY §3.5 patch insertion site (per PATTERNS.md T7 Analog):
- 当前 L171 行: `## 4. Two-stage Review Implementation Details`
- §3.5 Review Modality Table H3 patch 应**插在 L170 之前** (即 §3 节末尾, §4 节标题之前). 落地形态: 4 行 modality table + Phase 4.1 §7 Lesson 1 引用注脚

REVIEW_POLICY §2.12 patch insertion site:
- 当前 L29-§2 11 trigger 列表 (§2.1..§2.11) 末尾, 在 `### 🟡 Medium Risk` H3 (L72) 之前 — 即 §2 high-risk 段尾
- §2.12 H3 patch 落地形态: trigger / review modality / why / Phase 4.1 实证 4 字段
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1 (DECIDE-03 part 1/5): ADR-V2-024 Decision 段 layered 锁文 (双轴 + 三选一)</name>

  <!-- meta
  review_protocol: audit-fidelity-grep
  review_trigger: REVIEW_POLICY §2.12 (Phase 4.2 inline patch 后); 基础: §2 战略级 ADR strategy Decision-locking + cross-doc verbatim 引用 audit §4.2 + §5.5
  review_estimated_overhead: +5min (acceptance criteria pre-commit grep 自动验证)
  -->

  <files>docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md</files>

  <read_first>
    - docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md (整文件; 重点 L50-61 当前 Decision 段需替换的 substring)
    - docs/design/EAASP/adrs/decisions/2026-04-27-leg-decision-audit.md §4 (L266-296) + §5 (L299-364) 完整段 — Decision 段 §1 H3 verbatim 引用 §5.5 双轴 substance, §2 H3 verbatim 引用 §4.2 推荐措辞
    - .planning/phases/04.2-decide-document/04.2-PATTERNS.md §"T1 — ADR-V2-024 Decision 段最终锁文本" (Layered Decision template + acceptance criteria pattern)
    - .planning/phases/04.2-decide-document/04.2-CONTEXT.md `<decisions>` D-B-01..03 + `<open_items_resolved>` Open Item #5 (框架修订采纳 — 完整 supersede + 双轴主框架)
  </read_first>

  <action>
本任务实现 DECIDE-03 part 1/5 + Phase 4.1 §7 Lesson 1 经验 3 (cross-doc fidelity grep) 首次实证应用 — ADR-V2-024 Decision 段 layered 锁文本 (per CONTEXT.md D-B-01..03 + PATTERNS.md T1).

文件中文 (CLAUDE.md doc lang rule). Edit tool 手写, 不写 sed/script.

### Step 1.0 — 读 audit 落盘版本提取 verbatim 引用 (Fix #3 substitution principle)

执行前用 awk 读取 audit doc §4.2 + §5.1 + §5.5 段, 由执行人员从 audit 文本逐字 copy verbatim 入 Decision 段 (NOT 从本 PLAN hardcode):

```bash
# 提取 §4.2 推荐措辞段 (用于 Decision §2 H3)
awk '/^### §4\.2/,/^### §4\.3/' docs/design/EAASP/adrs/decisions/2026-04-27-leg-decision-audit.md

# 提取 §5.1 双轴模型提案段 (用于 Decision §1 H3 substance core)
awk '/^### §5\.1/,/^### §5\.2/' docs/design/EAASP/adrs/decisions/2026-04-27-leg-decision-audit.md

# 提取 §5.5 audit 推荐段 (用于 Decision §1 H3 末尾 co-equal status)
awk '/^### §5\.5/,/^### §5\.6/' docs/design/EAASP/adrs/decisions/2026-04-27-leg-decision-audit.md
```

执行人员从输出中取 verbatim 文本入 Decision §1 / §2 H3 段, 不允许同义替写或截断 substance.

### Step 1.1 — 替换 Decision 段 (current L50-61, 12 行) 为 layered 写法

**当前 L50-61 (要被替换的 substring)** — 见 ADR-V2-024 Phase 4.1 落盘 form, blockquote 顶层一句 + 4 个 bullet + "草稿状态" 段.

**替换后** (Decision 段 H2 + 顶层 blockquote pin + §1 H3 双轴 substance + §2 H3 三选一推荐 — per CONTEXT.md D-B-01 layered):

```markdown
## Decision / 决策

> 采纳 audit §5 双轴模型 (engine vs data/integration) 作为 Phase 4 product scope 主框架, audit §4.2 推荐 (两腿都推进) 是双轴模型在产品形态层的具体实例化。下层 §1 (双轴 substance) 与 §2 (三选一推荐) 在本 ADR 中 co-equal 并存, 后续讨论必须同时引用; 仅引用 §1 会丢失 SC#2 三选一硬要求, 仅引用 §2 会丢失 audit §0 partial-needs-revision elevation 要求的 §5 substance。

### §1. 双轴模型 substance (engine vs data/integration — audit §5.1 + §5.5 verbatim)

[verbatim 引用 audit doc §5.1 双轴模型提案段全文 (轴 1 + 轴 2 两块) + §5.5 末段 audit 推荐 "user 投 engine + 他人投 data/integration, Grid 全栈作为产品 leg 自然 active 但内部职责切清晰"; 必须含 anchor phrase "engine vs data/integration" + "engine = 可复用的核心组件" + "data/integration = 场景特定的横切关注"]

**5+3 字段切分** (per Open Item #4 user 决议 + audit §3.3 Q3):
- Engine 内置 (5 项, 走 proto wire-level + Level 2/3 操作): `priority_tier` (替代原 `dispatch_level`) / `risk_class` / `latency_class` / `audit_trace` / `model_lock`
- 厂商写 (3 项, 走 vendor skill / Path D): SCADA 接入适配 / 业务 KPI 定义 / 行业合规表
- 架构承诺: EAASP L3/L4 必须提供 extension hook API, 让行业扩展可访问并控制 5 个 engine 内置字段; 详细 API 推到 Phase 4.3+ 独立 ADR (合并 audit §3.3 Q3 ⚫ 6 项接入位 ADR 候选)

### §2. 三选一推荐 (产品形态实例 — audit §4.2 verbatim)

[verbatim 引用 audit doc §4.2 推荐措辞段全文 (blockquote `>` 完整保留); 必须含 anchor phrase "两腿都推进" + "据 audit §3.4 Q4 verdict yes (Grid 优先 + 并行可行)" + "Phase 4.2+ 子任务必须按 §5 双轴模型 (engine vs data/integration) 切, 而非按 Leg A/B 切"]

**工时 baseline** (per Open Item #2 user 决议 + audit §B.2 工时事实精确化): Grid 全栈 ≈60% / EAASP 引擎 ≈30% / 元工作 (planning/audit/governance) ≈10%

**优先发力组合** (per Open Item #3 user 决议 + audit §3.4 §F.Q4): grid-cli + grid-server 优先发力, grid-desktop / grid-platform / web* 静态 dormant 到下个 milestone

**草稿状态 → Accepted 翻转**: Phase 4.2 commit 当天 status: Proposed → Accepted + accepted_at 2026-04-XX + supersedes ADR-V2-023 (per T4 frontmatter delta).
```

### Step 1.2 — 不动其他 ADR-V2-024 段

T1 仅替换 Decision 段 L50-61. Consequences (L63-78), Affected Modules (L80-90), Alternatives Considered (L92-106 — 留 T2 改), References (L108-115), Implementation (L117-130 — T3 改), History (L132-136 — T5 加新 row) 全部保持不动.

### Commit

`docs(adrs): T1 ADR-V2-024 Decision 段 layered 锁文 (双轴 + 三选一)`

Body 答 why: Phase 4.1 audit §0 verdict partial-needs-revision elevation 要求 Decision 段同时读 §4.2 推荐 (产品形态切角) + §5.5 双轴 substance (职责切角); layered 写法 (top pin + §1 H3 + §2 H3) 让两段 co-equal 落地, 不是仅引用其一. 引用 audit §4.2 + §5.1 + §5.5 verbatim 实施 Phase 4.1 §7 Lesson 1 经验 3 (cross-doc fidelity grep modality 首次实证). Footer per CLAUDE.md.
  </action>

  <acceptance_criteria>
    自动验证 (acceptance criteria 全部 grep / awk, audit-fidelity-grep modality):

    1. **Decision 段 H2 还在 + layered 顶层 blockquote pin 存在**:
       ```bash
       ADR=docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md
       grep -c '^## Decision / 决策$' $ADR | xargs test 1 -eq
       awk '/^## Decision/,/^## Consequences/' $ADR | grep -F '采纳 audit §5 双轴模型' | head -1
       awk '/^## Decision/,/^## Consequences/' $ADR | grep -F '是双轴模型在产品形态层的具体实例化'
       ```

    2. **§1 + §2 两个 H3 subsection 存在 + 顺序 §1 → §2** (CONTEXT.md D-B-01 layered):
       ```bash
       awk '/^## Decision/,/^## Consequences/' $ADR | grep -nE '^### §[12]\.' | tee /tmp/h3.list
       wc -l /tmp/h3.list | awk '{print $1}' | xargs test 2 -eq
       head -1 /tmp/h3.list | grep -F '§1.'
       tail -1 /tmp/h3.list | grep -F '§2.'
       ```

    3. **§1 双轴 substance verbatim 引用 audit §5.1 + §5.5 — 3 anchor phrase 全在**:
       ```bash
       awk '/^### §1\./,/^### §2\./' $ADR | grep -F 'engine vs data/integration'
       awk '/^### §1\./,/^### §2\./' $ADR | grep -F 'engine = 可复用的核心组件'
       awk '/^### §1\./,/^### §2\./' $ADR | grep -F 'data/integration = 场景特定的横切关注'
       ```

    4. **§1 含 5+3 字段切分** (Open Item #4 user 决议):
       ```bash
       awk '/^### §1\./,/^### §2\./' $ADR | grep -F 'priority_tier'
       awk '/^### §1\./,/^### §2\./' $ADR | grep -F 'audit_trace'
       awk '/^### §1\./,/^### §2\./' $ADR | grep -F 'model_lock'
       awk '/^### §1\./,/^### §2\./' $ADR | grep -F 'SCADA'
       awk '/^### §1\./,/^### §2\./' $ADR | grep -F 'extension hook'
       ```

    5. **§2 三选一推荐 verbatim 引用 audit §4.2 — 3 anchor phrase 全在**:
       ```bash
       awk '/^### §2\./,/^## Consequences/' $ADR | grep -F '两腿都推进'
       awk '/^### §2\./,/^## Consequences/' $ADR | grep -F 'Grid 优先 + 并行可行'
       awk '/^### §2\./,/^## Consequences/' $ADR | grep -F 'Phase 4.2+ 子任务必须按 §5 双轴模型'
       ```

    6. **§2 含 60/30/10 baseline + grid-cli + grid-server** (Open Item #2 + #3 user 决议) (FIX: 更宽容的 grep — 不锁定 % 字符以容忍格式微调):
       ```bash
       awk '/^### §2\./,/^## Consequences/' $ADR | grep -qE '60.*30.*10|≈60.*≈30.*≈10'
       awk '/^### §2\./,/^## Consequences/' $ADR | grep -qF 'grid-cli + grid-server'
       ```

    7. **Cross-doc fidelity grep — Decision 段中提到 audit §5 + audit §4.2 各 ≥1 次**:
       ```bash
       awk '/^## Decision/,/^## Consequences/' $ADR | grep -cE 'audit §5|§5 双轴' | xargs test 1 -le
       awk '/^## Decision/,/^## Consequences/' $ADR | grep -cF 'audit §4.2' | xargs test 1 -le
       ```

    8. **下层手动测 awk 输出** (per Phase 4.1 T5 deviation Recommendation):
       ```bash
       # 手动测 awk 跨 lowercase prefix '## Consequences' 边界正确:
       awk '/^## Decision/,/^## Consequences/' $ADR | tail -3
       # 期望最后一行包含 §2 末尾内容 (草稿状态翻转), NOT '## Consequences' 行
       ```

    全部 8 项 PASS = T1 完成. 任何 1 项 FAIL → 修 + 重测.
  </acceptance_criteria>

  <verify>
    <automated>bash -c 'set -e; ADR=docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md; AUDIT=docs/design/EAASP/adrs/decisions/2026-04-27-leg-decision-audit.md; grep -q "^## Decision / 决策$" $ADR && awk "/^## Decision/,/^## Consequences/" $ADR | grep -qF "采纳 audit §5 双轴模型" && awk "/^## Decision/,/^## Consequences/" $ADR | grep -qE "^### §[12]\\." && [ $(awk "/^## Decision/,/^## Consequences/" $ADR | grep -cE "^### §[12]\\.") -eq 2 ] && awk "/^### §1\\./,/^### §2\\./" $ADR | grep -qF "engine vs data/integration" && awk "/^### §1\\./,/^### §2\\./" $ADR | grep -qF "priority_tier" && awk "/^### §1\\./,/^### §2\\./" $ADR | grep -qF "extension hook" && awk "/^### §2\\./,/^## Consequences/" $ADR | grep -qF "两腿都推进" && awk "/^### §2\\./,/^## Consequences/" $ADR | grep -qF "Phase 4.2+ 子任务必须按 §5 双轴模型" && awk "/^### §2\\./,/^## Consequences/" $ADR | grep -qF "grid-cli + grid-server" && echo T1 ALL 8 GREP PASS'</automated>
  </verify>

  <done>
    - Decision 段 layered 写法落地 (top blockquote pin + §1 双轴 H3 + §2 三选一 H3)
    - §1 verbatim 引用 audit §5.1 + §5.5 (3 anchor phrase) + 5+3 字段切分 + L3/L4 extension hook 架构承诺
    - §2 verbatim 引用 audit §4.2 (3 anchor phrase) + 60/30/10 baseline + grid-cli + grid-server 优先发力
    - 8 grep / awk acceptance criterion 全 PASS
    - Commit subject `docs(adrs): T1 ADR-V2-024 Decision 段 layered 锁文 (双轴 + 三选一)` ≤72 chars
    - Commit footer 含 Generated-By + Co-Authored-By 两行
  </done>
</task>

<task type="auto">
  <name>Task 2 (DECIDE-03 part 2/5): ADR-V2-024 Alternatives Considered 4 Options 展开 Positive/Negative/Risks</name>

  <!-- meta
  review_protocol: audit-fidelity-grep
  review_trigger: REVIEW_POLICY §2 战略级 ADR strategy 内容展开 + Alternatives 段含 audit §3 §4 verdict 引用; cross-doc fidelity grep 自动验证
  review_estimated_overhead: +5min
  -->

  <files>docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md</files>

  <read_first>
    - docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md L92-106 (4 Option 当前 placeholder, 要被展开)
    - docs/design/EAASP/adrs/decisions/2026-04-27-leg-decision-audit.md §3 (L160-264) §F Q1-Q4 verdict + §4 (L266-297) 三选一推荐 + §4.3 (L288-291) 否决两候选 + §0 (L14-43) elevation logic
    - docs/design/EAASP/adrs/ADR-V2-019-l1-runtime-deployment-model.md (作为 Accepted strategy ADR Alternatives 段 shape 参考, 若有 Positive/Negative/Risks 子标)
    - .planning/phases/04.2-decide-document/04.2-PATTERNS.md §"T2 — ADR-V2-024 Alternatives Considered 各 Option 展开" (replacement template)
  </read_first>

  <action>
本任务实现 DECIDE-03 part 2/5 — ADR-V2-024 Alternatives Considered 4 Option (A/B/C/D) 各自展开 Positive/Negative/Risks/Verdict/Evidence-chain (per CONTEXT.md D-D-02 T2 + PATTERNS.md T2 template + Claude's Discretion).

文件中文. Edit tool 手写.

### Step 2.0 — 不动 H2 + 不动顶层引言

`## Alternatives Considered / 候选方案` H2 (L92) 保持. 顶层引言段 (L94 — "以下 4 个 option 是 Phase 4 product scope 候选; audit §4 推荐其一, audit §0 + §5 决定是否升级双轴模型为主结论...") 保留, 但**追加一句**最末说明本 ADR Accepted 状态下 Decision 段已锁定 (Option C + D co-equal):

追加一句 (L94 末追加):
```markdown
本 ADR Accepted (Phase 4.2 commit 当天) 状态下 Decision 段已落实 Option C (audit §4.2 推荐 — 两腿都推进) + Option D (audit §5 双轴模型 co-equal output) 二者并存; Option A / B 在 audit §4.3 显式否决, evidence chain 见各 Option 子段 + audit §3 + §4。
```

### Step 2.1 — 替换 Option A (L96-97, 当前 1 行 placeholder + TBD 行)

**替换后** (5 子标 + ≥2 evidence-anchored bullets per 子标 — Negative + Risks bullet 条数 ≥2 各):

```markdown
### Option A: Leg A 硬化 (集中 EAASP 集成)

**Positive**:
- 收敛工时到 EAASP 引擎层 (L2/L3/L4), 短期内 EAASP-engine 进度可加速
- ADR-V2-023 字面 §决策 "腿 A primary" 直接落地, 不需要新 framing

**Negative**:
- §3.4 Q4 verdict yes (Grid 优先 + 并行可行) 直接否决 — Grid 5-7 项企业级品质必需 (M5 溯源链 / M9 model_lock / M6 evidence chain) 独立于 EAASP 决策, dormant 不可行
- PRE-AUDIT-NOTES §B.2 + §B.3 baseline "Grid 是工时主战场" 与本 option "Grid dormant" 冲突
- audit §4.3 显式否决, 与 baseline §D Sanity Guard 第 1 条冲突, 无 audit 硬证据推翻

**Risks**:
- 失去 Grid 5-7 项独立企业品质 → 客户绕过 EAASP 直接购入路径堵死
- 团队工时不饱和反向 (60% Grid baseline 无处放)

**Verdict** (per audit §0 + §4): rejected (audit §4.3)
**Evidence chain**: audit §3.4 Q4 yes + audit §4.3 否决两候选 + PRE-AUDIT-NOTES §B.2 + §B.3 + ADR-V2-023 §决策 §P5 framing 弱化 (audit §2.1 §P5.1 partial)
```

### Step 2.2 — 替换 Option B (L99-100, 当前 1 行 placeholder + TBD 行)

**替换后**:

```markdown
### Option B: Leg B 激活 (Grid 独立产品)

**Positive**:
- Grid 全栈优先 + EAASP dormant, 让 Grid 商业化路径无 EAASP 依赖
- 单租户 grid-server 与多租户 grid-platform 独立交付节奏

**Negative**:
- 与 §B.2 user 自陈 EAASP 引擎层是 user 工时主战场之一相悖 — EAASP engine (L2/L3/L4 基础组件) 由 user 自做是 baseline 锁定, "EAASP 引擎层交给他人做" 不在锁定内
- §3.1 §F.Q1 partial (并行倾向) + §3.2 §F.Q2 partial (做基础引擎部分) 共同要求 EAASP engine 持续扩展, 与本 option "EAASP dormant" 直接冲突
- audit 无硬证据推翻 baseline §D Sanity Guard 第 4-5 条 (EAASP 引擎层是 user 工时主战场)

**Risks**:
- EAASP 项目失速 → Grid 独立路径反而失去 L1 contract 验证抓手 (7 runtime 对比矩阵作为契约活体测试本身依赖 EAASP L2-L4 集成)
- 用户业务模型 (Grid 全栈 + EAASP 引擎都是 user 主战场) 与产品定位 (Grid 独立 + EAASP dormant) 错位, 沟通成本上升

**Verdict** (per audit §0 + §4): rejected (audit §4.3)
**Evidence chain**: audit §4.3 否决两候选 + PRE-AUDIT-NOTES §B.2 §D Sanity Guard 4-5 条 + audit §3.1 partial + audit §3.2 partial
```

### Step 2.3 — 替换 Option C (L102-103, 当前 1 行 placeholder + TBD 行)

**替换后**:

```markdown
### Option C: 两腿都推进 (audit recommended per §4.1)

**Positive**:
- §3.4 Q4 verdict yes (Grid 优先 + 并行可行) 直接支持 — Grid 已 active, EAASP 同仓孵化期间 user 自做引擎, 不存在二选一前提
- §2.4 §P5.4 partial baseline 默认成立 + §B.2 user 工时事实 60/30/10 baseline (Open Item #2 user 决议) 同时支持 Grid 全栈 + EAASP 引擎层投入
- audit §4.4 self-consistency 检查双框架 (Leg A/B + 双轴) 同向支持本推荐, 无 framing 巧合

**Negative**:
- 在 ADR-V2-023 二元 framing 下 "两腿都推进" 含糊 (是什么意思?), 必须配合 Option D (双轴模型) 才能清晰落地
- 工时分散风险 — 60/30/10 baseline 必须严格执行, 否则两腿都推 = 两腿都不推

**Risks**:
- 若工时 baseline 漂移 (Grid <60% 或 EAASP <30% 显著偏离), 两腿都推 退化为单腿
- 5+3 字段切分若不及时落 ADR (Phase 4.3+), engine 与 vendor 边界模糊化

**Verdict** (per audit §0 + §4): accepted (primary recommendation per audit §4.1)
**Evidence chain**: audit §4.1 推荐 + audit §3.4 Q4 yes + audit §2.4 §P5.4 partial baseline + audit §B.2 user 工时事实 + audit §0 verdict partial-needs-revision + audit §4.4 self-consistency
```

### Step 2.4 — 替换 Option D (L105-106, 当前 1 行 placeholder + TBD 行)

**替换后**:

```markdown
### Option D: 框架修订 (双轴模型 — engine vs data/integration) (audit co-equal output per §0 + §5)

**Positive**:
- §F.Q1-Q4 4 个 Q 的 verdict 在双轴下都自然归约且解释力强 — 不需要硬塞 Leg A/B 二元
- 工时分配明确 — user 投 engine, 他人投 data/integration, 不再纠结 "Grid 是不是该 dormant"
- §P5 4 条 trigger 在双轴下自然重新框定 (engine 节奏 / 接入面信号 / engine 跨厂商 / engine 工时充裕度), 见 audit §5.6
- 5+3 字段切分 (Open Item #4) 与双轴一致 — engine 5 项 (priority_tier/risk_class/latency_class/audit_trace/model_lock) + vendor 3 项 (SCADA/KPI/合规表) 边界清楚

**Negative**:
- 改 ADR 成本 — ADR-V2-023 supersede + ADR-V2-024 完全替代 (T4 双向锁链)
- CLAUDE.md / WORK_LOG / DEFERRED_LEDGER 等 5+ 文件 "Leg A / Leg B" 措辞 sweep 工程 (T6, ≤2h 评估)
- 未来沟通对外措辞 — 双轴模型解释门槛高于 "Leg A vs Leg B 二元", 需要团队 / 客户文档同步更新

**Risks**:
- 双轴 framing 跨 milestone 漂移 — 若下个 milestone roadmap 又退回 Leg A/B 措辞, 双轴失效
- L3/L4 extension hook API 详细 schema 推到 Phase 4.3+ 独立 ADR, 期间 vendor 接入面边界仍模糊

**Verdict** (per audit §0 + §4): accepted (co-equal with Option C, audit §0 partial-needs-revision elevation per audit §5)
**Evidence chain**: audit §5 框架修订建议 + audit §0 verdict partial-needs-revision elevation + audit §5.3 双轴优势 + audit §5.6 §P5 trigger 兼容性 + Open Item #4 user 决议 (5+3 字段切分 + L3/L4 extension hook) + Open Item #5 user 决议 (完全 supersede ADR-V2-023)
```

### Commit

`docs(adrs): T2 ADR-V2-024 Alternatives 4 Options Positive/Negative/Risks`

Body 答 why: ADR-V2-024 落 Accepted 前 Alternatives 各 Option 必须展开 (F4 review_checklist requires); 各 Option 5 子标 (Positive/Negative/Risks/Verdict/Evidence-chain) 全部 audit §3-§4 verdict 引用 anchor, 不自创立场. Footer per CLAUDE.md.
  </action>

  <acceptance_criteria>
    自动验证 (audit-fidelity-grep modality):

    1. **4 Option H3 仍存在 + 顺序 A/B/C/D**:
       ```bash
       ADR=docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md
       awk '/^## Alternatives Considered/,/^## References/' $ADR | grep -nE '^### Option [ABCD]' | tee /tmp/opt.list
       wc -l /tmp/opt.list | awk '{print $1}' | xargs test 4 -eq
       awk -F: '{print $2}' /tmp/opt.list | tr -d ' ' | sort | uniq | wc -l | xargs test 4 -eq  # 4 distinct
       ```

    2. **每个 Option 含 5 子标 (Positive/Negative/Risks/Verdict/Evidence chain)** (FIX per cross-AI HIGH#3: 用 per-option specific end markers, 避免 `^### Option ` 自终止 Option A start line):
       ```bash
       # 关联 start → end pattern (option-specific 避免 self-termination)
       for pair in 'Option A:|Option B:' 'Option B:|Option C:' 'Option C:|Option D:' 'Option D:|## References'; do
         start="${pair%|*}"; end="${pair#*|}"
         section=$(awk "/^### ${start}/,/^${end}/" $ADR)
         echo "$section" | grep -cF '**Positive**' | xargs test 1 -le
         echo "$section" | grep -cF '**Negative**' | xargs test 1 -le
         echo "$section" | grep -cF '**Risks**' | xargs test 1 -le
         echo "$section" | grep -cE '\*\*Verdict\*\*' | xargs test 1 -le
         echo "$section" | grep -cE '\*\*Evidence chain\*\*' | xargs test 1 -le
       done
       ```

    3. **每个 Option 含 ≥6 个 `- ` bullet** (≥2 per 4 子标 Positive+Negative+Risks 共; Verdict + Evidence chain 各 1 行 prose) (FIX same as #2):
       ```bash
       for pair in 'Option A:|Option B:' 'Option B:|Option C:' 'Option C:|Option D:' 'Option D:|## References'; do
         start="${pair%|*}"; end="${pair#*|}"
         opt="${start%:}"
         count=$(awk "/^### ${start}/,/^${end}/" $ADR | grep -cE '^- ')
         echo "$opt bullets=$count"
         [ $count -ge 6 ]
       done
       ```

    4. **Verdict 词汇白名单** (rejected / accepted, 不允许 TBD / pending / etc.):
       ```bash
       awk '/^## Alternatives Considered/,/^## References/' $ADR | grep -E '^\*\*Verdict\*\*' | grep -vE '\b(rejected|accepted)\b' && exit 1 || true
       awk '/^## Alternatives Considered/,/^## References/' $ADR | grep -cE '^\*\*Verdict\*\*.*\b(rejected|accepted)\b' | xargs test 4 -eq
       ```

    5. **Evidence chain 引用 audit § cross-ref ≥4 次**:
       ```bash
       awk '/^## Alternatives Considered/,/^## References/' $ADR | grep -cE '\*\*Evidence chain\*\*.*audit §' | xargs test 4 -le
       ```

    6. **Option C + D 各标 'audit recommended' / 'audit co-equal'** (cross-doc fidelity to audit §4.1 + §0 elevation):
       ```bash
       awk '/^### Option C:/,/^### Option D:/' $ADR | grep -F 'audit recommended'
       awk '/^### Option D:/,/^## References/' $ADR | grep -F 'audit co-equal'
       ```

    7. **Option A + B Verdict 含 'rejected' + cite audit §4.3**:
       ```bash
       awk '/^### Option A:/,/^### Option B:/' $ADR | grep -F 'rejected'
       awk '/^### Option A:/,/^### Option B:/' $ADR | grep -F 'audit §4.3'
       awk '/^### Option B:/,/^### Option C:/' $ADR | grep -F 'rejected'
       awk '/^### Option B:/,/^### Option C:/' $ADR | grep -F 'audit §4.3'
       ```

    8. **顶层引言段追加句**:
       ```bash
       awk '/^## Alternatives Considered/,/^### Option A:/' $ADR | grep -F 'Option C (audit §4.2 推荐 — 两腿都推进) + Option D (audit §5 双轴模型 co-equal output)'
       ```

    全部 8 项 PASS = T2 完成.
  </acceptance_criteria>

  <verify>
    <automated>bash -c 'set -e; ADR=docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md; awk "/^## Alternatives Considered/,/^## References/" $ADR | grep -qE "^### Option A:" && awk "/^## Alternatives Considered/,/^## References/" $ADR | grep -qE "^### Option D:" && [ $(awk "/^## Alternatives Considered/,/^## References/" $ADR | grep -cE "^### Option [ABCD]:") -eq 4 ] && for pair in "Option A:|Option B:" "Option B:|Option C:" "Option C:|Option D:" "Option D:|## References"; do start="${pair%|*}"; end="${pair#*|}"; opt="${start%:}"; c=$(awk "/^### ${start}/,/^${end}/" $ADR | grep -cE "^- "); [ $c -ge 6 ] || { echo "FAIL: $opt only $c bullets"; exit 1; }; done && [ $(awk "/^## Alternatives Considered/,/^## References/" $ADR | grep -cE "^\*\*Verdict\*\*.*\\b(rejected|accepted)\\b") -eq 4 ] && awk "/^### Option C:/,/^### Option D:/" $ADR | grep -qF "audit recommended" && awk "/^### Option D:/,/^## References/" $ADR | grep -qF "audit co-equal" && echo T2 ALL 8 GREP PASS (per-option specific end markers per HIGH#3 fix)'</automated>
  </verify>

  <done>
    - 4 Option (A/B/C/D) 各自展开 5 子标 (Positive/Negative/Risks/Verdict/Evidence chain)
    - 每 Option ≥6 个 `- ` bullets
    - Verdict 全部 rejected/accepted 锁词汇
    - Evidence chain 引用 audit § cross-ref ≥4 处
    - Option C + D 各标 'audit recommended' / 'audit co-equal'
    - 顶层引言段追加 Accepted 状态说明
    - 8 grep / awk acceptance criterion 全 PASS
    - Commit subject `docs(adrs): T2 ADR-V2-024 Alternatives 4 Options Positive/Negative/Risks` ≤72 chars
    - Footer Generated-By + Co-Authored-By
  </done>
</task>

<task type="auto">
  <name>Task 3 (DECIDE-03 part 3/5): ADR-V2-024 Enforcement 填 + audit doc §6 Open Items 5 行 backfill</name>

  <!-- meta
  review_protocol: gsd-standard
  review_trigger: 涉及 audit doc 历史快照修改 (§6 backfill); 走 inline self-check + grep assertion
  review_estimated_overhead: +3min
  -->

  <files>docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md, docs/design/EAASP/adrs/decisions/2026-04-27-leg-decision-audit.md</files>

  <read_first>
    - docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md (frontmatter L13-17 enforcement 段; L117-130 Implementation 段)
    - docs/design/EAASP/adrs/decisions/2026-04-27-leg-decision-audit.md §6 Open Items (5 行 + §6.1 优先级矩阵 — 待 backfill)
    - .planning/phases/04.2-decide-document/04.2-CONTEXT.md `<open_items_resolved>` (5 条 verbatim answer 来源)
    - .planning/phases/04.2-decide-document/04.2-PATTERNS.md §"T3 — ADR-V2-024 Enforcement + audit §6 backfill"
    - docs/design/EAASP/adrs/ADR-V2-019-l1-runtime-deployment-model.md (frontmatter precedent — strategic level 允许 trace=[])
  </read_first>

  <action>
本任务双目标: (1) ADR-V2-024 Enforcement 段从占位形态填实 (F4 lint 要求 review_checklist 非空 + level=strategic 允许 trace=[] per V2-019/V2-023 precedent), (2) audit doc §6 Open Items 5 行 verbatim backfill 来自 CONTEXT.md `<open_items_resolved>` (5 条已收齐 yes/no 答案), 关闭 Phase 4.1 留下的 5 条待补条目。

### Step 3.1 — ADR-V2-024 Enforcement 段确认 + Implementation 段更新

**当前 frontmatter L13-17** 已经满足 F4 schema (level=strategic, trace=[], review_checklist 非空):

```yaml
enforcement:
  level: strategic
  trace: []
  review_checklist: "docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md"
```

T3 不动 frontmatter (T4 才动 supersedes). T3 唯一动作: 把 Implementation 段 (L117-130) 从 "草稿状态 + Phase 4.2 graduation deltas" 列表更新为 "Phase 4.2 已落实情况" 完成清单。

**替换 Implementation 段 (L117-130)** 为:

```markdown
## Implementation

### Phase 4.1 (草稿落盘):
- 创建 audit doc + 本 ADR Proposed 草稿 ✓
- F1-F3 lint pass ✓ (commit `0542139`)
- audit Open Items 列出待 user 补 5 条 ✓ (audit §6)

### Phase 4.2 (Accepted 翻转):
- audit Open Items 5 条 user 已补 ✓ (T3 backfill, 见 audit §6 + 本 ADR §References)
- Decision 段填实 ✓ (T1 layered 锁文)
- Alternatives Considered 4 Option Positive/Negative/Risks 展开 ✓ (T2)
- Enforcement 段确认 strategic level + review_checklist 非空 ✓ (T3, F4 lint 要求满足)
- frontmatter supersedes ADR-V2-023 ✓ (T4 — ADR-V2-024 supersedes 列出 V2-023 + V2-023 frontmatter flip Superseded)
- F4 lint pass + status Proposed → Accepted ✓ (T5 — slash command fallback path 见 T5 acceptance)
```

### Step 3.2 — audit doc §6 Open Items 5 行 verbatim backfill

audit doc §6 当前每行末尾留 "**待 user 补**: ..." 文字, T3 在每行后追加 user 已补答案 (来自 CONTEXT.md `<open_items_resolved>` verbatim, 不允许同义替换).

**修改 audit doc §6 第 1 项 §P5.2** — 在 "**ADR-block: yes**" 行后追加:

```markdown
- **Phase 4.2 backfill (2026-04-XX, T3 task)**: **0 家** (verdict: no, ADR-block:yes 解除). User 直陈 — 公司在 EAASP 同仓孵化期, 客户还没被外部试压. 明确负面 evidence 也是 evidence (per CONTEXT.md `<open_items_resolved>` Open Item #1).
```

**修改 audit doc §6 第 2 项 §P5.4** — 在 "推荐格式" 句后追加:

```markdown
- **Phase 4.2 backfill (2026-04-XX, T3 task)**: **Grid 全栈 ≈60% / EAASP 引擎 ≈30% / 元工作 (planning/audit/governance) ≈10%** (verdict: yes, 从 partial 收敛). 与 PRE-AUDIT-NOTES §B.2 baseline "Grid 主战场" 一致 (精确化 60/30/10) (per CONTEXT.md `<open_items_resolved>` Open Item #2).
```

**修改 audit doc §6 第 3 项 §F.Q1** — 在 "字段级" 句后追加:

```markdown
- **Phase 4.2 backfill (2026-04-XX, T3 task)**: **5+3 切分** (verdict: yes, 从 partial 收敛). Engine 内置 5 项 = `priority_tier` (替代原 `dispatch_level` — 跨行业通用名) / `risk_class` / `latency_class` / `audit_trace` / `model_lock`. 厂商写 3 项 = SCADA 接入适配 / 业务 KPI 定义 / 行业合规表. 架构承诺: EAASP L3/L4 必须提供 extension hook API, 详细 API 推到 Phase 4.3+ 独立 ADR (合并 §3.3 Q3 ⚫ 6 项接入位 ADR 候选) (per CONTEXT.md `<open_items_resolved>` Open Item #4).
```

**修改 audit doc §6 第 4 项 §F.Q4** — 在 "回答" 句后追加:

```markdown
- **Phase 4.2 backfill (2026-04-XX, T3 task)**: **grid-cli + grid-server** 优先发力 (verdict: yes 已锁 + 五者优先 2 个收敛). grid-desktop / grid-platform / web* 静态 dormant 到下个 milestone. grid-cli 已 active (主要入口), grid-server 单租户工作台 × EAASP L1 双身份完整覆盖开发者 + 单租户场景 (per CONTEXT.md `<open_items_resolved>` Open Item #3).
```

**修改 audit doc §6 第 5 项 (框架修订决策)** — 在 "若同意, ADR-V2-024 是否同时 supersede ADR-V2-023" 句后追加:

```markdown
- **Phase 4.2 backfill (2026-04-XX, T3 task)**: **采纳 §5 双轴模型作为 Phase 4 product scope 主框架** + **ADR-V2-024 完全 supersede ADR-V2-023** (verdict: yes, Phase 4.2 完整落实 audit §0 elevation). 双轴模型职责切清楚 (engine = user, data/integration = 他人); 下个 milestone 起点清洁不背 Leg A/B 包袱; T6 sweep 工作量 ~1-2h 已纳入本 phase plan (per CONTEXT.md `<open_items_resolved>` Open Item #5).
```

### Step 3.3 — 不动其他段

T3 仅修改 ADR-V2-024 Implementation 段 + audit doc §6 5 项 backfill 追加. 不动 audit §1-§5 + §7 + §6.1 优先级矩阵 (优先级矩阵原文反映"待补"状态保留, backfill 是新增行, 不替换矩阵).

### Commit

`docs(adrs): T3 ADR-V2-024 Enforcement + audit §6 Open Items 5 行 backfill`
  </action>

  <acceptance_criteria>
    1. **ADR-V2-024 Implementation 段含 "Phase 4.2 (Accepted 翻转)" 完成清单**:
       ```bash
       grep -c '^### Phase 4.2 (Accepted 翻转):' docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md | xargs test 1 -eq
       ```

    2. **Implementation 段含 6 条 ✓ 标记** (audit Open Items / Decision / Alternatives / Enforcement / supersedes / lint pass):
       ```bash
       awk '/^### Phase 4.2 \(Accepted/,/^## /' docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md | grep -c '✓' | xargs test 6 -le
       ```

    3. **audit doc §6 5 项各含 "Phase 4.2 backfill (2026-04-XX, T3 task)" anchor**:
       ```bash
       grep -c 'Phase 4.2 backfill (2026-04-' docs/design/EAASP/adrs/decisions/2026-04-27-leg-decision-audit.md | xargs test 5 -eq
       ```

    4. **§P5.2 backfill 含 "0 家" verbatim**:
       ```bash
       awk '/^- \*\*§P5\.2/,/^- \*\*§P5\.4/' docs/design/EAASP/adrs/decisions/2026-04-27-leg-decision-audit.md | grep -F '0 家'
       ```

    5. **§P5.4 backfill 含 "Grid 全栈 ≈60%" verbatim**:
       ```bash
       awk '/^- \*\*§P5\.4/,/^- \*\*§F\.Q1/' docs/design/EAASP/adrs/decisions/2026-04-27-leg-decision-audit.md | grep -F 'Grid 全栈 ≈60%'
       ```

    6. **§F.Q1 backfill 含 "priority_tier" + "5+3 切分" verbatim**:
       ```bash
       awk '/^- \*\*§F\.Q1/,/^- \*\*§F\.Q4/' docs/design/EAASP/adrs/decisions/2026-04-27-leg-decision-audit.md | grep -F 'priority_tier'
       awk '/^- \*\*§F\.Q1/,/^- \*\*§F\.Q4/' docs/design/EAASP/adrs/decisions/2026-04-27-leg-decision-audit.md | grep -F '5+3 切分'
       ```

    7. **§F.Q4 backfill 含 "grid-cli + grid-server" verbatim**:
       ```bash
       awk '/^- \*\*§F\.Q4/,/^- \*\*\(框架修订/' docs/design/EAASP/adrs/decisions/2026-04-27-leg-decision-audit.md | grep -F 'grid-cli + grid-server'
       ```

    8. **(框架修订决策) backfill 含 "采纳 §5 双轴模型" + "完全 supersede ADR-V2-023" verbatim**:
       ```bash
       awk '/^- \*\*\(框架修订/,/^### §6\.1/' docs/design/EAASP/adrs/decisions/2026-04-27-leg-decision-audit.md | grep -F '采纳 §5 双轴模型'
       awk '/^- \*\*\(框架修订/,/^### §6\.1/' docs/design/EAASP/adrs/decisions/2026-04-27-leg-decision-audit.md | grep -F '完全 supersede ADR-V2-023'
       ```

    全部 8 项 PASS = T3 完成.
  </acceptance_criteria>

  <verify>
    <automated>bash -c 'set -e; ADR=docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md; AUDIT=docs/design/EAASP/adrs/decisions/2026-04-27-leg-decision-audit.md; [ $(grep -c "^### Phase 4.2 (Accepted 翻转):" $ADR) -eq 1 ] && [ $(awk "/^### Phase 4.2 \(Accepted/,/^## /" $ADR | grep -c "✓") -ge 6 ] && [ $(grep -c "Phase 4.2 backfill (2026-04-" $AUDIT) -eq 5 ] && grep -qF "0 家" $AUDIT && grep -qF "Grid 全栈 ≈60%" $AUDIT && grep -qF "priority_tier" $AUDIT && grep -qF "5+3 切分" $AUDIT && grep -qF "grid-cli + grid-server" $AUDIT && grep -qF "采纳 §5 双轴模型" $AUDIT && grep -qF "完全 supersede ADR-V2-023" $AUDIT && echo T3 ALL 8 GREP PASS'</automated>
  </verify>

  <done>
    - ADR-V2-024 Implementation 段更新为 Phase 4.2 完成清单 (≥6 ✓ 标记)
    - audit doc §6 5 项各加 "Phase 4.2 backfill (2026-04-XX, T3 task)" anchor 行
    - 5 项答案 verbatim from CONTEXT.md `<open_items_resolved>` (无同义替换)
    - 8 grep acceptance criterion 全 PASS
    - Commit subject ≤72 chars
    - Footer Generated-By + Co-Authored-By
  </done>
</task>

<task type="auto">
  <name>Task 4 (DECIDE-03 part 4/5): ADR-V2-024 supersedes V2-023 + V2-023 frontmatter flip Superseded</name>

  <!-- meta
  review_protocol: gsd-standard
  review_trigger: 双向 supersede 锁链 + ADR 状态机修改 (V2-023 Accepted → Superseded 是 repo 历史首次)
  review_estimated_overhead: +5min
  -->

  <files>docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md, docs/design/EAASP/adrs/ADR-V2-023-grid-two-leg-product-strategy.md</files>

  <read_first>
    - docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md (frontmatter L1-26, supersedes 字段当前 [])
    - docs/design/EAASP/adrs/ADR-V2-023-grid-two-leg-product-strategy.md (frontmatter L1-25; status/superseded_by/deprecated_at/deprecated_reason 4 字段当前)
    - .adr-plugin/scripts/adr_lint.py (F1-F4 schema validation, AdrMeta.validate())
    - .planning/phases/04.2-decide-document/04.2-PATTERNS.md §"T4 — supersede 双向锁链 schema"
    - docs/design/EAASP/adrs/ADR-V2-019-l1-runtime-deployment-model.md (multi-line YAML supersedes precedent — V2-019 自身 supersedes:[] 但同 author multi-line YAML form 在其他字段如 affected_modules 已实证)
  </read_first>

  <action>
本任务双目标: (1) ADR-V2-024 frontmatter `supersedes:` 多行 YAML list 加 `- ADR-V2-023`, (2) ADR-V2-023 frontmatter 4 字段 flip 反映 Superseded 状态. 这是本仓库 ADR 历史**首次** supersede 操作 (per `git log --all --grep "supersede"` returns 0).

### Step 4.1 — 修改 ADR-V2-024 frontmatter `supersedes:` 字段

**当前 form** (line 8): `supersedes: []`

**目标 form** (multi-line YAML list, 顺序敏感):

```yaml
supersedes:
  - ADR-V2-023
```

**Edit 操作** — Edit tool 把 `supersedes: []` 替换为 multi-line form (单 entry 但仍用 list 形式以保持 schema 一致, per ADR-V2-019 affected_modules multi-line precedent).

**awk 跨 lowercase prefix 字段 — 手动测一次** (per Phase 4.1 T5 deviation Recommendation): 验证 `supersedes` 字段范围 awk pattern. 用 `sed -n '/^supersedes:/,/^superseded_by:/p'` 而非 `awk '/^supersedes:/,/^[a-z]/'` (后者会因下一字段 `superseded_by:` 也 lowercase 起始而过早终止 — Phase 4.1 T5 awk pattern bug 教训).

### Step 4.2 — 修改 ADR-V2-023 frontmatter 4 字段

**当前 V2-023 frontmatter** (line 4-12):

```yaml
status: Accepted
accepted_at: 2026-04-19
date: 2026-04-19
phase: "Phase 3.5 预热"
author: "Jiangwen Su"
supersedes: []
superseded_by: null
deprecated_at: null
deprecated_reason: null
```

**目标 form** — 4 字段 flip:

```yaml
status: Superseded
accepted_at: 2026-04-19
date: 2026-04-19
phase: "Phase 3.5 预热"
author: "Jiangwen Su"
supersedes: []
superseded_by: ADR-V2-024
deprecated_at: 2026-04-XX  # commit 当天日期
deprecated_reason: "Superseded by ADR-V2-024 — 双轴模型 (engine vs data/integration) 替代 Leg A/B 二元框架, 详见 ADR-V2-024 Decision 段 + audit doc §5"
```

**4 字段 delta**:
1. `status: Accepted` → `status: Superseded`
2. `superseded_by: null` → `superseded_by: ADR-V2-024` (string scalar, 非 list)
3. `deprecated_at: null` → `deprecated_at: 2026-04-XX` (commit 当天, executor 替换为 `date +%Y-%m-%d` 实际值)
4. `deprecated_reason: null` → `deprecated_reason: "Superseded by ADR-V2-024 — 双轴模型 (engine vs data/integration) 替代 Leg A/B 二元框架, 详见 ADR-V2-024 Decision 段 + audit doc §5"`

`accepted_at: 2026-04-19` 保持 (历史事实, V2-023 仍记录其 Accepted 起点).

### Step 4.2.5 — 更新 ADR-V2-023 body Status 行 (REVISION per cross-AI MEDIUM: frontmatter + body 必须同步, 否则讨论引用 V2-023 看 body 仍以为 Accepted)

ADR-V2-023 body 第 5 行当前是 `**Status:** Accepted` (frontmatter 后第一行 H1 后 metadata)。

**Edit**: 替换为 `**Status:** Superseded by ADR-V2-024 (2026-04-XX)`

(2026-04-XX 与 deprecated_at 同步当天日期。frontmatter 与 body 双同步是 first-supersede 治理完整性要求。)

### Step 4.3 — F1-F3 lint 重新跑 (T5 才跑 F4, 但 T4 后 F1-F3 不应 break)

```bash
python .adr-plugin/scripts/adr_lint.py --check F1,F2,F3 --ci docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md
echo "V2-024 F1-F3 exit: $?"
python .adr-plugin/scripts/adr_lint.py --check F1,F2,F3 --ci docs/design/EAASP/adrs/ADR-V2-023-grid-two-leg-product-strategy.md
echo "V2-023 F1-F3 exit: $?"
```

两个 ADR 都需 F1-F3 exit 0. V2-023 在 Superseded 状态下 F1-F3 仍应 PASS (lint schema 允许 Superseded status, deprecated_at + deprecated_reason 非 null 时合法).

### Commit

`docs(adrs): T4 ADR-V2-024 supersedes V2-023 + V2-023 status flip Superseded`
  </action>

  <acceptance_criteria>
    1. **V2-024 supersedes 字段含 `- ADR-V2-023`** (multi-line YAML form):
       ```bash
       sed -n '/^supersedes:/,/^superseded_by:/p' docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md | grep -c '^  - ADR-V2-023' | xargs test 1 -eq
       ```

    2. **V2-024 supersedes 字段不再是 `[]` (空 list)**:
       ```bash
       grep -c '^supersedes: \[\]' docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md | xargs test 0 -eq
       ```

    3. **V2-023 status: Superseded**:
       ```bash
       grep -c '^status: Superseded' docs/design/EAASP/adrs/ADR-V2-023-grid-two-leg-product-strategy.md | xargs test 1 -eq
       ```

    4. **V2-023 status: Accepted 已移除** (确保不残留):
       ```bash
       awk '/^---$/{c++; if(c==2) exit} c==1' docs/design/EAASP/adrs/ADR-V2-023-grid-two-leg-product-strategy.md | grep -c '^status: Accepted' | xargs test 0 -eq
       ```

    5. **V2-023 superseded_by: ADR-V2-024** (string scalar 非 list):
       ```bash
       grep -c '^superseded_by: ADR-V2-024$' docs/design/EAASP/adrs/ADR-V2-023-grid-two-leg-product-strategy.md | xargs test 1 -eq
       ```

    6. **V2-023 deprecated_at 非 null** (具体日期 YYYY-MM-DD form):
       ```bash
       grep -E '^deprecated_at: 2026-[0-9]{2}-[0-9]{2}$' docs/design/EAASP/adrs/ADR-V2-023-grid-two-leg-product-strategy.md
       ```

    7. **V2-023 deprecated_reason 含 "Superseded by ADR-V2-024" + "双轴模型" anchor**:
       ```bash
       grep -F 'Superseded by ADR-V2-024' docs/design/EAASP/adrs/ADR-V2-023-grid-two-leg-product-strategy.md
       grep -F '双轴模型' docs/design/EAASP/adrs/ADR-V2-023-grid-two-leg-product-strategy.md
       ```

    8. **F1-F3 lint pass on both ADRs**:
       ```bash
       python .adr-plugin/scripts/adr_lint.py --check F1,F2,F3 --ci docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md
       python .adr-plugin/scripts/adr_lint.py --check F1,F2,F3 --ci docs/design/EAASP/adrs/ADR-V2-023-grid-two-leg-product-strategy.md
       ```

    9. **V2-023 body Status 行同步 Superseded by ADR-V2-024** (REVISION per cross-AI MEDIUM: frontmatter + body 必须双同步, first-supersede 讨论可发现):
       ```bash
       grep -E '^\*\*Status:\*\* Superseded by ADR-V2-024' docs/design/EAASP/adrs/ADR-V2-023-grid-two-leg-product-strategy.md
       ```

    全部 9 项 PASS = T4 完成.
  </acceptance_criteria>

  <verify>
    <automated>bash -c 'set -e; V24=docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md; V23=docs/design/EAASP/adrs/ADR-V2-023-grid-two-leg-product-strategy.md; [ $(sed -n "/^supersedes:/,/^superseded_by:/p" $V24 | grep -c "^  - ADR-V2-023") -eq 1 ] && [ $(grep -c "^supersedes: \[\]" $V24) -eq 0 ] && grep -q "^status: Superseded" $V23 && [ $(awk "/^---\$/{c++; if(c==2) exit} c==1" $V23 | grep -c "^status: Accepted") -eq 0 ] && grep -q "^superseded_by: ADR-V2-024\$" $V23 && grep -qE "^deprecated_at: 2026-[0-9]{2}-[0-9]{2}\$" $V23 && grep -qF "Superseded by ADR-V2-024" $V23 && grep -qF "双轴模型" $V23 && python .adr-plugin/scripts/adr_lint.py --check F1,F2,F3 --ci $V24 && python .adr-plugin/scripts/adr_lint.py --check F1,F2,F3 --ci $V23 && echo T4 ALL 8 GREP+LINT PASS'</automated>
  </verify>

  <done>
    - V2-024 supersedes multi-line YAML list 含 `- ADR-V2-023`
    - V2-023 status flip Accepted → Superseded
    - V2-023 superseded_by 字符串 ADR-V2-024
    - V2-023 deprecated_at 当天日期 (非 null)
    - V2-023 deprecated_reason 含 "Superseded by ADR-V2-024" + "双轴模型"
    - F1-F3 lint pass on both ADRs (V2-023 Superseded 状态合法)
    - awk 跨 lowercase prefix 字段用 sed 替代 (Phase 4.1 T5 deviation 修正)
    - 8 grep+lint acceptance criterion 全 PASS
    - Commit subject ≤72 chars
    - Footer Generated-By + Co-Authored-By
  </done>
</task>

<task type="auto">
  <name>Task 5 (DECIDE-03 part 5/5): F4 lint pass + ADR-V2-024 status Proposed → Accepted</name>

  <!-- meta
  review_protocol: gsd-standard
  review_trigger: ADR 状态机翻转 + slash command availability fallback gate
  review_estimated_overhead: +3min
  -->

  <files>docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md</files>

  <read_first>
    - docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md (frontmatter status + accepted_at, History 段)
    - .adr-plugin/scripts/adr_lint.py (F4 schema validation)
    - .planning/phases/04.1-discuss-audit-p5/04.1-01-SUMMARY.md §Deviations §Auto-fixed Issues 第 2 条 (Phase 4.1 T5 deviation `/adr:new` slash command unavailable + manual frontmatter fallback path 实证)
    - .planning/phases/04.2-decide-document/04.2-PATTERNS.md §"T5 — F4 lint + Accepted 翻转 + slash command fallback gate"
  </read_first>

  <action>
本任务收尾 ADR-V2-024 Proposed → Accepted 翻转, 含 slash command availability fallback gate (Phase 4.1 T5 deviation 教训应用).

### Step 5.0 — Static path selection (REVISION per cross-AI MEDIUM: `which adr` probe meaningless — slash command 不是 shell binary)

**静态策略**:
- **autonomous executor (gsd-execute-phase / gsd-quick) 模式 → Path B always** (本 phase frontmatter `autonomous: true`, 默认走 Path B)
- **interactive human-driven session → Path A optional** (用户可在 Claude Code 交互 session 内手动 `/adr:accept ADR-V2-024`, 但不在本 plan 范围)

```bash
# Phase 4.2 frontmatter 已锁 autonomous: true → Path B always
echo "Phase 4.2 mode: autonomous → Path B (manual frontmatter + adr_lint.py)"
```

**Path B 必走** (autonomous executor 默认):

```bash
# Step 5.1 — F4 lint pass (frontmatter 完整性)
python .adr-plugin/scripts/adr_lint.py --check F1,F2,F3,F4 --ci docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md
echo "V2-024 F1-F4 exit: $?"
# 必须 exit 0
```

如 F4 lint exit 非 0, 报错并 abort T5 (回 T3 / T4 修正再回来).

### Step 5.2 — Manual frontmatter status update (Path B fallback)

修改 ADR-V2-024 frontmatter 2 字段:

**当前**:
```yaml
status: Proposed
accepted_at: null
```

**目标**:
```yaml
status: Accepted
accepted_at: 2026-04-XX  # commit 当天 (用 `date +%Y-%m-%d` 实际值替换)
```

**Edit 操作** — Edit tool 替换 `status: Proposed` → `status: Accepted` 和 `accepted_at: null` → `accepted_at: <today>`.

### Step 5.3 — Body 顶层 Status 行更新

frontmatter 后 body 第 5 行当前: `**Status:** Proposed`

替换为: `**Status:** Accepted`

### Step 5.4 — History 段加新 row

History 表追加 row (replace 现有 1 row 为 2 row 表):

```markdown
| 日期 | 状态 | 变更说明 |
|------|------|---------|
| 2026-04-27 | Proposed | Phase 4.1 audit 完成后落盘草稿; Decision 段 verbatim 引用 audit §4.2 推荐措辞 + §0 verdict partial-needs-revision 要求 §5 co-equal; Enforcement/Alternatives 各 option Positive-Negative-Risks 段留 Phase 4.2 填实(per CONTEXT.md D-F-03) |
| 2026-04-XX | Accepted | Phase 4.2 翻 Accepted; Decision 段 layered 锁文 (T1) + Alternatives 4 Option 展开 (T2) + Enforcement 段确认 + audit §6 Open Items 5 行 backfill (T3) + supersedes ADR-V2-023 (T4) + F4 lint pass (T5 Path B fallback per Phase 4.1 T5 deviation precedent) |
```

`2026-04-XX` 替换为 commit 当天日期 (与 accepted_at 一致).

### Step 5.5 — F4 lint 重新跑 (Accepted 状态下 schema 应仍 PASS)

```bash
python .adr-plugin/scripts/adr_lint.py --check F1,F2,F3,F4 --ci docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md
# 必须 exit 0; Accepted 状态需 accepted_at 非 null + 已有 supersedes 链 (T4 已加)
```

### Step 5.6 — Final joint lint on V2-023 + V2-024 (REVISION per cross-AI MEDIUM: repo 历史首次 supersede, T4 仅跑 V2-023 F1-F3, T5 仅跑 V2-024 F4 — 缺 final 双 ADR 联合 audit)

```bash
# 联合 F1-F4 lint, 验证 supersede 链 cross-ADR consistency
echo "=== Joint F1-F4 lint on V2-023 + V2-024 ==="
python .adr-plugin/scripts/adr_lint.py --check F1,F2,F3,F4 --ci docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md docs/design/EAASP/adrs/ADR-V2-023-grid-two-leg-product-strategy.md
echo "Joint lint exit: $?"
# 必须 exit 0 — 双 ADR 同时 lint pass 证明 supersede 锁链 fully consistent
```

T5 commit 之前必须双 ADR joint lint exit 0; 不通过则回 T4 修正后再回 T5.

### Commit

`docs(adrs): T5 ADR-V2-024 status Proposed → Accepted (F4 lint pass)`
  </action>

  <acceptance_criteria>
    1. **status: Accepted**:
       ```bash
       grep -c '^status: Accepted$' docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md | xargs test 1 -eq
       ```

    2. **status: Proposed 已移除** (frontmatter 段不残留):
       ```bash
       awk '/^---$/{c++; if(c==2) exit} c==1' docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md | grep -c '^status: Proposed' | xargs test 0 -eq
       ```

    3. **accepted_at 非 null** (具体日期):
       ```bash
       grep -E '^accepted_at: 2026-[0-9]{2}-[0-9]{2}$' docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md
       ```

    4. **Body Status 行 = Accepted**:
       ```bash
       grep -c '^\*\*Status:\*\* Accepted$' docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md | xargs test 1 -eq
       ```

    5. **History 表含 2 row** (Proposed + Accepted):
       ```bash
       awk '/^## History/,/^$/' docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md | grep -cE '^\| [0-9]{4}-[0-9]{2}-[0-9]{2}' | xargs test 2 -eq
       ```

    6. **History 第 2 row 含 "Accepted" + "F4 lint pass"**:
       ```bash
       awk '/^## History/,/^$/' docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md | grep '| Accepted |' | grep -F 'F4 lint pass'
       ```

    7. **F4 lint exit 0**:
       ```bash
       python .adr-plugin/scripts/adr_lint.py --check F1,F2,F3,F4 --ci docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md
       ```

    8. **slash command fallback gate 实证 — commit message 含 "Path B fallback"** (FIX: 显式标 post-commit gate — 此项必须在 T5 commit 完成后由 verifier 在 VERIFICATION.md 验证, NOT pre-commit acceptance):
       ```bash
       # POST-COMMIT GATE — verifier (T8 / end-phase) 验证, T5 自身 acceptance 不跑
       # 通过判据: T5 commit body 提及 fallback path 走的是 Path B per Phase 4.1 T5 deviation
       git log -1 docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md | grep -F 'Path B fallback'
       ```

    全部 1-7 项 pre-commit PASS + 第 8 项 post-commit gate (verifier 在 VERIFICATION.md 验证) = T5 完成 (ADR-V2-024 Accepted 翻转完成 = DECIDE-03 5/5 全部完成).
  </acceptance_criteria>

  <verify>
    <automated>bash -c 'set -e; ADR=docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md; grep -q "^status: Accepted\$" $ADR && [ $(awk "/^---\$/{c++; if(c==2) exit} c==1" $ADR | grep -c "^status: Proposed") -eq 0 ] && grep -qE "^accepted_at: 2026-[0-9]{2}-[0-9]{2}\$" $ADR && grep -q "^\*\*Status:\*\* Accepted\$" $ADR && [ $(awk "/^## History/,/^\$/" $ADR | grep -cE "^\| [0-9]{4}-[0-9]{2}-[0-9]{2}") -eq 2 ] && awk "/^## History/,/^\$/" $ADR | grep "| Accepted |" | grep -qF "F4 lint pass" && python .adr-plugin/scripts/adr_lint.py --check F1,F2,F3,F4 --ci $ADR && echo T5 7/8 GREP+LINT PASS (8th = post-commit)'</automated>
  </verify>

  <done>
    - ADR-V2-024 status frontmatter Proposed → Accepted
    - accepted_at 当天日期 (非 null)
    - Body **Status:** 行同步 Accepted
    - History 表 2 row (Proposed 历史 + Accepted 翻转)
    - F4 lint exit 0
    - slash command Path B fallback 实证 (commit body 提 "Path B fallback per Phase 4.1 T5 deviation")
    - 8 acceptance criterion 全 PASS (8th 在 commit 后 git log 验证)
    - Commit subject ≤72 chars
    - Footer Generated-By + Co-Authored-By
  </done>
</task>

<task type="auto">
  <name>Task 6 (DECIDE-02 part 1/2 + GOVERNANCE-03): Sweep Leg A/B 措辞 + /gsd-resume-work 中段实测</name>

  <!-- meta
  review_protocol: skip
  review_trigger: mechanical pattern replacement, GOVERNANCE-03 实测放在 verifier 评估
  review_estimated_overhead: +0min (skip)
  -->

  <files>CLAUDE.md, docs/dev/WORK_LOG.md, docs/design/EAASP/DEFERRED_LEDGER.md</files>

  <read_first>
    - CLAUDE.md (整文件, 找 "Leg A" / "Leg B" 措辞)
    - docs/dev/WORK_LOG.md (整文件, 同上)
    - docs/design/EAASP/DEFERRED_LEDGER.md (整文件, 同上)
    - .planning/phases/04.2-decide-document/04.2-PATTERNS.md §"T6 — Sweep Leg A/B 措辞 + GOVERNANCE-03 实测"
    - .planning/phases/04.0-bootstrap-cleanup-gsd/04.0-01-PLAN.md T1 CLEANUP-01 chunk_type sweep (mechanical sweep precedent)
  </read_first>

  <action>
本任务双目标: (1) 仓库内 "Leg A" / "Leg B" 措辞 sweep (per ADR-V2-024 supersedes V2-023 工程后果 — audit §5.4 风险 #2 ~1-2h), (2) GOVERNANCE-03 `/gsd-resume-work` 中段实测 (Phase 4.1 SC#4 deferred 闭环, mechanical task 性质合适 trigger /clear).

### Step 6.0 — 排除范围 + grep 找候选行

**排除路径** (per CONTEXT.md D-D-04 Claude's Discretion):
- `docs/design/EAASP/adrs/ADR-V2-023*` — V2-023 自身保留措辞作历史 ADR 不动
- `.planning/phases/04.1-*/` — Phase 4.1 audit + discuss-phase 历史快照不动
- `.planning/phases/04.2-*/` — Phase 4.2 自身 (CONTEXT.md / DISCUSSION-LOG.md / PATTERNS.md / PLAN.md / SUMMARY.md) 不动
- `docs/design/EAASP/adrs/decisions/2026-04-27-leg-decision-audit.md` — audit 历史快照不动 (T3 backfill 已加新 row 不替换原 §6 待补行)
- git history (commit message 不动)
- archived `.planning/phases/` 之前 milestone 内容 (BA → 4a) 不动 — 历史快照保留

**必扫文件**:
- CLAUDE.md
- docs/dev/WORK_LOG.md (注意只扫顶部 GSD Adoption Notes / 当前 milestone section, 不动历史 entry)
- docs/design/EAASP/DEFERRED_LEDGER.md (注意只动 Active section + preamble, 历史 closed D-item 不动)

**Grep 找候选**:

```bash
# 找出所有引用文件
git grep -l 'Leg A\|Leg B' \
  -- ':!docs/design/EAASP/adrs/ADR-V2-023*' \
     ':!.planning/phases/04.1-*' \
     ':!.planning/phases/04.2-*' \
     ':!docs/design/EAASP/adrs/decisions/'
echo "Files with Leg A/B references:"

# 详细行号
git grep -n 'Leg A\|Leg B' \
  -- ':!docs/design/EAASP/adrs/ADR-V2-023*' \
     ':!.planning/phases/04.1-*' \
     ':!.planning/phases/04.2-*' \
     ':!docs/design/EAASP/adrs/decisions/'
```

### Step 6.1 — 替换措辞表

**替换原则**: 不删除旧措辞 (避免破坏历史可读性), 改为加注脚形式 — eg.:

| 原文措辞 | 替换为 |
|---------|--------|
| Leg A | engine 接入面 (原 Leg A, see ADR-V2-024 supersedes ADR-V2-023) |
| Leg B | Grid 独立 (原 Leg B, see ADR-V2-024 supersedes ADR-V2-023) |
| "Leg A 硬化" | "engine 硬化 (原 Leg A 硬化, see ADR-V2-024)" |
| "Leg B 激活" | "Grid 独立产品激活 (原 Leg B 激活, see ADR-V2-024)" |
| "Leg A/B" 或 "Leg A vs B" | "engine vs data/integration (原 Leg A/B 二元框架, see ADR-V2-024 §1)" |
| 表格列名 "Leg A" / "Leg B" | "engine 接入面 / data/integration 接入面" + 表格脚注 |

**首次出现替换全文 + 后续出现简化**: 每文件第一次替换用完整 anchor 形式, 后续相同段内出现可用简化形 ("engine 接入面" + 隐含的 see-link).

### Step 6.2 — 逐文件应用替换 (Edit tool, 不写 sed)

(REVISION per cross-AI review HIGH#1 + HIGH#2 + HIGH#4: T6 sweep scope 扩展到 5 文件 active-doc 全集; GOVERNANCE-03 升级 hard gate; DEFERRED_LEDGER no-op 直接从 sweep target 移除避免矛盾)

**T6 sweep 全量 in-scope file 列表** (5 文件, per CONTEXT.md must-have + REVIEWS.md HIGH#1):

| 文件 | Pre-sweep occurrence | Sub-commit subject |
|------|----------------------|-------------------|
| CLAUDE.md | 13 | `docs(claude): T6 sweep Leg A/B → engine vs data/integration` |
| .planning/REQUIREMENTS.md | 8 | `docs(requirements): T6 sweep Leg A/B → engine vs data/integration` |
| .planning/PROJECT.md | 4 | `docs(project): T6 sweep Leg A/B → engine vs data/integration` |
| .planning/ROADMAP.md | 12 | `docs(roadmap): T6 sweep Leg A/B → engine vs data/integration` |
| docs/dev/WORK_LOG.md | 2 (L117+L123) | `docs(work-log): T6 sweep Leg A/B → engine vs data/integration` |
| ~~DEFERRED_LEDGER.md~~ | **0 — REMOVED** | (no sub-commit, ledger 真无 occurrence per pre-sweep grep) |

逐文件 Edit (mechanical 但需保持上下文连贯, sed 风险大):

**1. CLAUDE.md sweep** — 第一文件应用替换. 完成后 commit:
```
docs(claude): T6 sweep Leg A/B → engine vs data/integration (CLAUDE.md, 13 hits)
```

**2. GOVERNANCE-03 hard gate (Phase 4.1 SC#4 闭环, REVISION per HIGH#2)**:

完成 CLAUDE.md sweep + commit 后, **创建 .continue-here.md 并 PAUSE — 需 user 手动 /clear + /gsd-resume-work 后才能继续**:

```bash
# Step 6.2.5 — GOVERNANCE-03 hard gate (mandatory pause)
cat > .planning/phases/04.2-decide-document/.continue-here.md <<EOF
# Phase 4.2 T6 sweep — checkpoint after CLAUDE.md (1/5 files done)

**Completed**: CLAUDE.md sweep + commit (\`<T6.1_HASH>\`)
**Remaining**: REQUIREMENTS.md, PROJECT.md, ROADMAP.md, WORK_LOG.md (4 files, ~26 occurrences total)
**Trigger**: GOVERNANCE-03 /gsd-resume-work hard gate (per CONTEXT.md D-E-02 + REVIEWS.md HIGH#2 fix)

## Hard-gate Resume protocol (user MUST manually run before T6 continues):
1. \`/clear\` (in Claude Code session)
2. \`/gsd-resume-work\` — 自动读 STATE.md + 本 .continue-here.md, 恢复 Phase 4.2 active context
3. 验证恢复完整 (a/b/c 三档 — 见下方判据)
4. 用户在 SUMMARY.md §T6 GOVERNANCE-03 实测结果 段贴出 /gsd-resume-work 输出
5. 然后回到 PLAN T6 step 6.2.6 继续 sweep 剩余 4 文件

## 通过判据 (a/b/c 三档, 用户回填 SUMMARY.md §T6):
- **(a)** STATE.md frontmatter \`status\` 反映 active phase 4.2 ✓/✗
- **(b)** /gsd-resume-work 输出引用本 .continue-here.md (\`Resume file: ...continue-here.md\`) ✓/✗
- **(c)** 恢复后能继续 sweep 剩余文件 (无丢工作) ✓/✗

3 全 ✓ = SC#4 PASS; ≥1 ✗ = SC#4 PARTIAL, deferred + 加 D-item 到 DEFERRED_LEDGER (验证 gap 由 verifier 评估)。
EOF

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "  GSD ► T6 PAUSED FOR GOVERNANCE-03 HARD GATE"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "User action required:"
echo "  1. /clear (in Claude Code session)"
echo "  2. /gsd-resume-work"
echo "  3. Paste output to SUMMARY.md §T6"
echo "  4. Resume by re-invoking /gsd-execute-phase 4.2"
echo ""
echo "Resume file: .planning/phases/04.2-decide-document/.continue-here.md"
echo "GOVERNANCE-03 通过判据 a/b/c — 见 .continue-here.md"

# T6 在此 PAUSE — executor 必须 abort/yield, 不能继续 step 6.2.6
exit 78  # EX_CONFIG, signal to gsd-execute-phase to halt cleanly
```

**Hard-gate semantics (REVISION per cross-AI HIGH#2)**:
- T6 sweep 是 mechanical 性质适合 trigger /clear 的唯一时机 (cross-AI Q4 共识 — 不在 high-cognitive-load task 中段触发)
- autonomous executor 在 step 6.2.5 显式 abort with `exit 78`, 让 gsd-execute-phase capture clean halt signal
- user `/clear` + `/gsd-resume-work`, 验证 a/b/c 三档判据, 在 SUMMARY.md §T6 GOVERNANCE-03 实测结果 段贴出输出
- user 重新 invoke `/gsd-execute-phase 4.2`, executor 检测 .continue-here.md exists + SUMMARY.md §T6 实测结果 段已有 user-pasted output, T6 跳到 step 6.2.6 继续 sweep 剩余文件
- **不允许 user skip hard gate** — autonomous executor 必须 halt; user 自由选择 manually edit SUMMARY 标 SC#4=PARTIAL 然后跳过, 但需要显式编辑

**3-6. 剩余 4 文件 sweep** (step 6.2.6, post-resume):

3. **REQUIREMENTS.md sweep** (per HIGH#1, 8 hits):
```
docs(requirements): T6 sweep Leg A/B → engine vs data/integration (REQUIREMENTS.md, 8 hits)
```

4. **PROJECT.md sweep** (4 hits):
```
docs(project): T6 sweep Leg A/B → engine vs data/integration (PROJECT.md, 4 hits)
```

5. **ROADMAP.md sweep** (12 hits, 含 Phase Details framing):
```
docs(roadmap): T6 sweep Leg A/B → engine vs data/integration (ROADMAP.md, 12 hits)
```

6. **WORK_LOG.md sweep** (2 hits, 仅 GSD Adoption Notes 顶部段; 历史 entry 不动):
```
docs(work-log): T6 sweep Leg A/B → engine vs data/integration (WORK_LOG.md, 2 hits)
```

(DEFERRED_LEDGER.md 移除 sub-commit per HIGH#4 — pre-sweep grep 0 hit, 无需操作.)

### Step 6.3 — 删除 .continue-here.md trigger gate

T6 全部完成 (5 文件 sweep + commits, GOVERNANCE-03 hard gate 通过), 删除 .continue-here.md (一次性 artifact):

```bash
rm .planning/phases/04.2-decide-document/.continue-here.md
```

### Commit (T6 总 commit subject — 各 sub-commit 已分别提)

T6 包含 3 sub-commit + .continue-here.md create + .continue-here.md delete. 总 subject 选 first sub-commit subject 用作 task 主标识. atomic-commit-per-task 在本 task 弱化 (sweep 跨 3 文件 3 commit 是合理粒度).
  </action>

  <acceptance_criteria>
    1. **CLAUDE.md sweep 完成 — 排除 ADR-V2-023* 后 Leg A/B 引用 = 0 (除注脚 see-link)**:
       ```bash
       grep -cE 'Leg [AB]([^/]|$)' CLAUDE.md | xargs test 0 -eq
       # OR 允许带 see-link 的 anchor 形式 (eg. "原 Leg A, see ADR-V2-024")
       ```

       (注: acceptance 要求第一次替换后, 残留 "Leg A" / "Leg B" 必须 100% 在 see-link anchor 后, 即每个 Leg X 字符串后紧跟 "see ADR-V2-024" / 上下文 anchor)

    2. **WORK_LOG.md sweep 完成** (相同标准):
       ```bash
       # 历史 entry 不动, 但 Phase 4.2 段不应有裸 Leg A/B (无 anchor)
       awk '/^## .*[Pp]hase 4\.2/,/^## .*[Pp]hase 4a/' docs/dev/WORK_LOG.md | grep -cE 'Leg [AB]([^/]|$)' | xargs test 0 -eq
       ```

    3. **DEFERRED_LEDGER.md sweep 完成** (相同标准):
       ```bash
       # preamble + Active section sweep, closed item 历史不动
       awk '/^## Active/,/^## (Closed|Deprecated)/' docs/design/EAASP/DEFERRED_LEDGER.md | grep -cE 'Leg [AB]([^/]|$)' | xargs test 0 -eq
       ```

    4. **GOVERNANCE-03 trigger gate 已实证** (FIX: .continue-here.md 不进 git, 改为非-git 证据 — SUMMARY §T6 GOVERNANCE-03 实测结果 段必须包含 trigger gate timestamp + creation/cleanup proof):
       ```bash
       # T6 完成后, SUMMARY 中段 GOVERNANCE-03 实测结果记录段必须含 trigger gate creation 时间戳 + cleanup 完成 marker
       # T6 内 PLAN 要求: 在第 1 个 sub-commit (CLAUDE.md) 后 + 第 2 个 sub-commit (WORK_LOG.md) 前的时间窗口创建 .continue-here.md
       # T6 完成后 .continue-here.md 必须删除 (一次性 artifact)
       # 此 acceptance 在 SUMMARY (T8) 期间验证 — T6 内 self-check 仅验证 cleanup 后文件不存在
       test ! -f .planning/phases/04.2-decide-document/.continue-here.md  # T6 完成后必删
       # SUMMARY 内 verifier 验证 trigger gate 完整度由 T8 Phase Gate Composite 中 'GOVERNANCE-03 trigger gate' anchor 体现
       ```

    5. **GOVERNANCE-03 trigger gate cleanup verified** (改为 T6 自身 final state 验证):
       ```bash
       # T6 commit 完成后 .continue-here.md 不存在
       test ! -f .planning/phases/04.2-decide-document/.continue-here.md
       ```

    6. **5 sub-commit 各自存在** (REVISION per cross-AI HIGH#1: 加入 REQUIREMENTS / PROJECT / ROADMAP; HIGH#4: 移除 DEFERRED_LEDGER no-op sub-commit):
       ```bash
       git log --oneline -20 | grep -cE 'T6 sweep.*\(CLAUDE\.md' | xargs test 1 -le
       git log --oneline -20 | grep -cE 'T6 sweep.*\(REQUIREMENTS\.md' | xargs test 1 -le
       git log --oneline -20 | grep -cE 'T6 sweep.*\(PROJECT\.md' | xargs test 1 -le
       git log --oneline -20 | grep -cE 'T6 sweep.*\(ROADMAP\.md' | xargs test 1 -le
       git log --oneline -20 | grep -cE 'T6 sweep.*\(WORK_LOG\.md' | xargs test 1 -le
       ```

    7. **see-link anchor 形式至少 5 处** (5 sweep 文件各 ≥1) (REVISION per HIGH#1 5 文件 expansion):
       ```bash
       count=0; for f in CLAUDE.md .planning/REQUIREMENTS.md .planning/PROJECT.md .planning/ROADMAP.md docs/dev/WORK_LOG.md; do grep -qF 'see ADR-V2-024' "$f" 2>/dev/null && count=$((count+1)); done; [ "$count" -ge 5 ]
       ```

    8. **排除范围内文件未被改 (V2-023 ADR + audit doc + Phase 4.1 + Phase 4.2 自身 + DEFERRED_LEDGER)**:
       ```bash
       # git diff name-only 应不含排除路径
       git diff --name-only HEAD~7..HEAD | grep -cE '(ADR-V2-023[^/]*\.md|2026-04-27-leg-decision-audit\.md|\.planning/phases/04\.1-|DEFERRED_LEDGER\.md)' | xargs test 0 -eq
       ```

    9. **GOVERNANCE-03 hard gate 通过** (REVISION per HIGH#2: SUMMARY §T6 必须含 user-pasted /gsd-resume-work 输出 + a/b/c 三档判据 verdict; 不允许仅 trigger gate 创建即 PASS):
       ```bash
       grep -q '## §T6 GOVERNANCE-03 实测结果' .planning/phases/04.2-decide-document/04.2-01-SUMMARY.md
       grep -E '^- \*\*\(a\)\*\*.*✓|^- \*\*\(a\)\*\*.*✗' .planning/phases/04.2-decide-document/04.2-01-SUMMARY.md
       grep -E '^- \*\*\(b\)\*\*.*✓|^- \*\*\(b\)\*\*.*✗' .planning/phases/04.2-decide-document/04.2-01-SUMMARY.md
       grep -E '^- \*\*\(c\)\*\*.*✓|^- \*\*\(c\)\*\*.*✗' .planning/phases/04.2-decide-document/04.2-01-SUMMARY.md
       ```

    全部 9 项 PASS = T6 完成 (5 文件 sweep + GOVERNANCE-03 hard gate user-paste 实证). 不允许仅 trigger gate 创建.
  </acceptance_criteria>

  <verify>
    <automated>bash -c 'set -e; SUM=.planning/phases/04.2-decide-document/04.2-01-SUMMARY.md; for f in CLAUDE.md .planning/REQUIREMENTS.md .planning/PROJECT.md .planning/ROADMAP.md docs/dev/WORK_LOG.md; do [ $(grep -cE "Leg [AB]([^/]|\$)" "$f" 2>/dev/null | grep -v "see ADR-V2-024" || echo 0) -eq 0 ] || { echo "FAIL: $f has bare Leg A/B"; exit 1; }; done && [ ! -f .planning/phases/04.2-decide-document/.continue-here.md ] && [ $(git log --oneline -20 | grep -cE "T6 sweep.*\(CLAUDE\.md") -ge 1 ] && [ $(git log --oneline -20 | grep -cE "T6 sweep.*\(REQUIREMENTS\.md") -ge 1 ] && [ $(git log --oneline -20 | grep -cE "T6 sweep.*\(PROJECT\.md") -ge 1 ] && [ $(git log --oneline -20 | grep -cE "T6 sweep.*\(ROADMAP\.md") -ge 1 ] && [ $(git log --oneline -20 | grep -cE "T6 sweep.*\(WORK_LOG\.md") -ge 1 ] && (test -f $SUM && grep -q "## §T6 GOVERNANCE-03 实测结果" $SUM || echo "INFO: T6 verify pre-T8 — SUMMARY GOVERNANCE-03 段在 T8 时验证") && echo T6 ALL CORE GREP PASS (5 sub-commits + GOVERNANCE-03 hard gate)'</automated>
  </verify>

  <done>
    - 5 文件 (CLAUDE.md / REQUIREMENTS.md / PROJECT.md / ROADMAP.md / WORK_LOG.md) 裸 "Leg A/B" 措辞 = 0 (全部带 see-link anchor) — REVISION per HIGH#1 expansion
    - 5 sub-commit 分别提 (atomic per file in mechanical sweep) — REVISION per HIGH#1: REQUIREMENTS/PROJECT/ROADMAP added
    - .continue-here.md 在 step 6.2.5 创建 (CLAUDE.md commit 后) 作 GOVERNANCE-03 hard gate trigger
    - executor `exit 78` halt; user 手动 /clear + /gsd-resume-work + 在 SUMMARY §T6 GOVERNANCE-03 实测结果 段贴出 a/b/c 三档判据 — REVISION per HIGH#2 hard gate
    - .continue-here.md 在 T6 全部完成后删除 (一次性 artifact)
    - 排除路径 (ADR-V2-023*, 04.1-*, audit doc, DEFERRED_LEDGER) 未被改 — REVISION per HIGH#4: DEFERRED_LEDGER moved to exclusion (0 hit, no-op)
    - 9 acceptance criterion 全 PASS
    - Footer Generated-By + Co-Authored-By 在每 sub-commit
  </done>
</task>

<task type="auto">
  <name>Task 7 (DECIDE-02 part 2/2): PROJECT.md flip + ROADMAP SC ✅ + Milestone close + REVIEW_POLICY inline patch</name>

  <!-- meta
  review_protocol: gsd-standard
  review_trigger: 多文件 milestone close cascade; cross-doc 一致性 grep 必要
  review_estimated_overhead: +5min
  -->

  <files>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/REVIEW_POLICY.md</files>

  <read_first>
    - .planning/PROJECT.md (§Active "Phase 4 主决策" 行 L47 当前形态; §Validated 段当前内容)
    - .planning/ROADMAP.md (§Phase 4.2 5 SC + Milestone 关闭 traceability table)
    - .planning/REVIEW_POLICY.md (§2 trigger rows + §3 review_protocol 三档 + §7 Lesson 1 三经验 — Phase 4.1 commit `d4ba2b8`)
    - .planning/phases/04.2-decide-document/04.2-PATTERNS.md §"T7 — Milestone close cascade + REVIEW_POLICY inline patch"
    - .planning/phases/04.0-bootstrap-cleanup-gsd/04.0-01-SUMMARY.md (Phase 4.0 close-phase cascade precedent)
  </read_first>

  <action>
本任务三目标: (1) PROJECT.md §Active "Phase 4 主决策" 行划掉移入 §Validated, (2) ROADMAP §Phase 4.2 5 SC 全部 ✅ + Milestone close traceability table (CLEANUP-01..04 + DECIDE-01..03 + GOVERNANCE-01..03 全 ✅), (3) REVIEW_POLICY §3.5 modality table + §2.12 trigger row inline patch (per CONTEXT.md D-F-01 close-phase cascade 顺手, 不拆独立 task).

### Step 7.1 — PROJECT.md §Active flip

**当前 PROJECT.md L47** (per session reading earlier):
```markdown
- [ ] **Phase 4 主决策:engine vs data/integration 切分 + EAASP 分仓 graduation criteria + Grid 自身产品化路径**(详见 `.planning/phases/4.1-PRE-AUDIT-NOTES.md` §C.3 + §F audit agenda Q1-Q4)—— Phase 4.1 先 socratic discuss + §P5 重新框定 → ADR-V2-024 strategy-record
```

**目标 form (§Active 中此行删除, §Validated 中加新 row)**:

§Active 中删除 L47 行 (整行).

§Validated 段末尾追加 (FIX: 用 `✓` checkmark 与既有 Phase 4.0 §Validated row 风格一致):

```markdown
- ✓ **Phase 4 主决策**(2026-04-XX) —— 走"双轴模型(engine vs data/integration)主框架 + 两腿都推进(产品形态实例)" 路径; 详见 ADR-V2-024 (commit `<T5_COMMIT_HASH>`, status: Accepted, supersedes ADR-V2-023) + audit doc `docs/design/EAASP/adrs/decisions/2026-04-27-leg-decision-audit.md`
```

`<T5_COMMIT_HASH>` 由执行人员从 `git log --oneline -10 | grep "T5 ADR-V2-024 status Proposed"` 取 7-char hash 替换. `2026-04-XX` 替换为 commit 当天日期.

### Step 7.2 — ROADMAP §Phase 4.2 5 SC 全部 ✅

**当前 ROADMAP §Phase 4.2 5 SC**: 5 行无 ✅ marker (per ROADMAP §Phase 4.2 当前形态).

**目标 form**: 5 行各加 ✅ + 引用 commit hash:

```markdown
**Success Criteria** (what must be TRUE):
  1. ✅ `.planning/phases/4.2/PLAN.md` 内容反映 Phase 4.1 audit 决策方向 — 实施 5+3 切分 + grid-cli + grid-server 优先 + 双轴模型主框架 (per audit §4.1 §5.5 §F.Q1 §F.Q4 + Open Items §6 backfill T3 commit `<T3_HASH>`)
  2. ✅ ADR-V2-024 经创建 (Phase 4.1 Proposed commit `0542139`) + Decision/Alternatives/Enforcement 段 Phase 4.2 填实 (T1 `<T1_HASH>` + T2 `<T2_HASH>` + T3 `<T3_HASH>`); F1-F4 lint exit 0 (T5 `<T5_HASH>`)
  3. ✅ ADR-V2-024 status Accepted (T5 commit `<T5_HASH>` Path B fallback per Phase 4.1 T5 deviation precedent); supersedes ADR-V2-023 (T4 commit `<T4_HASH>`)
  4. ✅ PROJECT.md §Active "Phase 4 主决策" 行划掉移入 §Validated, 引用 ADR-V2-024 commit hash (T7 commit `<T7_HASH>`)
  5. ✅ Milestone 关闭检查点: CLEANUP-01..04 (Phase 4.0) + DECIDE-01..03 (Phase 4.1+4.2) + GOVERNANCE-01..03 (Phase 4.0+4.1+4.2) 全部 traceability ✅; debt water-line 无新增 P0/P1-active 项 (T7 commit + T8 SUMMARY)
```

`<TX_HASH>` 替换规则 (REVISION per cross-AI MEDIUM#2 — 不允许 self-referencing commit hash inside same commit):
- **T1-T6 hash**: T7 commit 时由 executor 用 `git log --oneline -10 | grep T1` 取实际 hash 替换 (T1-T6 已 commit)
- **T7 hash**: T7 commit 时**留为 `<T7_HASH>` placeholder**, **T8 SUMMARY task 期间 backfill** — T8 用 `git log -1 --format=%h` 取 T7 commit 实际 hash, 在 T8 同 commit 内 amend ROADMAP §Phase 4.2 SC#4 + REVIEW_POLICY §7 Lesson 1 entry 4 的 `<T7_HASH>` placeholder
- **T7 commit 不 amend** (atomic-commit-per-task 原则); 而是由 T8 在新 commit 内 patch 这两个 placeholder 字段 (commit subject: `docs(planning): Phase 4.2 SUMMARY + T7 hash backfill`)

### Step 7.3 — ROADMAP Coverage table 标记 ✅

**当前 ROADMAP Coverage** L (per session reading): 10 REQ-IDs 当前 4 ✅ (CLEANUP 已完成 4 个) + 6 待标.

**目标 form**: 全部 10 REQ-IDs ✅:

```markdown
| REQ-ID | Phase | Notes |
|--------|-------|-------|
| ✅ CLEANUP-01 | 4.0 | (already done) ... |
| ✅ CLEANUP-02 | 4.0 | (already done) ... |
| ✅ CLEANUP-03 | 4.0 | (already done) ... |
| ✅ CLEANUP-04 | 4.0 | (already done) ... |
| ✅ DECIDE-01 | 4.1 | audit doc commit `<various Phase 4.1>` |
| ✅ DECIDE-02 | 4.2 | PLAN.md + 5+3 切分 + grid-cli/server 优先 (T6+T7) |
| ✅ DECIDE-03 | 4.2 | ADR-V2-024 Accepted (T1-T5) |
| ✅ GOVERNANCE-01 | 4.0 | REVIEW_POLICY.md draft (commit `165cab4`) |
| ✅ GOVERNANCE-02 | 4.1 | GSD 端到端 (Phase 4.1 SUMMARY) |
| ✅ GOVERNANCE-03 | 4.1+4.2 | Phase 4.1 deferred → Phase 4.2 T6 trigger gate (commit `<T6_HASH>`) |
```

(✅ marker 加在 REQ-ID 前 — 不破 table column 数, 加在第一列 cell 文本前.)

### Step 7.4 — REVIEW_POLICY §3.5 modality table inline patch

**当前 §3** (per session reading earlier): 三档 review_protocol (skip / gsd-standard / superpowers).

**目标**: 在 §3 末尾追加 §3.5:

```markdown
### §3.5 Review modality table (Phase 4.1 Lesson 1 实证扩展)

| Modality | When to use | Cost | Coverage |
|----------|-------------|------|---------|
| `executor inline acceptance` | mechanical sweep / cleanup task; acceptance criteria 全 grep / lint / file-exist | low (no extra agent) | 等同 single-pass review for grep-verifiable artifacts |
| `independent reviewer agent` | code change / architecture change / cross-vendor contract change | high (extra agent + tokens) | 高于 inline (catches subjective issues) |
| `audit-fidelity-grep` | ADR Decision lock / cross-doc verbatim 引用 (eg. ADR-V2-024 Decision §4.2 + §5.5 引用 audit doc) | low | 等同 (cross-doc grep PASS = fidelity proven) |

**适用规则** (Phase 4.1 §7 Lesson 1 经验 1+3 实证):
- design-heavy phase task 在 single-agent autonomous executor 模式下默认 fallback `executor inline acceptance` (Phase 4.1 实证)
- ADR strategy 草稿 / 锁文 类 task 走 `audit-fidelity-grep` 比独立 reviewer 更可靠 (Phase 4.1 T5 + Phase 4.2 T1 实证)
- code change 类 task 仍走 `independent reviewer agent` (Phase 4.1 经验未实证此 modality, 保留 superpowers two-stage 路径)
```

### Step 7.5 — REVIEW_POLICY §2 第 12 个 trigger bullet inline patch

**当前 §2 schema** (per session reading): §2 High Risk 子段 (`### 🔴 High Risk — review_protocol: superpowers-two-stage`) 含 11 个 numbered bullet (1-11). NOT `### §2.X` H3 subheading 风格. 修订后必须匹配既有 schema = 加第 12 个 numbered bullet.

**目标**: 在 §2 High Risk 子段第 11 个 bullet 后追加第 12 个 numbered bullet (匹配既有 11 个 bullet 格式 — 每 bullet 可含子 bullet "- ..."):

```markdown
12. **ADR strategy 草稿创建 / 锁文 (Phase 4.1 + 4.2 实证)** — review_protocol: `audit-fidelity-grep` (per §3.5 modality table)
    - **触发**: 创建 / 修改 ADR type=strategy 的 Decision / Alternatives / Enforcement 段
    - **rationale**: cross-doc verbatim 引用 (eg. ADR Decision 段引用 audit doc 段) 的正确性用 grep PASS 证明; 独立 reviewer 复读两文档对比反而引入主观误判 (Phase 4.1 §7 Lesson 1 经验 3)
    - **acceptance criteria template**: 每 cross-doc 引用必须 `grep -F '<verbatim phrase>' <target_file>` exit 0 + `grep -F '<verbatim phrase>' <source_file>` exit 0; 用 awk + line count 验证段级 verbatim 复制完整性
```

插入位置: 在 §2 High Risk 子段第 11 个 bullet 末尾后, `### 🟡 Medium Risk` H3 之前. 不创建新 H3 subheading.

### Step 7.6 — REVIEW_POLICY §7 Lesson 1 entry 4 (Phase 4.2 实证)

**当前 §7 Lesson 1** (per Phase 4.1 commit `d4ba2b8`): 三经验.

**目标**: 在 Lesson 1 末尾追加 entry 4:

```markdown
4. (Phase 4.2 实证, commit `<T7_HASH>`) `audit-fidelity-grep` modality 在 Phase 4.2 T1 ADR-V2-024 Decision 段 layered 锁文 + T2 Alternatives Considered 4 Option 展开 + T3 audit §6 5 行 backfill 三 task 实证应用 — cross-doc 引用 audit §4.2 / §5.5 / §6 / §0 verbatim 完整复制, 8+8+8 grep acceptance criterion 全 PASS, 0 critical issue. 比独立 reviewer agent 复读两文档对比更可靠. §3.5 modality table + §2.12 trigger row 在本 phase 同步落地 (T7 inline patch).
```

`<T7_HASH>` 在 commit 后 backfill.

### Step 7.7 — REVIEW_POLICY L345 Last updated 字段

`Last updated: 2026-04-XX` (commit 当天).

### Commit

`docs(milestone-close): T7 PROJECT/ROADMAP/REVIEW_POLICY milestone close cascade`

(Note: 单 commit 含 3 文件 修改 — 这是 close-phase cascade 性质, atomic 在 task level 而非 file level. Phase 4.0 SUMMARY commit precedent 同模式.)
  </action>

  <acceptance_criteria>
    1. **PROJECT.md §Active 中 "Phase 4 主决策" 行已删除** (FIX: PROJECT.md uses `### Active`, not `## Active`):
       ```bash
       awk '/^### Active/,/^### (Validated|Out of Scope)/' .planning/PROJECT.md | grep -c 'Phase 4 主决策' | xargs test 0 -eq
       ```

    2. **PROJECT.md §Validated 中 "Phase 4 主决策 ✓" 新行存在** (FIX: existing §Validated rows use `✓` not `✅` per Phase 4.0 row precedent):
       ```bash
       awk '/^### Validated/,/^### Active/' .planning/PROJECT.md | grep -c '✓ \*\*Phase 4 主决策' | xargs test 1 -eq
       ```

    3. **ROADMAP §Phase 4.2 SC 全部 5 行有 ✅ marker** (FIX: POSIX-compliant awk range + [[:space:]] not \s):
       ```bash
       awk '/^### Phase 4\.2:/,/^## Phase 之外/' .planning/ROADMAP.md | grep -cE '^[[:space:]]+[0-9]+\.[[:space:]]+✅' | xargs test 5 -eq
       ```

    4. **ROADMAP Coverage table 10 REQ-IDs 全部 ✅**:
       ```bash
       awk '/^## Coverage/,/^## Granularity/' .planning/ROADMAP.md | grep -cE '^\| ✅ ' | xargs test 10 -eq
       ```

    5. **REVIEW_POLICY §3.5 modality table 存在**:
       ```bash
       grep -c '^### §3\.5 Review modality table' .planning/REVIEW_POLICY.md | xargs test 1 -eq
       awk '/^### §3\.5/,/^## 4\./' .planning/REVIEW_POLICY.md | grep -c '| Modality |' | xargs test 1 -eq
       ```

    6. **REVIEW_POLICY §2 高风险段第 12 个 trigger bullet 存在 — "ADR strategy 草稿创建"** (FIX: existing §2 schema is 11 numbered bullets in §2 High Risk subsection, NOT `### §2.X` subheadings — patch must match existing style):
       ```bash
       awk '/^### 🔴 High Risk/,/^### 🟡 Medium Risk/' .planning/REVIEW_POLICY.md | grep -cE '^[[:space:]]*12\.[[:space:]]+ADR strategy 草稿创建' | xargs test 1 -eq
       awk '/^### 🔴 High Risk/,/^### 🟡 Medium Risk/' .planning/REVIEW_POLICY.md | grep -F 'audit-fidelity-grep'
       ```

    7. **REVIEW_POLICY §7 Lesson 1 entry 4 存在 (Phase 4.2 实证)**:
       ```bash
       awk '/^### Lesson 1/,/^## 8\./' .planning/REVIEW_POLICY.md | grep -c 'Phase 4\.2 实证' | xargs test 1 -le
       ```

    8. **REVIEW_POLICY Last updated 字段 = 当天**:
       ```bash
       grep -E '^\*Last updated: 2026-[0-9]{2}-[0-9]{2}\*$' .planning/REVIEW_POLICY.md
       ```

    全部 8 项 PASS = T7 完成.
  </acceptance_criteria>

  <verify>
    <automated>bash -c 'set -e; [ $(awk "/^### Active/,/^### (Validated|Out of Scope)/" .planning/PROJECT.md | grep -c "Phase 4 主决策") -eq 0 ] && [ $(awk "/^### Validated/,/^### Active/" .planning/PROJECT.md | grep -c "✓ \*\*Phase 4 主决策") -eq 1 ] && [ $(awk "/^### Phase 4\\.2:/,/^## Phase 之外/" .planning/ROADMAP.md | grep -cE "^[[:space:]]+[0-9]+\\.[[:space:]]+✅") -eq 5 ] && [ $(awk "/^## Coverage/,/^## Granularity/" .planning/ROADMAP.md | grep -cE "^\\| ✅ ") -eq 10 ] && grep -q "^### §3\\.5 Review modality table" .planning/REVIEW_POLICY.md && [ $(awk "/^### 🔴 High Risk/,/^### 🟡 Medium Risk/" .planning/REVIEW_POLICY.md | grep -cE "^[[:space:]]*12\\.[[:space:]]+ADR strategy 草稿创建") -eq 1 ] && echo T7 ALL CORE GREP PASS'</automated>
  </verify>

  <done>
    - PROJECT.md §Active "Phase 4 主决策" 行删除 + §Validated 新 row 引用 ADR-V2-024 commit hash
    - ROADMAP §Phase 4.2 5 SC 全部 ✅ + Coverage 10 REQ-IDs 全 ✅
    - REVIEW_POLICY §3.5 modality table 加 (3 modality + 适用规则)
    - REVIEW_POLICY §2.12 ADR strategy 草稿创建 trigger row 加
    - REVIEW_POLICY §7 Lesson 1 entry 4 加 (Phase 4.2 实证)
    - REVIEW_POLICY Last updated 当天日期
    - 8 acceptance criterion 全 PASS
    - 单 commit (close-phase cascade 性质 atomic-at-task-level)
    - Footer Generated-By + Co-Authored-By
  </done>
</task>

<task type="auto">
  <name>Task 8: SUMMARY.md (Phase 4.2 close-phase cascade)</name>

  <!-- meta
  review_protocol: skip
  review_trigger: SUMMARY 标准模板, 内容由 T1-T7 commit hash 拼装
  review_estimated_overhead: +0min (skip)
  -->

  <files>.planning/phases/04.2-decide-document/04.2-01-SUMMARY.md</files>

  <read_first>
    - .planning/phases/04.0-bootstrap-cleanup-gsd/04.0-01-SUMMARY.md (Phase 4.0 SUMMARY frontmatter + body precedent)
    - .planning/phases/04.1-discuss-audit-p5/04.1-01-SUMMARY.md (Phase 4.1 SUMMARY 形态 — design-heavy phase + deviation 段, 含 T5 Path B fallback Rule 3 deviation 教训)
    - .planning/phases/04.2-decide-document/04.2-CONTEXT.md (decisions + open_items_resolved 摘要源)
    - .planning/phases/04.2-decide-document/04.2-01-PLAN.md (T1-T7 acceptance criterion + commit subject)
    - .planning/ROADMAP.md §Phase 4.2 5 SC (cross-ref 源)
  </read_first>

  <action>
本任务创建 Phase 4.2 SUMMARY.md, mirror Phase 4.0 / 4.1 SUMMARY 形态. SUMMARY 是 close-phase cascade 标准模板, 内容拼装 T1-T7 commit hash + acceptance 结果 + deviation 记录.

### SUMMARY.md 结构

```markdown
---
phase: 04.2
plan: 01
subsystem: planning, design, governance, milestone-close
tags: [ADR-V2-024-Accepted, supersedes-V2-023, 双轴模型, milestone-close, GOVERNANCE-03-trigger-gate]
dependency_graph:
  requires:
    - ADR-V2-023 (Accepted 2026-04-19, 本 phase supersede)
    - ADR-V2-024 (Proposed Phase 4.1 commit 0542139)
    - audit doc 2026-04-27-leg-decision-audit.md (Phase 4.1 落盘)
    - REVIEW_POLICY.md (Active Phase 4.1 commit d4ba2b8)
    - Phase 4.1 SUMMARY (T5 deviation precedent 教训)
  provides:
    - ADR-V2-024 Accepted (Decision layered 锁文 + Alternatives 4 Option 展开 + Enforcement 填 + supersedes V2-023 + F4 lint pass)
    - ADR-V2-023 Superseded (frontmatter 4 字段 flip)
    - audit doc §6 Open Items 5 行 backfill (5 yes/no 答案 verbatim from CONTEXT.md)
    - 仓库内 Leg A/B 措辞 sweep (CLAUDE.md / WORK_LOG / DEFERRED_LEDGER + see-link anchor)
    - PROJECT.md §Validated "Phase 4 主决策 ✅" 行 + ADR-V2-024 commit hash 引用
    - ROADMAP §Phase 4.2 5 SC ✅ + Coverage 10 REQ-IDs ✅
    - REVIEW_POLICY §3.5 modality table + §2.12 trigger row + §7 Lesson 1 entry 4
    - GOVERNANCE-03 trigger gate (.continue-here.md 中段创建+cleanup) — Phase 4.1 SC#4 闭环
  affects:
    - 下个 milestone (engine 子任务 + data/integration 接入面 子任务列, 由 ADR-V2-024 §1 §2 + Open Items §F.Q1 §F.Q4 驱动)
    - Phase 4.3+ (extension hook API 独立 ADR — audit §3.3 Q3 ⚫ 6 项接入位 ADR 候选)
tech_stack:
  added: []
  patterns:
    - "Layered ADR Decision 段 (top-line pin + §H3 双轴 substance + §H3 三选一推荐) — Phase 4.2 T1 实证"
    - "audit-fidelity-grep modality 实证应用 (cross-doc verbatim 引用 8 grep PASS = fidelity proven, 替代独立 reviewer agent for ADR strategy 草稿) — Phase 4.2 T1 + T2 + T3"
    - "ADR supersede 双向锁链 (V2-024 supersedes multi-line YAML + V2-023 superseded_by string scalar + 4 frontmatter field flip) — repo 历史首次 supersede"
    - "Slash command Path B fallback (autonomous executor 模式下 /adr:accept 不可用 → manual frontmatter status update + adr_lint.py --ci) — Phase 4.1 T5 deviation precedent 实证应用"
    - "GOVERNANCE-03 trigger gate (.continue-here.md 在 mechanical sweep 中段创建作 user 手动 /clear 测试入口) — Phase 4.1 deferred 闭环 模式"
key_files:
  created:
    - .planning/phases/04.2-decide-document/04.2-01-SUMMARY.md
  modified:
    - docs/design/EAASP/adrs/ADR-V2-024-phase4-product-scope-decision.md (T1+T2+T3+T4+T5)
    - docs/design/EAASP/adrs/ADR-V2-023-grid-two-leg-product-strategy.md (T4 — Superseded flip)
    - docs/design/EAASP/adrs/decisions/2026-04-27-leg-decision-audit.md (T3 §6 backfill)
    - CLAUDE.md (T6)
    - docs/dev/WORK_LOG.md (T6)
    - docs/design/EAASP/DEFERRED_LEDGER.md (T6)
    - .planning/PROJECT.md (T7 — §Active flip)
    - .planning/ROADMAP.md (T7 — SC ✅ + Coverage ✅)
    - .planning/REVIEW_POLICY.md (T7 — §3.5 + §2.12 + §7 Lesson 1 entry 4)
decisions:
  - DECIDE-02 落实: PLAN.md + 5+3 切分 + grid-cli/grid-server 优先 + 双轴模型主框架
  - DECIDE-03 落实: ADR-V2-024 Proposed → Accepted (Decision layered + Alternatives 4 Option + Enforcement 填 + supersedes V2-023 + F4 lint pass)
  - GOVERNANCE-03 闭环: T6 sweep 中段 .continue-here.md trigger gate 创建 + verifier 评估 — Phase 4.1 deferred 解
  - 框架修订决策: 采纳 §5 双轴模型 + ADR-V2-024 完全 supersede ADR-V2-023 (per Open Item #5)
  - REVIEW_POLICY §3.5 modality table + §2.12 trigger row 落地 — Phase 4.1 §7 Lesson 1 三经验实证扩展
metrics:
  duration: ~Xh (T1+T2 ~Xh + T3+T4+T5 ~Xh + T6+T7 ~Xh + T8 ~10min)
  completed_date: 2026-04-XX
  tasks_total: 8
  tasks_completed: 8
  tasks_skipped: 0
  commits: 9-10  # T1+T2+T3+T4+T5+T6(3 sub)+T7+T8 + close-phase cascade
  files_changed: 9
---

# Phase 04.2 Plan 01: Decide & Document / 决策落定 Summary

Phase 4.2 完整跑完 8/8 task — ADR-V2-024 Decision/Alternatives/Enforcement 段填实 + supersedes ADR-V2-023 (repo 历史首次 supersede) + F4 lint pass 翻 Accepted (T1-T5) + 仓库 Leg A/B 措辞 sweep (T6) + PROJECT/ROADMAP/REVIEW_POLICY milestone close cascade (T7) + SUMMARY (T8). 9-10 atomic commits, ROADMAP §Phase 4.2 5 SC 全 ✅, audit-fidelity-grep modality 在 ADR strategy 草稿 / 锁文 task 实证应用. GOVERNANCE-03 (Phase 4.1 deferred) 通过 T6 sweep 中段 .continue-here.md trigger gate 闭环.

## Task Outcomes

| Task | Status | Commit | Files | Notes |
|------|--------|--------|-------|-------|
| T1 — ADR-V2-024 Decision 段 layered 锁文 (双轴 + 三选一) | PASS | `<T1_HASH>` | ADR-V2-024 Decision 段 | 8 audit-fidelity-grep PASS; layered 顶层 + §1 §5.5 verbatim + §2 §4.2 verbatim |
| T2 — ADR-V2-024 Alternatives Considered 4 Option Positive/Negative/Risks | PASS | `<T2_HASH>` | ADR-V2-024 Alternatives 段 | 8 grep PASS; Option C 标 audit recommended + Option D 标 audit co-equal |
| T3 — ADR-V2-024 Enforcement 段 + audit doc §6 Open Items 5 行 backfill | PASS | `<T3_HASH>` | ADR-V2-024 Implementation + audit §6 | 8 grep PASS; 5 答案 verbatim from CONTEXT.md `<open_items_resolved>` |
| T4 — ADR-V2-024 supersedes V2-023 + V2-023 frontmatter flip Superseded | PASS | `<T4_HASH>` | ADR-V2-024 + ADR-V2-023 frontmatter | 8 grep+lint PASS; repo 历史首次 supersede; awk 跨 lowercase prefix 用 sed 替代 (Phase 4.1 T5 deviation 修正) |
| T5 — F4 lint pass + ADR-V2-024 status Proposed → Accepted | PASS | `<T5_HASH>` | ADR-V2-024 frontmatter + Body Status + History | 8 grep+lint PASS; Path B fallback 实证 (slash command unavailable in autonomous executor, per Phase 4.1 T5 deviation precedent) |
| T6 — Sweep Leg A/B 措辞 + GOVERNANCE-03 trigger gate | PASS | `<T6a_HASH>+T6b+T6c` | CLAUDE.md / WORK_LOG.md / DEFERRED_LEDGER.md | 3 sub-commit; .continue-here.md 中段创建 (CLAUDE.md commit 后) + T6 完成后 cleanup; GOVERNANCE-03 通过 verifier 评估 trigger gate 完整度 |
| T7 — PROJECT/ROADMAP/REVIEW_POLICY milestone close cascade | PASS | `<T7_HASH>` | 3 文件 single commit | 8 grep PASS; PROJECT §Active flip + ROADMAP 5 SC ✅ + Coverage 10 REQ-IDs ✅ + REVIEW_POLICY §3.5 modality table + §2.12 trigger + Lesson 1 entry 4 |
| T8 — SUMMARY.md (close-phase cascade) | PASS | `<T8_HASH>` | this file | mirror Phase 4.0 / 4.1 SUMMARY 形态 |

## Total Commits Produced

9-10 atomic commits (T1-T8 + T6 3 sub-commit) — close-phase cascade 性质 T7 single commit (3 文件).

## Phase Gate Composite Check

```
✓ SC#1 PLAN.md 反映 audit 决策方向 (5+3 切分 + grid-cli/server 优先 + 双轴主框架)
✓ SC#2 ADR-V2-024 Decision/Alternatives/Enforcement 段填实 + F1-F4 lint 全 PASS
✓ SC#3 ADR-V2-024 Accepted (status + accepted_at + supersedes V2-023)
✓ SC#4 PROJECT.md §Active flip + §Validated 引用 ADR-V2-024 commit hash
✓ SC#5 Milestone 关闭检查点: CLEANUP-01..04 + DECIDE-01..03 + GOVERNANCE-01..03 全 ✅; debt water-line 无新增 P0/P1-active 项

✓ audit-fidelity-grep modality 实证应用 (T1+T2+T3 8+8+8 grep PASS)
✓ Path B fallback 实证 (slash command unavailable → manual frontmatter; Phase 4.1 T5 deviation precedent 应用)
✓ GOVERNANCE-03 trigger gate 闭环 (Phase 4.1 deferred → Phase 4.2 T6 创建+cleanup)
✓ REVIEW_POLICY §3.5 + §2.12 + §7 Lesson 1 entry 4 落地 (Phase 4.1 经验扩展)

── Phase 4.2 gate PASS ──
── Milestone "Phase 4 — Product Scope Decision" CLOSED ──
```

## ROADMAP §Phase 4.2 Success Criteria Cross-References

1. **SC#1 ✓**: `.planning/phases/04.2-decide-document/04.2-01-PLAN.md` (本 plan) 8 task 反映 audit 决策方向
2. **SC#2 ✓**: ADR-V2-024 Decision (T1) + Alternatives (T2) + Enforcement (T3) + F1-F4 lint pass (T5)
3. **SC#3 ✓**: ADR-V2-024 status: Accepted (T5 Path B fallback)
4. **SC#4 ✓**: PROJECT.md §Active flip + §Validated 引用 ADR-V2-024 commit (T7)
5. **SC#5 ✓**: ROADMAP Coverage 10 REQ-IDs 全 ✅ + REVIEW_POLICY §7 Lesson 1 entry 4 (T7)

## Deviations from Plan

[由 executor 在 commit 时填; 预期: T5 Path B fallback (期望 deviation, 非 surprise — Phase 4.1 T5 deviation 教训应用), 其他 T 期望无 deviation]

## Self-Check: PASSED

[由 executor 在 commit 时填具体 file 存在性 + commit 存在性 检查]

**Phase 4.2 ✅ COMPLETE** — 8/8 task PASS + 0 SKIPPED; 9-10 atomic commits.
**Milestone "Phase 4 — Product Scope Decision" ✅ CLOSED.**
```

`<TX_HASH>` 在 commit 时由 executor 用 git log 取实际 hash 替换. `2026-04-XX` 替换为 commit 当天.

### Step 8.5 — T7 hash backfill (REVISION per cross-AI MEDIUM#2)

T7 commit (PROJECT.md / ROADMAP.md / REVIEW_POLICY.md) 在前一个 commit 内留下 `<T7_HASH>` placeholder (per ROADMAP SC#4 + REVIEW_POLICY §7 Lesson 1 entry 4 引用 T7 commit hash). T8 task 同时:

1. 取 T7 实际 hash:
   ```bash
   T7_HASH=$(git log -1 --format=%h -- .planning/PROJECT.md .planning/ROADMAP.md .planning/REVIEW_POLICY.md | head -1)
   echo "T7 commit hash: $T7_HASH"
   ```

2. 在 ROADMAP §Phase 4.2 SC#4 + REVIEW_POLICY §7 Lesson 1 entry 4 内替换 `<T7_HASH>` placeholder:
   ```bash
   sed -i '' "s/<T7_HASH>/$T7_HASH/g" .planning/ROADMAP.md .planning/REVIEW_POLICY.md
   ```

3. 把 ROADMAP + REVIEW_POLICY backfill 与 SUMMARY.md 一起放入同一 T8 commit (atomic at task level), commit subject = `docs(planning): Phase 4.2 SUMMARY + T7 hash backfill`

### Step 8.6 — Semantic verification checklist (REVISION per cross-AI MEDIUM#5: audit-fidelity-grep validates presence not reasoning, 加 4 项 semantic check)

Phase 4.2 close-phase 前执行 4 项 semantic 自查 (人工读 + grep cross-check), 结果落到 SUMMARY.md §Semantic Check 段:

| # | Check | 验证方法 | Verdict |
|---|-------|---------|--------|
| 1 | 任何 active doc 都不再用 "Leg A/B" 作为顶层 framing (only inline historical anchor 含 "see ADR-V2-024") | `git grep 'Leg [AB]' -- ':!docs/design/EAASP/adrs/ADR-V2-023*' ':!.planning/phases/04.[01]-*' ':!.planning/phases/04.2-*' ':!docs/design/EAASP/adrs/decisions/'` 输出每行必含 "see ADR-V2-024" | ✓/✗ |
| 2 | ADR-V2-024 Decision 段 + Alternatives 段 不存在与 Open Items §6 backfill 矛盾的内容 | 人工对比 ADR-V2-024 §Decision §Alternatives vs audit doc §6 5 行 backfill, 标注矛盾点 (期望 0) | ✓/✗ |
| 3 | V2-023 在仓库内 discoverably superseded — `git grep -l 'ADR-V2-023'` 查到的 active doc 引用至少 50% 含 "Superseded by ADR-V2-024" 或 "supersede" 措辞 | `git grep -l 'ADR-V2-023' -- ':!docs/design/EAASP/adrs/ADR-V2-023*' ':!.planning/phases/04.[01]-*'` then 人工抽检 | ✓/✗ |
| 4 | GOVERNANCE-03 状态在 SUMMARY §T6 + ROADMAP Coverage table 一致 (PASS / PARTIAL / DEFERRED) — 不允许 SUMMARY 标 PASS 但 Coverage 标 ⚪/△ | `grep -E 'GOVERNANCE-03.*(PASS\|PARTIAL\|DEFERRED)' SUMMARY.md ROADMAP.md` 结果 string 一致 | ✓/✗ |

如任一 ✗, 走相应修正 (回 T1-T7) 或在 SUMMARY § Deviations 段显式记录 deviation + deferred D-item.

### Commit

`docs(planning): Phase 4.2 SUMMARY + T7 hash backfill`
  </action>

  <acceptance_criteria>
    1. **SUMMARY.md 文件存在**:
       ```bash
       test -f .planning/phases/04.2-decide-document/04.2-01-SUMMARY.md
       ```

    2. **frontmatter 含 8 个 metric 字段** (phase / plan / subsystem / tags / dependency_graph / tech_stack / key_files / decisions / metrics):
       ```bash
       awk '/^---$/{c++; if(c==2) exit} c==1' .planning/phases/04.2-decide-document/04.2-01-SUMMARY.md | grep -cE '^(phase|plan|subsystem|tags|dependency_graph|tech_stack|key_files|decisions|metrics):' | xargs test 9 -le
       ```

    3. **Task Outcomes table 含 8 row** (T1-T8):
       ```bash
       awk '/^## Task Outcomes/,/^## Total Commits/' .planning/phases/04.2-decide-document/04.2-01-SUMMARY.md | grep -cE '^\| T[0-9]' | xargs test 8 -eq
       ```

    4. **Phase Gate Composite 含 5 SC ✓ + 4 extra anchor**:
       ```bash
       awk '/^## Phase Gate/,/^## ROADMAP/' .planning/phases/04.2-decide-document/04.2-01-SUMMARY.md | grep -cE '^✓ SC#' | xargs test 5 -eq
       awk '/^## Phase Gate/,/^## ROADMAP/' .planning/phases/04.2-decide-document/04.2-01-SUMMARY.md | grep -F 'audit-fidelity-grep'
       awk '/^## Phase Gate/,/^## ROADMAP/' .planning/phases/04.2-decide-document/04.2-01-SUMMARY.md | grep -F 'Path B fallback'
       awk '/^## Phase Gate/,/^## ROADMAP/' .planning/phases/04.2-decide-document/04.2-01-SUMMARY.md | grep -F 'GOVERNANCE-03 trigger gate'
       awk '/^## Phase Gate/,/^## ROADMAP/' .planning/phases/04.2-decide-document/04.2-01-SUMMARY.md | grep -F 'REVIEW_POLICY §3.5'
       ```

    5. **ROADMAP §Phase 4.2 SC Cross-References 段含 5 row**:
       ```bash
       awk '/^## ROADMAP/,/^## Deviations/' .planning/phases/04.2-decide-document/04.2-01-SUMMARY.md | grep -cE '^[0-9]+\. \*\*SC#' | xargs test 5 -eq
       ```

    6. **closing line 含 "Milestone .* CLOSED"**:
       ```bash
       tail -3 .planning/phases/04.2-decide-document/04.2-01-SUMMARY.md | grep -F 'Milestone' | grep -F 'CLOSED'
       ```

    7. **closing line 含 "Phase 4.2 ✅ COMPLETE"**:
       ```bash
       tail -3 .planning/phases/04.2-decide-document/04.2-01-SUMMARY.md | grep -F 'Phase 4.2 ✅ COMPLETE'
       ```

    8. **Deviations 段存在** (可空但 section header 必有):
       ```bash
       grep -c '^## Deviations from Plan' .planning/phases/04.2-decide-document/04.2-01-SUMMARY.md | xargs test 1 -eq
       ```

    全部 8 项 PASS = T8 完成 = Phase 4.2 完整 close.
  </acceptance_criteria>

  <verify>
    <automated>bash -c 'set -e; SUM=.planning/phases/04.2-decide-document/04.2-01-SUMMARY.md; test -f $SUM && [ $(awk "/^---\$/{c++; if(c==2) exit} c==1" $SUM | grep -cE "^(phase|plan|subsystem|tags|dependency_graph|tech_stack|key_files|decisions|metrics):") -ge 9 ] && [ $(awk "/^## Task Outcomes/,/^## Total Commits/" $SUM | grep -cE "^\| T[0-9]") -eq 8 ] && [ $(awk "/^## Phase Gate/,/^## ROADMAP/" $SUM | grep -cE "^✓ SC#") -eq 5 ] && tail -3 $SUM | grep -qF "Phase 4.2 ✅ COMPLETE" && grep -q "^## Deviations from Plan" $SUM && echo T8 ALL CORE GREP PASS'</automated>
  </verify>

  <done>
    - SUMMARY.md 文件创建 (≈250+ LOC)
    - frontmatter 9 字段 (phase / plan / subsystem / tags / dependency_graph / tech_stack / key_files / decisions / metrics)
    - Task Outcomes table 8 row T1-T8 + commit hash + acceptance 摘要
    - Phase Gate Composite 5 SC ✓ + 4 extra anchor (audit-fidelity-grep / Path B fallback / GOVERNANCE-03 / REVIEW_POLICY)
    - ROADMAP §Phase 4.2 5 SC Cross-References 段
    - Deviations 段 (可空, header 必有)
    - closing line "Phase 4.2 ✅ COMPLETE" + "Milestone CLOSED"
    - 8 acceptance criterion 全 PASS
    - Footer Generated-By + Co-Authored-By
  </done>
</task>

</tasks>

---

## Verification (post-execution by gsd-verifier)

After all 8 tasks complete, gsd-verifier reads ROADMAP §Phase 4.2 5 SC + 本 PLAN must_haves + git log T1-T8 commit hash + 各 task acceptance criterion 实际 grep 输出, 产出 VERIFICATION.md 含:

- 8 task verdict (PASS / PARTIAL / FAIL) + grep 输出引用
- 5 SC verdict (per ROADMAP) + commit hash cross-ref
- GOVERNANCE-03 trigger gate 完整度评估 (Phase 4.1 SC#4 闭环判据)
- Deviations from PLAN (期望: T5 Path B fallback; 其他 T 期望无)
- must_haves frontmatter 内 6 truths 各自验证状态
- Phase Gate composite (Phase 4.2 PASS / FAIL)
- Milestone close 状态 (per Phase 4.2 SC#5 — CLEANUP/DECIDE/GOVERNANCE 全 ✅)

如 verifier 发现 issue, 走 superpowers two-stage / inline self-check (per REVIEW_POLICY §2.12 + §3.5) 修正回提.

---

*Phase: 04.2-decide-document*
*Plan: 01 (single plan, 8 task atomic-commit-per-task; T6 内 3 sub-commit + GOVERNANCE-03 trigger gate)*
*Authored: 2026-04-27 by gsd-planner (T1+T2) + Claude Code orchestrator inline (T3-T8 due to API socket failures during planner Task — strict format mirror)*

