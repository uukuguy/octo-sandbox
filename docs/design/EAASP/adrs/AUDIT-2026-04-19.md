# ADR Audit Report — 2026-04-19

Inaugural audit after ADR governance plugin deployment (ADR-V2-022). This report is generated after the initial frontmatter backfill wave for ADR-V2-001 through ADR-V2-022 (14 ADRs total).

## Summary

Total ADRs: 14

Type distribution:

- **contract**: 8 (ADR-V2-001, 002, 003, 005, 006, 018, 020, 021)
- **strategy**: 5 (ADR-V2-015, 016, 017, 019, 022)
- **record**: 1 (ADR-V2-004) — candidate for `/adr:downgrade`

Status distribution:

- **Accepted**: 12 (including V2-004 which is Accepted/Deferred)
- **Proposed**: 2 (ADR-V2-019, ADR-V2-021)

## Per-ADR classification

| ID | Title | Type | Status | Enforcement | Concerns |
|---|---|---|---|---|---|
| ADR-V2-001 | EmitEvent 接口形式：混合拦截器 + OPTIONAL RPC | contract | Accepted | contract-test (no trace) | WARN: missing `trace`; no grep hit for L1→L4 event ingest test. Need proto round-trip test + interceptor coverage in `tests/contract/`. |
| ADR-V2-002 | Session Event Stream 后端选型：SQLite WAL + 接口抽象 | contract | Accepted | contract-test (no trace) | WARN: missing `trace`; `EventStreamBackend` Protocol has no conformance test. `SqliteWalBackend` unit tests exist in `tools/eaasp-l4-orchestration/` but not wired as contract test. |
| ADR-V2-003 | Event Clustering 插件化接口：4 Handler Pipeline | contract | Accepted | contract-test (no trace) | WARN: missing `trace`; `EventHandler` Protocol + `EventEngine` pipeline has no cross-runtime contract test. |
| ADR-V2-004 | L4→L1 Real gRPC Binding（延迟到 Phase 1） | record | Accepted/Deferred | review-only | DOWNGRADE CANDIDATE: phase-specific S4.T2 4b-lite implementation record. Should be moved to `docs/plans/completed/` via `/adr:downgrade ADR-V2-004 --to plan`. Decision requires user approval. |
| ADR-V2-005 | Session 级工具容器隔离（Tool Sandbox Container） | contract | Accepted | contract-test (no trace) | WARN: missing `trace`; tool-sandbox container MCP-over-SSE bridge has no contract test fixture. |
| ADR-V2-006 | Hook Envelope Contract (PreToolUse / PostToolUse / Stop) | contract | Accepted | contract-test (no trace) | WARN: missing `trace`; §10 test vectors are normative but not wired as contract-test matrix. Should reference `tests/contract/fixtures/hook_probe.py` + per-runtime envelope parity tests. |
| ADR-V2-015 | L2 Memory Engine Semantic 检索选型 | strategy | Accepted | strategic | OK: strategic choice with HNSW-vs-pgvector decision matrix. Self-review via ADR body. |
| ADR-V2-016 | Agent Loop 通用性原则 | strategy | Accepted | strategic | GOOD: has a real regression test (`crates/grid-engine/tests/d87_multi_step_workflow_regression.rs`) bound to the principle. |
| ADR-V2-017 | L1 Runtime 生态策略（三轨） | strategy | Accepted | strategic | OK: broad architectural strategy; mentions shared `tests/contract/` suite as the implied enforcement vector. |
| ADR-V2-018 | PreCompact Hook 协议 + 迭代式 Summary 复用 + 跨压缩 Token 预算 | contract | Accepted | contract-test (no trace) | WARN: missing `trace`; proto schema changes and hook event are defined but no listed contract-test file. |
| ADR-V2-019 | L1 Runtime Deployment Model | strategy | Proposed | strategic | OK: deployment-mode policy. Note: Status Proposed — should graduate to Accepted once D142/D143 close. |
| ADR-V2-020 | 工具命名空间契约（L0/L1/L2 分层） | contract | Accepted | contract-test (3 tests traced) | GOOD: 3 contract tests real and referenced — `tests/contract/cases/test_tool_namespace_enforcement.py`, `test_tool_conflict_resolution.py`, `test_pre_phase3_skill_compat.py` (all exist on disk). |
| ADR-V2-021 | SendResponse.chunk_type 契约冻结（统一枚举） | contract | Proposed | contract-test (trace declared) | WARN: `tests/contract/cases/test_chunk_type_contract.py` and `.github/workflows/phase3-contract.yml` listed in frontmatter but do NOT yet exist on disk. This is expected — the ADR is Proposed and the test is implementation work for Phase 3.5. File follow-up to land both before `/adr:accept`. |
| ADR-V2-022 | ADR 生命周期治理机制 | strategy | Accepted | strategic | GOOD: meta-governance, enforcement is the ADR governance plugin itself; paths live under `~/.claude/skills/adr-governance/`. |

## Downgrade candidates (record type)

- **ADR-V2-004** — "L4→L1 Real gRPC Binding（延迟到 Phase 1）". This ADR is explicitly phase-specific (Phase 0 MVP S4.T2 收尾) and documents an implementation decision ("4b-lite" mode) rather than a lasting architectural contract or strategy. It details file-by-file implementation scope, precise test locations, and a task breakdown (D54.a through D54.e) that reads as an implementation plan. Recommended action: `/adr:downgrade ADR-V2-004 --to plan` after user confirmation.

No other record-type candidates identified. ADR-V2-019 is arguably phase-specific (Phase 2.5 W1.T2.5 Dockerfile) but articulates deployment-model invariants (D1-D5) that apply to all L1 runtimes long-term — correctly classified as strategy.

## Missing contract traces

The following contract-type ADRs have empty or non-existent `enforcement.trace`. Each should have a follow-up task to wire a contract test into `tests/contract/` or `.github/workflows/`:

1. **ADR-V2-001** — EmitEvent interceptor + RPC dual-source; need a contract test that asserts both paths produce equivalent `PRE_TOOL_USE` / `POST_TOOL_USE` / `STOP` events and Deduplicator collapses duplicates.
2. **ADR-V2-002** — `EventStreamBackend` Protocol conformance; need a test that any backend implementation passes `append → list_events → subscribe → search → count` round-trip.
3. **ADR-V2-003** — Event Clustering 4-Handler Pipeline; need a contract test that pipeline runs all four handlers in order and Deduplicator returns None for replays.
4. **ADR-V2-005** — Tool sandbox SSE bridge; need a contract test verifying a runtime can connect to tool-sandbox via SSE and invoke a tool end-to-end.
5. **ADR-V2-006** — Hook envelope contract; need test vectors from §10 wired as per-runtime matrix. `tests/contract/fixtures/hook_probe.py` exists in session memory logs but is not listed in ADR trace. This is the highest-priority backfill given hook is already in production.
6. **ADR-V2-018** — PreCompact hook protocol; need a contract test that proto schema round-trips and `PreCompactHook` event shape is stable across runtimes that implement compaction.
7. **ADR-V2-021** — chunk_type enum; listed trace `tests/contract/cases/test_chunk_type_contract.py` does not yet exist (ADR is Proposed; test is implementation work).

## Conflicts detected

None. Manual inspection of `affected_modules` across 14 ADRs did not find two Accepted ADRs with contradictory decisions covering the same module. Observed overlaps that are complementary (not conflicting):

- ADR-V2-001, V2-002, V2-003 all touch `tools/eaasp-l4-orchestration/` — describe different layers (interface / backend / pipeline) of the same Event Engine, composable.
- ADR-V2-017 and V2-019 both touch `crates/grid-runtime/` and `lang/*-runtime-*` — V2-017 scopes which runtimes exist; V2-019 scopes how each runtime is deployed. Orthogonal.
- ADR-V2-006 and V2-018 both define hook protocols — V2-006 is PreTool/PostTool/Stop, V2-018 is PreCompact. Different oneof branches in `hook.proto`.
- ADR-V2-020 and V2-006 both touch `tools/eaasp-skill-registry/` and hook scope — V2-020 is about tool routing, V2-006 is about hook envelope. Orthogonal.

## Next steps

1. User reviews this audit and decides on ADR-V2-004 downgrade.
2. For each of the 7 contract-type ADRs missing `trace`, file a follow-up task to add contract tests into `tests/contract/cases/` and update the ADR `enforcement.trace` field (non-breaking change).
3. ADR-V2-019 and ADR-V2-021 should graduate from Proposed to Accepted via `/adr:accept` once their verification criteria land (D142/D143 for V2-019; chunk_type unification PR for V2-021).
4. Re-run `/adr:audit` after resolutions and compare trace-completion delta.

## Audit metadata

- Audit date: 2026-04-19
- Auditor: Agent D (coder agent, swarm multi-agent orchestration)
- Frontmatter backfill wave: initial (14/14 ADRs)
- Project config: `.adr-config.yaml` at project root (inaugural version)
- Tools expected but not yet verified on disk: `~/.claude/skills/adr-governance/scripts/adr_frontmatter.py` (parser) — will be tested once plugin install completes
