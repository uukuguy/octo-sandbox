# octo-workbench v1.0 发布冲刺实施计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**目标：** 修复所有已知 P0/P1 bug，完整实现三模式交互能力，完善可观测性，使 octo-workbench 达到企业级自主智能体可发布标准。

**架构：** 以 AgentRuntime → AgentExecutor → AgentLoop 为主线，Phase A 修复稳定性缺陷，Phase B 完善后端能力，Phase C 实现前端控制台，Phase D 集成验收。

**Tech Stack：** Rust (tokio, axum, SQLite), React 18 (TypeScript, Vite, Jotai, TailwindCSS), WebSocket

---

## 当前代码基准（执行前必读）

| 文件 | 关键状态 |
|------|----------|
| `crates/octo-engine/src/agent/runtime.rs` | `stop_primary()` 发 Cancel 消息（未 drop tx）；`build_tool_registry()` 每次创建独立 Arc |
| `crates/octo-engine/src/agent/executor.rs` | `tools: Arc<ToolRegistry>`（快照，非共享引用）；Cancel 只设 flag |
| `crates/octo-engine/src/agent/loop_.rs` | 已有 `RetryPolicy`（S6 已实现）；缺 `LoopTurnStarted` 事件 |
| `crates/octo-engine/src/scheduler/mod.rs` | `run_now()` 硬编码 "Manually triggered" 从不真实执行 |
| `crates/octo-server/src/main.rs` | `shutdown_signal()` 只捕获 CTRL+C，未调用 `mcp_manager.shutdown_all()` |
| `crates/octo-server/src/state.rs` | `agent_handle: AgentExecutorHandle`（非 Option，固定单 handle） |
| `crates/octo-server/src/api/mod.rs` | 已有 metrics、scheduler、mcp_servers 等路由；无 /api/tasks、/health 增强版 |

---

## Phase A：稳定地基（目标：~3天）

### Task A1：修复 stop_primary（drop tx 语义）

**目标：** `stop_primary()` 真正终止 AgentExecutor，而不仅仅发 Cancel 消息。

**文件：**
- 修改: `crates/octo-engine/src/agent/runtime.rs:542-551`
- 修改: `crates/octo-server/src/state.rs`

**当前代码（runtime.rs:542-551）：**
```rust
pub async fn stop_primary(&self) {
    let handle = {
        let mut guard = self.primary_handle.lock().await;
        guard.take()
    };
    if let Some(h) = handle {
        let _ = h.send(AgentMessage::Cancel).await;  // ← 问题：仅发消息，Executor 继续等待
        info!("Primary AgentExecutor stopped");
    }
}
```

**Step 1：写失败测试**

```rust
// crates/octo-engine/tests/test_stop_primary.rs
#[tokio::test]
async fn test_stop_primary_terminates_executor() {
    // 启动 executor，发 stop，确认 task 结束
    // 验证：stop 后 rx.recv() 返回 None，tokio task 自然结束
}
```

**Step 2：运行测试确认失败**
```bash
cargo test test_stop_primary -p octo-engine -- --nocapture
```
期望：编译通过，测试 FAIL（或 hang）

**Step 3：修复 runtime.rs - stop_primary 只需 drop handle**

修改 `runtime.rs:542-551`，将发消息改为直接 drop（注释掉 send 行）：

```rust
pub async fn stop_primary(&self) {
    let _dropped_handle = {
        let mut guard = self.primary_handle.lock().await;
        guard.take()
    };
    // AgentExecutorHandle 被 drop → tx 被 drop → rx.recv() 返回 None
    // → AgentExecutor while 循环自然退出 → tokio task 结束
    if _dropped_handle.is_some() {
        info!("Primary AgentExecutor stopped (tx dropped)");
    }
}
```

**Step 4：运行测试确认通过**
```bash
cargo test test_stop_primary -p octo-engine -- --nocapture
```
期望：PASS

**Step 5：验证编译**
```bash
cargo check --workspace
```
期望：零错误

**Step 6：提交**
```bash
git add crates/octo-engine/src/agent/runtime.rs crates/octo-engine/tests/
git commit -m "fix(S1): stop_primary drops tx to naturally terminate AgentExecutor"
```

---

### Task A2：ToolRegistry 版本化共享引用（MCP 热插拔生效）

**目标：** AgentExecutor 持有 `Arc<StdMutex<ToolRegistry>>` 共享引用，AgentLoop 每 round 前生成快照。

**文件：**
- 修改: `crates/octo-engine/src/agent/executor.rs:67`（tools 类型）
- 修改: `crates/octo-engine/src/agent/executor.rs:88-125`（构造器参数）
- 修改: `crates/octo-engine/src/agent/executor.rs:141-145`（传给 AgentLoop）
- 修改: `crates/octo-engine/src/agent/runtime.rs:491`（start_primary 调用）
- 修改: `crates/octo-engine/src/agent/runtime.rs:502-519`（AgentExecutor::new 调用）

**当前代码（executor.rs:67）：**
```rust
tools: Arc<ToolRegistry>,  // ← 快照，与 runtime.tools 解耦
```

**Step 1：写失败测试**

```rust
// crates/octo-engine/tests/test_tool_hot_reload.rs
#[tokio::test]
async fn test_mcp_tool_visible_after_registration() {
    // 1. 启动 AgentExecutor
    // 2. 在 runtime 添加 MCP 工具
    // 3. 发消息触发新 round
    // 4. 验证 AgentLoop 能看到新工具
}
```

**Step 2：运行测试确认失败**
```bash
cargo test test_mcp_tool_visible -p octo-engine -- --nocapture
```

**Step 3：修改 executor.rs - tools 类型改为 Arc<StdMutex<ToolRegistry>>**

```rust
// executor.rs:67
tools: Arc<std::sync::Mutex<crate::tools::ToolRegistry>>,
```

修改构造器参数：
```rust
// executor.rs:95-96
tools: Arc<std::sync::Mutex<crate::tools::ToolRegistry>>,
```

修改 AgentLoop 构建处（executor.rs:141-145）：
```rust
let tools_snapshot = {
    let guard = self.tools.lock().unwrap();
    let mut registry = crate::tools::ToolRegistry::new();
    for (name, tool) in guard.iter() {
        registry.register_arc(name.clone(), tool);
    }
    Arc::new(registry)
};
let mut agent_loop = AgentLoop::new(
    self.provider.clone(),
    tools_snapshot,  // ← 每 round 从共享引用生成快照
    self.memory.clone(),
);
```

**Step 4：修改 runtime.rs - start_primary 传共享引用而非快照**

修改 `runtime.rs:491`：
```rust
// 旧：let (tools, system_prompt, model, config) = self.resolve_runtime_config(agent_id);
// 新：直接使用 self.tools（共享引用）
let (_, system_prompt, model, config) = self.resolve_runtime_config(agent_id);
let tools_ref = Arc::clone(&self.tools);  // 共享引用
```

修改 `runtime.rs:502-519` 中 `AgentExecutor::new(...)` 的 `tools` 参数：
```rust
// tools 参数改为 tools_ref
```

**Step 5：运行测试确认通过**
```bash
cargo test test_mcp_tool_visible -p octo-engine -- --nocapture
cargo check --workspace
```
期望：PASS，零编译错误

**Step 6：提交**
```bash
git add crates/octo-engine/src/agent/executor.rs crates/octo-engine/src/agent/runtime.rs
git commit -m "fix(S2): AgentExecutor holds shared ToolRegistry ref for MCP hot-reload"
```

---

### Task A3：修复 Scheduler run_now 真实执行

**目标：** `run_now()` 调用 `execute_scheduled_task()` 执行真实 Agent 任务。

**文件：**
- 修改: `crates/octo-engine/src/scheduler/mod.rs:524-562`

**当前代码（mod.rs:543-551）：**
```rust
let execution = TaskExecution {
    status: ExecutionStatus::Success,
    result: Some("Manually triggered".to_string()),  // ← 假执行
    ..
};
```

**Step 1：了解 execute_scheduled_task 签名**

阅读 `scheduler/mod.rs` 中 `execute_scheduled_task` 方法签名（应接收 `ScheduledTask` 参数）。

**Step 2：写失败测试**

```rust
// crates/octo-engine/tests/test_scheduler_run_now.rs
#[tokio::test]
async fn test_run_now_actually_executes() {
    // 创建任务
    // 调用 run_now()
    // 验证 execution.result 不等于 "Manually triggered"
    // 验证 execution.status 是真实执行结果
}
```

**Step 3：修改 run_now()，调用 execute_scheduled_task()**

修改 `scheduler/mod.rs:524-562`：

```rust
pub async fn run_now(
    &self,
    task_id: &str,
    user_id: Option<&str>,
) -> Result<TaskExecution, SchedulerError> {
    let task = self
        .storage
        .get_task(task_id)
        .await?
        .ok_or_else(|| SchedulerError::TaskNotFound(task_id.to_string()))?;

    // Check user ownership
    if let (Some(req_user), Some(task_user)) = (user_id, &task.user_id) {
        if req_user != task_user {
            return Err(SchedulerError::TaskNotFound(task_id.to_string()));
        }
    }

    // 真实执行
    let execution = self.execute_scheduled_task(&task).await?;

    // Update last_run timing
    let now = Utc::now();
    self.storage
        .update_timing(task_id, Some(now), task.next_run)
        .await?;

    Ok(execution)
}
```

**Step 4：运行测试和编译验证**
```bash
cargo test test_scheduler_run_now -p octo-engine -- --nocapture
cargo check --workspace
```

**Step 5：提交**
```bash
git add crates/octo-engine/src/scheduler/mod.rs
git commit -m "fix(S3): scheduler run_now calls execute_scheduled_task for real execution"
```

---

### Task A4：WorkingMemory per-session 隔离

**目标：** 每个 AgentExecutor session 使用独立 WorkingMemory 实例，消除全局状态污染。

**文件：**
- 修改: `crates/octo-engine/src/agent/runtime.rs`（start_primary 中 memory 参数）

**当前代码（runtime.rs:510-511）：**
```rust
self.memory.clone(),  // ← 全局共享实例，所有 session 混用
```

**Step 1：写失败测试**

```rust
// crates/octo-engine/tests/test_working_memory_isolation.rs
#[tokio::test]
async fn test_sessions_have_isolated_working_memory() {
    // 启动两个 AgentExecutor
    // 在 session A 写入内存
    // 验证 session B 看不到该内容
}
```

**Step 2：修改 start_primary 创建独立 InMemoryWorkingMemory**

在 `runtime.rs` 中找到 `start_primary` 的 `AgentExecutor::new(...)` 调用，修改 `memory` 参数：

```rust
// 改为每 session 独立实例
let session_memory: Arc<dyn WorkingMemory> =
    Arc::new(crate::memory::InMemoryWorkingMemory::new());

let runtime = AgentExecutor::new(
    session_id.clone(),
    user_id,
    sandbox_id,
    initial_history,
    rx,
    broadcast_tx,
    self.provider.clone(),
    tools,
    session_memory,  // ← 独立实例，非 self.memory.clone()
    Some(self.memory_store.clone()),
    Some(model),
    Some(self.session_store.clone()),
    system_prompt,
    config,
    self.working_dir.clone(),
    self.event_bus.clone(),
);
```

需要确认 `InMemoryWorkingMemory::new()` 是否 pub，若不是则改为 pub。

**Step 3：运行测试和编译验证**
```bash
cargo test test_working_memory_isolation -p octo-engine -- --nocapture
cargo check --workspace
```

**Step 4：提交**
```bash
git add crates/octo-engine/src/agent/runtime.rs crates/octo-engine/src/memory/
git commit -m "fix(S4): create per-session InMemoryWorkingMemory to prevent data pollution"
```

---

### Task A5：优雅关机（MCP shutdown_all）

**目标：** 服务器 CTRL+C 时，调用 `McpManager.shutdown_all()` 清理所有 MCP 子进程。

**文件：**
- 修改: `crates/octo-server/src/main.rs:225-229`（graceful shutdown）

**当前代码（main.rs:232-237）：**
```rust
async fn shutdown_signal() {
    tokio::signal::ctrl_c()
        .await
        .expect("failed to install CTRL+C signal handler");
    tracing::info!("shutdown signal received");
    // ← 未清理 MCP 子进程
}
```

**Step 1：修改 main.rs - 在 serve 后调用 shutdown**

将 `shutdown_signal` 方式替换为在 `axum::serve` 后主动清理：

```rust
// 在 main.rs 中：
axum::serve(listener, app)
    .with_graceful_shutdown(shutdown_signal())
    .await?;

// Graceful shutdown：清理 MCP 子进程
tracing::info!("Shutting down MCP servers...");
if let Some(mcp) = state.agent_supervisor.mcp_manager() {
    let mut guard = mcp.lock().await;
    guard.shutdown_all().await;
    tracing::info!("MCP servers shut down");
}

Ok(())
```

注意：需要将 `state` 移出 `AppState::new()` 之前保存引用，或者通过 Arc 保持。

**Step 2：编译验证**
```bash
cargo check --workspace
```

**Step 3：提交**
```bash
git add crates/octo-server/src/main.rs
git commit -m "fix(S5): call McpManager.shutdown_all() on server graceful shutdown"
```

---

### Task A6：确认 Provider 重试已实现

**目标：** 确认 RetryPolicy 已在 loop_.rs 中正确使用，无需额外修改。

**Step 1：阅读并验证**

检查 `crates/octo-engine/src/agent/loop_.rs:271-307`：
- `RetryPolicy::default()` 已被使用（max_retries=3, base_delay=1s, backoff_factor=2.0）
- `should_retry_str()` 判断是否重试
- `delay_for()` 计算指数退避延迟

**Step 2：运行现有重试测试**
```bash
cargo test test_retry -p octo-engine -- --nocapture
```
期望：PASS（retry.rs 中已有全套单元测试）

**Step 3：提交（记录状态，无代码改动）**
```bash
git commit --allow-empty -m "chore(S6): verify provider retry already implemented via RetryPolicy"
```

---

### Phase A 验收

```bash
# 全量测试
cargo test --workspace

# 手动验证 MCP 热插拔：
# 1. 启动服务器
# 2. 通过 API 添加 MCP server
# 3. 发送聊天消息，确认新工具可被调用
# 期望：无需重启，工具即时生效
```

---

## Phase B：后端能力（目标：~4天）

### Task B1：并行工具执行（enable_parallel 生效）

**目标：** `AgentConfig.enable_parallel=true` 时，多工具并发执行（Semaphore 限流）。

**文件：**
- 修改: `crates/octo-engine/src/agent/loop_.rs`（工具执行段）
- 参考: `crates/octo-engine/src/agent/parallel.rs`（已存在）

**Step 1：查看 parallel.rs 现有实现**
```bash
cat crates/octo-engine/src/agent/parallel.rs
```
确认 `execute_parallel` 函数签名。

**Step 2：写失败测试**

```rust
#[tokio::test]
async fn test_parallel_tools_execute_concurrently() {
    // 创建带 enable_parallel=true 的 AgentConfig
    // 模拟两个工具调用，每个耗时 100ms
    // 验证总耗时 < 150ms（并行），而非 > 200ms（串行）
}
```

**Step 3：修改 loop_.rs 工具执行段**

在 loop_.rs 工具执行处（`// Execute tools and build tool result messages` 之后），添加并行分支：

```rust
let tool_result_contents: Vec<ContentBlock> = if self.config.enable_parallel && tool_uses.len() > 1 {
    // 并行执行
    execute_parallel(
        &self.tools,
        &parsed_tools,
        self.config.max_parallel_tools,
        &mut self.loop_guard,
        &tool_ctx,
        tx.clone(),
        self.recorder.as_deref(),
    ).await?
} else {
    // 现有串行逻辑
    let mut results = Vec::new();
    for (tu, input) in &parsed_tools {
        // ... 现有逻辑 ...
    }
    results
};
```

**Step 4：运行测试**
```bash
cargo test test_parallel_tools -p octo-engine -- --nocapture
cargo check --workspace
```

**Step 5：提交**
```bash
git add crates/octo-engine/src/agent/loop_.rs
git commit -m "feat(C1): enable parallel tool execution via Semaphore when enable_parallel=true"
```

---

### Task B2：后台任务 API（POST/GET /api/tasks）

**目标：** 新增后台任务 REST API，客户端可提交、查询、取消异步任务。

**文件：**
- 新建: `crates/octo-server/src/api/tasks.rs`
- 修改: `crates/octo-server/src/api/mod.rs`（注册路由）

**Step 1：了解现有 scheduler API 结构**
```bash
cat crates/octo-server/src/api/scheduler.rs
```

**Step 2：创建 tasks.rs**

```rust
// crates/octo-server/src/api/tasks.rs
use axum::{
    extract::{Path, State},
    http::StatusCode,
    response::Json,
    routing::{delete, get, post},
    Router,
};
use serde::{Deserialize, Serialize};
use std::sync::Arc;

use crate::state::AppState;

#[derive(Debug, Deserialize)]
pub struct CreateTaskRequest {
    pub prompt: String,
    pub model: Option<String>,
}

#[derive(Debug, Serialize)]
pub struct TaskResponse {
    pub id: String,
    pub status: String,
    pub result: Option<String>,
    pub error: Option<String>,
    pub created_at: String,
    pub finished_at: Option<String>,
}

/// POST /api/tasks - 提交后台任务
pub async fn create_task(
    State(state): State<Arc<AppState>>,
    Json(req): Json<CreateTaskRequest>,
) -> Result<Json<TaskResponse>, StatusCode> {
    // 通过 scheduler 创建临时单次任务
    // 调用 execute_scheduled_task()
    // 返回 task ID 和初始状态
    todo!()
}

/// GET /api/tasks - 列出后台任务
pub async fn list_tasks(
    State(state): State<Arc<AppState>>,
) -> Json<Vec<TaskResponse>> {
    todo!()
}

/// GET /api/tasks/:id - 获取任务详情
pub async fn get_task(
    State(state): State<Arc<AppState>>,
    Path(id): Path<String>,
) -> Result<Json<TaskResponse>, StatusCode> {
    todo!()
}

/// DELETE /api/tasks/:id - 取消任务
pub async fn cancel_task(
    State(state): State<Arc<AppState>>,
    Path(id): Path<String>,
) -> StatusCode {
    todo!()
}

pub fn router() -> Router<Arc<AppState>> {
    Router::new()
        .route("/tasks", post(create_task).get(list_tasks))
        .route("/tasks/{id}", get(get_task).delete(cancel_task))
}
```

**Step 3：在 mod.rs 注册路由**

在 `api/mod.rs` 的 `routes()` 函数中添加：
```rust
.merge(tasks::router())
```
并在文件顶部添加 `pub mod tasks;`

**Step 4：实现 create_task - 调用 execute_scheduled_task**

在 `create_task` handler 中：
1. 创建 `AgentTaskConfig { prompt: req.prompt, model: req.model, .. }`
2. 创建临时 `ScheduledTask`（cron 为空，仅执行一次）
3. 调用 `state.agent_supervisor` 的 `execute_scheduled_task()`
4. 保存执行记录并返回 ID

**Step 5：curl 验证**
```bash
# 启动服务器后：
curl -X POST http://localhost:3001/api/tasks \
  -H "Content-Type: application/json" \
  -d '{"prompt": "列出当前目录文件", "model": null}'
# 期望：返回 {"id": "xxx", "status": "running"}

curl http://localhost:3001/api/tasks
# 期望：返回任务列表

curl http://localhost:3001/api/tasks/{id}
# 期望：返回任务详情和结果
```

**Step 6：提交**
```bash
git add crates/octo-server/src/api/tasks.rs crates/octo-server/src/api/mod.rs
git commit -m "feat(C3/B2): add POST/GET /api/tasks for background task submission"
```

---

### Task B3：增强 /health 端点

**目标：** `GET /health` 返回所有组件状态（provider、db、mcp_servers、uptime）。

**文件：**
- 修改: `crates/octo-server/src/router.rs:13-15`（health handler）
- 修改: `crates/octo-server/src/state.rs`（可选：添加 start_time 字段）

**当前 health handler：**
```rust
async fn health() -> &'static str {
    "ok"  // ← 过于简单
}
```

**Step 1：在 state.rs 添加 start_time**

```rust
pub struct AppState {
    // ... 现有字段 ...
    pub start_time: std::time::Instant,
}

impl AppState {
    pub fn new(...) -> Self {
        Self {
            // ...
            start_time: std::time::Instant::now(),
        }
    }
}
```

**Step 2：修改 router.rs health handler**

```rust
use axum::extract::State;
use serde::Serialize;
use std::sync::Arc;

#[derive(Serialize)]
struct HealthResponse {
    status: &'static str,
    uptime_secs: u64,
    provider: String,
    mcp_servers: Vec<McpServerStatus>,
    version: &'static str,
}

#[derive(Serialize)]
struct McpServerStatus {
    name: String,
    status: String,
}

async fn health(State(state): State<Arc<AppState>>) -> Json<HealthResponse> {
    let uptime = state.start_time.elapsed().as_secs();
    let mcp_servers = state.agent_supervisor
        .list_mcp_servers()
        .iter()
        .map(|s| McpServerStatus {
            name: s.name.clone(),
            status: format!("{:?}", s),
        })
        .collect();

    Json(HealthResponse {
        status: "ok",
        uptime_secs: uptime,
        provider: state.config.provider.name.clone(),
        mcp_servers,
        version: env!("CARGO_PKG_VERSION"),
    })
}
```

**Step 3：curl 验证**
```bash
curl http://localhost:3001/api/health
# 期望：{"status":"ok","uptime_secs":42,"provider":"anthropic","mcp_servers":[],"version":"0.1.0"}
```

**Step 4：提交**
```bash
git add crates/octo-server/src/router.rs crates/octo-server/src/state.rs
git commit -m "feat(O6/B3): enhance /api/health with component status and uptime"
```

---

### Task B4：补发 LoopTurnStarted 事件（修复 turns.total 指标）

**目标：** 在 `loop_.rs` 每 round 开始处发布 `LoopTurnStarted` 事件，使 `turns.total` 计数器正确递增。

**文件：**
- 修改: `crates/octo-engine/src/agent/loop_.rs:221`（for round 循环开始处）

**当前代码（loop_.rs:221）：**
```rust
for round in 0..max_rounds {
    debug!(round, "Agent round starting");
    // ← 未发布 LoopTurnStarted 事件
```

**Step 1：查看 EventBus 和现有事件类型**
```bash
grep -n "LoopTurn" crates/octo-engine/src/event/
grep -n "pub enum.*Event" crates/octo-engine/src/event/
```

**Step 2：在 round 开始处添加事件发布**

```rust
for round in 0..max_rounds {
    debug!(round, "Agent round starting");

    // 发布 LoopTurnStarted 事件（用于指标统计）
    if let Some(ref bus) = self.event_bus {
        let _ = bus.publish(crate::event::OctoEvent::LoopTurnStarted {
            session_id: session_id.clone(),
            round,
        });
    }
    // ...
```

**Step 3：若 OctoEvent 无 LoopTurnStarted 变体，先添加**

检查 `crates/octo-engine/src/event/mod.rs`，如需要添加：
```rust
LoopTurnStarted {
    session_id: SessionId,
    round: u32,
},
```

**Step 4：验证 MetricsRegistry 监听该事件**

检查 `MetricsRegistry` 是否订阅了 `LoopTurnStarted`，若没有则添加处理逻辑。

**Step 5：运行测试**
```bash
cargo test --workspace
curl http://localhost:3001/api/metrics
# 期望：turns.total > 0（发消息后）
```

**Step 6：提交**
```bash
git add crates/octo-engine/src/agent/loop_.rs crates/octo-engine/src/event/
git commit -m "fix(O4): publish LoopTurnStarted event per round to fix turns.total metric"
```

---

### Task B5：JSON 日志格式支持

**目标：** `OCTO_LOG_FORMAT=json` 时，以 JSON 格式输出日志（便于 ELK 等日志聚合）。

**文件：**
- 修改: `crates/octo-server/src/main.rs:73-78`（logging 初始化）

**Step 1：修改 logging 初始化**

```rust
// main.rs:73-78
let log_format = std::env::var("OCTO_LOG_FORMAT").unwrap_or_default();
if log_format == "json" {
    tracing_subscriber::fmt()
        .json()
        .with_env_filter(
            EnvFilter::try_from_default_env()
                .unwrap_or_else(|_| EnvFilter::new(&log_filter)),
        )
        .init();
} else {
    tracing_subscriber::fmt()
        .with_env_filter(
            EnvFilter::try_from_default_env()
                .unwrap_or_else(|_| EnvFilter::new(&log_filter)),
        )
        .init();
}
```

**Step 2：验证**
```bash
OCTO_LOG_FORMAT=json cargo run --bin octo-server
# 期望：输出 JSON 格式日志
```

**Step 3：提交**
```bash
git add crates/octo-server/src/main.rs
git commit -m "feat(O5/B5): add JSON log format via OCTO_LOG_FORMAT=json env var"
```

---

### Task B6：移除 Option\<McpManager\>（清理噪声）

**目标：** `mcp_manager` 从 `Option<Arc<Mutex<McpManager>>>` 改为 `Arc<Mutex<McpManager>>`，消除不必要的 `.ok_or(AgentError::McpNotInitialized)` 噪声。

**文件：**
- 修改: `crates/octo-engine/src/agent/runtime.rs:91`（字段类型）
- 修改: 所有使用 `mcp_manager.as_ref().ok_or(...)` 的地方

**Step 1：确认 mcp_manager 总是被初始化**

检查 `runtime.rs` 的 `new()` 方法，确认 `mcp_manager: Some(Arc::new(...))` 总是被赋值（非条件）。

**Step 2：修改字段类型和相关方法**

```rust
// runtime.rs:91
mcp_manager: Arc<Mutex<crate::mcp::manager::McpManager>>,

// runtime.rs:new() 中
mcp_manager: Arc::new(Mutex::new(McpManager::new())),

// 所有 mcp_manager.as_ref().ok_or(...) 改为直接使用
// mcp_manager().as_ref() → &self.mcp_manager
```

**Step 3：更新 pub getter**

```rust
pub fn mcp_manager(&self) -> &Arc<Mutex<crate::mcp::manager::McpManager>> {
    &self.mcp_manager
}
```

**Step 4：运行测试**
```bash
cargo test --workspace
```

**Step 5：提交**
```bash
git add crates/octo-engine/src/agent/runtime.rs
git commit -m "refactor(B6): remove Option<McpManager>, always initialized"
```

---

### Phase B 验收

```bash
# 全量测试
cargo test --workspace

# API curl 验证
curl http://localhost:3001/api/health
curl http://localhost:3001/api/metrics
curl -X POST http://localhost:3001/api/tasks -H "Content-Type: application/json" -d '{"prompt":"hello"}'
curl http://localhost:3001/api/tasks

# 期望：所有接口正常返回，无 500 错误
```

---

## Phase C：前端控制台（目标：~5天）

### Task C1：TabBar 扩展（添加新页面标签）

**目标：** 在现有 TabBar 中添加 Tasks、Schedule、Tools、Memory、Debug 标签。

**文件：**
- 修改: `web/src/components/TabBar.tsx`（或找到对应的导航组件）
- 修改: `web/src/atoms/ui.ts`（若有 tab state atom）

**Step 1：找到现有 TabBar 组件**
```bash
grep -rn "TabBar\|tabs\|navigation" web/src/components/ --include="*.tsx" | head -20
```

**Step 2：添加新标签配置**

```typescript
const TABS = [
  { id: 'chat', label: 'Chat', icon: '💬' },
  { id: 'tasks', label: 'Tasks', icon: '⚡' },
  { id: 'schedule', label: 'Schedule', icon: '🕐' },
  { id: 'tools', label: 'Tools', icon: '🔧' },
  { id: 'memory', label: 'Memory', icon: '🧠' },
  { id: 'debug', label: 'Debug', icon: '🔍' },
];
```

**Step 3：确保路由/页面切换逻辑正确**

检查 `web/src/App.tsx` 或路由配置，确认新 tab 对应正确的页面组件渲染。

**Step 4：运行构建验证**
```bash
cd web && npx tsc --noEmit
cd web && npx vite build
```
期望：零错误

**Step 5：提交**
```bash
git add web/src/
git commit -m "feat(C1): extend TabBar with Tasks, Schedule, Tools, Memory, Debug tabs"
```

---

### Task C2：Tasks 页面

**目标：** 实现后台任务的提交和查询界面。

**文件：**
- 新建: `web/src/pages/Tasks.tsx`

**Step 1：创建 Tasks.tsx**

```typescript
// web/src/pages/Tasks.tsx
import { useState, useEffect } from 'react';

interface Task {
  id: string;
  status: 'pending' | 'running' | 'done' | 'failed';
  result?: string;
  error?: string;
  created_at: string;
}

export function TasksPage() {
  const [tasks, setTasks] = useState<Task[]>([]);
  const [prompt, setPrompt] = useState('');
  const [loading, setLoading] = useState(false);

  const fetchTasks = async () => {
    const res = await fetch('/api/tasks');
    const data = await res.json();
    setTasks(data);
  };

  const submitTask = async () => {
    setLoading(true);
    await fetch('/api/tasks', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ prompt }),
    });
    setPrompt('');
    await fetchTasks();
    setLoading(false);
  };

  useEffect(() => { fetchTasks(); }, []);

  return (
    <div className="p-4">
      <h2 className="text-lg font-bold mb-4">Background Tasks</h2>

      {/* 提交表单 */}
      <div className="mb-6 flex gap-2">
        <input
          value={prompt}
          onChange={e => setPrompt(e.target.value)}
          placeholder="Enter task prompt..."
          className="flex-1 border rounded p-2"
        />
        <button
          onClick={submitTask}
          disabled={loading || !prompt}
          className="px-4 py-2 bg-blue-500 text-white rounded disabled:opacity-50"
        >
          Submit
        </button>
      </div>

      {/* 任务列表 */}
      <div className="space-y-2">
        {tasks.map(task => (
          <div key={task.id} className="border rounded p-3">
            <div className="flex justify-between items-center">
              <span className="font-mono text-sm">{task.id.slice(0, 8)}</span>
              <StatusBadge status={task.status} />
            </div>
            {task.result && (
              <pre className="mt-2 text-sm text-gray-600 whitespace-pre-wrap">
                {task.result}
              </pre>
            )}
          </div>
        ))}
      </div>
    </div>
  );
}

function StatusBadge({ status }: { status: string }) {
  const colors = {
    pending: 'bg-yellow-100 text-yellow-800',
    running: 'bg-blue-100 text-blue-800',
    done: 'bg-green-100 text-green-800',
    failed: 'bg-red-100 text-red-800',
  };
  return (
    <span className={`px-2 py-1 rounded text-xs ${colors[status] ?? ''}`}>
      {status}
    </span>
  );
}
```

**Step 2：构建验证**
```bash
cd web && npx tsc --noEmit
```

**Step 3：提交**
```bash
git add web/src/pages/Tasks.tsx
git commit -m "feat(C2): add Tasks page for background task submission and monitoring"
```

---

### Task C3：Schedule 页面

**目标：** 实现定时任务管理界面（列表、添加、删除、查看执行历史）。

**文件：**
- 新建: `web/src/pages/Schedule.tsx`

**Step 1：查看现有 scheduler API**
```bash
cat crates/octo-server/src/api/scheduler.rs | head -60
```
确认 CRUD 端点路径（通常是 `/api/scheduler/tasks`）。

**Step 2：创建 Schedule.tsx**

实现以下功能：
- 列出定时任务（`GET /api/scheduler/tasks`）
- 显示每个任务的 cron 表达式、下次执行时间、最近结果
- 添加新任务（modal 表单：name、cron、prompt）
- 删除任务
- 手动触发（`POST /api/scheduler/tasks/:id/run`）
- 查看执行历史（`GET /api/scheduler/tasks/:id/executions`）

**Step 3：构建验证**
```bash
cd web && npx tsc --noEmit
```

**Step 4：提交**
```bash
git add web/src/pages/Schedule.tsx
git commit -m "feat(C4): add Schedule page for cron task management"
```

---

### Task C4：Tools 页面（MCP + Built-in + Skills）

**目标：** 展示并管理 MCP servers、内置工具、Skills。

**文件：**
- 新建: `web/src/pages/Tools.tsx`（整合或新建）
- 参考: `web/src/components/mcp/`（现有 MCP 组件）

**Step 1：查看现有 MCP 组件**
```bash
ls web/src/components/mcp/
cat web/src/components/mcp/ServerList.tsx
```

**Step 2：创建 Tools.tsx**

三个 Tab：
1. **MCP Servers**：展示服务器状态，添加（Stdio/SSE）、移除
2. **Built-in Tools**：从 `GET /api/tools` 获取列表，只读展示
3. **Skills**：列出已加载 Skills（从 config/API 获取）

添加 MCP Server 的表单（来自现有 `ServerList.tsx` 中未完成的 Add 按钮）：
```typescript
interface AddMcpServerForm {
  name: string;
  transport: 'stdio' | 'sse';
  command?: string;  // stdio
  args?: string[];   // stdio
  url?: string;      // sse
}
```

调用 `POST /api/mcp/servers` 添加，`DELETE /api/mcp/servers/:name` 移除。

**Step 3：构建验证**
```bash
cd web && npx tsc --noEmit
```

**Step 4：提交**
```bash
git add web/src/pages/Tools.tsx
git commit -m "feat(C2/C5): add Tools page with MCP servers, built-in tools, and skills"
```

---

### Task C5：Memory 页面

**目标：** 实现持久化记忆的查看、搜索、增删改界面。

**文件：**
- 新建: `web/src/pages/Memory.tsx`

**Step 1：了解 memories API**
```bash
grep -n "memories" crates/octo-server/src/api/mod.rs
curl http://localhost:3001/api/memories
curl http://localhost:3001/api/memories/working
```

**Step 2：创建 Memory.tsx**

功能：
- 搜索框（`GET /api/memories?q=xxx`）
- 记忆列表（block type、内容预览、created_at）
- 点击展开全文
- 删除（`DELETE /api/memories/:id`）
- 手动添加（`POST /api/memories`）
- 显示 Working Memory（`GET /api/memories/working`）

**Step 3：构建验证**
```bash
cd web && npx tsc --noEmit
```

**Step 4：提交**
```bash
git add web/src/pages/Memory.tsx
git commit -m "feat(C7): add Memory page for persistent memory management"
```

---

### Task C6：Debug 页面

**目标：** 开发者工具页面，展示工具执行追踪、LoopGuard 统计、系统健康。

**文件：**
- 新建: `web/src/pages/Debug.tsx`

**Step 1：了解可用 API**

```bash
curl http://localhost:3001/api/metrics
curl http://localhost:3001/api/executions
curl http://localhost:3001/api/executions/stats
curl http://localhost:3001/api/health
```

**Step 2：创建 Debug.tsx**

三个部分：
1. **System Health**：展示 `/api/health` 数据（provider、uptime、MCP servers）
2. **Tool Executions**：
   - 从 `/api/executions` 获取历史（工具名、耗时、成功/失败、时间）
   - 从 `/api/executions/stats` 获取统计（每工具调用次数、平均耗时、成功率）
3. **Metrics**：从 `/api/metrics` 展示 turns_total、tool_calls 等

```typescript
// 简单表格展示工具执行统计
interface ToolStat {
  tool_name: string;
  call_count: number;
  avg_latency_ms: number;
  success_rate: number;
}
```

**Step 3：构建验证**
```bash
cd web && npx tsc --noEmit
```

**Step 4：提交**
```bash
git add web/src/pages/Debug.tsx
git commit -m "feat(O1/O2/O3): add Debug page with tool traces, metrics, and health"
```

---

### Task C7：Chat 页面完善

**目标：** 改善 Chat 页面，添加 Token 用量实时显示和取消按钮。

**文件：**
- 修改: `web/src/pages/Chat.tsx`（或相关组件）

**Step 1：查看现有 Chat 页面**
```bash
ls web/src/pages/
cat web/src/pages/Chat.tsx | head -80
```

**Step 2：添加 Token 用量显示**

订阅 WebSocket 的 `token_budget_update` 事件，在右上角显示：
```typescript
interface TokenBudget {
  used: number;
  total: number;
  percentage: number;
}

// 在 Chat.tsx 顶部添加
const [tokenBudget, setTokenBudget] = useState<TokenBudget | null>(null);

// ws 消息处理中：
case 'token_budget_update':
  setTokenBudget(msg.budget);
  break;

// UI：
{tokenBudget && (
  <div className="text-xs text-gray-500">
    {tokenBudget.used.toLocaleString()} / {tokenBudget.total.toLocaleString()} tokens
  </div>
)}
```

**Step 3：取消按钮**

当 `streaming === true` 时显示取消按钮，点击发送 `{"type": "cancel"}` WS 消息。

**Step 4：构建验证**
```bash
cd web && npx tsc --noEmit && npx vite build
```
期望：零错误

**Step 5：提交**
```bash
git add web/src/pages/Chat.tsx web/src/
git commit -m "feat(O2/C7): add token usage display and cancel button to Chat page"
```

---

### Phase C 验收

```bash
# TypeScript 类型检查
cd web && npx tsc --noEmit

# 生产构建
cd web && npx vite build

# 所有页面手动访问验证：
# - http://localhost:5180/  → Chat 页面
# - Tasks 页面：提交任务、查看状态
# - Schedule 页面：添加 cron 任务
# - Tools 页面：MCP servers 管理
# - Memory 页面：搜索记忆
# - Debug 页面：工具执行统计
```

---

## Phase D：集成验收（目标：~2天）

### Task D1：端到端测试脚本

**目标：** 编写覆盖三模式的端到端测试脚本。

**文件：**
- 新建: `tests/e2e/test_three_modes.sh`

**Step 1：创建 e2e 测试脚本**

```bash
#!/bin/bash
set -e

BASE_URL="http://localhost:3001"

echo "=== E2E Test: Three Interaction Modes ==="

# 模式1：对话模式（WebSocket）
echo "--- Mode 1: Chat (WebSocket) ---"
# 使用 wscat 或 websocat 发送消息并等待响应
# 验证：收到 TextDelta 事件 + Done 事件

# 模式2：后台任务
echo "--- Mode 2: Background Task ---"
TASK_ID=$(curl -s -X POST $BASE_URL/api/tasks \
  -H "Content-Type: application/json" \
  -d '{"prompt":"echo hello world"}' | jq -r .id)
echo "Task created: $TASK_ID"

# 等待任务完成
for i in {1..30}; do
  STATUS=$(curl -s $BASE_URL/api/tasks/$TASK_ID | jq -r .status)
  if [ "$STATUS" = "done" ] || [ "$STATUS" = "failed" ]; then
    break
  fi
  sleep 2
done
echo "Task final status: $STATUS"
[ "$STATUS" = "done" ] || (echo "FAIL: task not done" && exit 1)

# 模式3：定时调度
echo "--- Mode 3: Scheduled Task ---"
SCHED_ID=$(curl -s -X POST $BASE_URL/api/scheduler/tasks \
  -H "Content-Type: application/json" \
  -d '{"name":"test","cron":"0 * * * *","agent_config":{"prompt":"test"}}' | jq -r .id)

# 手动触发
curl -s -X POST $BASE_URL/api/scheduler/tasks/$SCHED_ID/run
echo "Scheduled task triggered"

echo "=== All modes passed ==="
```

**Step 2：运行验证**
```bash
# 先启动服务器
make server &
sleep 3

# 运行测试
bash tests/e2e/test_three_modes.sh
```

**Step 3：提交**
```bash
git add tests/e2e/
git commit -m "test(D1): add e2e test script for three interaction modes"
```

---

### Task D2：Docker Compose 一键启动

**目标：** `docker compose up` 启动完整系统（backend + frontend）。

**文件：**
- 检查/修改: `docker-compose.yml`（若已存在）
- 新建: `Dockerfile`（若不存在）

**Step 1：检查现有 Docker 配置**
```bash
ls Dockerfile docker-compose.yml docker-compose.yaml 2>/dev/null
```

**Step 2：创建/更新 Dockerfile**

```dockerfile
FROM rust:1.82-slim AS builder
WORKDIR /app
COPY . .
RUN cargo build --release --bin octo-server

FROM node:20-slim AS frontend-builder
WORKDIR /web
COPY web/package.json web/pnpm-lock.yaml ./
RUN npm install -g pnpm && pnpm install
COPY web/ .
RUN pnpm run build

FROM debian:bookworm-slim
WORKDIR /app
COPY --from=builder /app/target/release/octo-server .
COPY --from=frontend-builder /web/dist ./web/dist
COPY config.yaml .
EXPOSE 3001
CMD ["./octo-server"]
```

**Step 3：创建/更新 docker-compose.yml**

```yaml
version: '3.8'
services:
  octo:
    build: .
    ports:
      - "3001:3001"
      - "5180:5180"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - OCTO_PORT=3001
    volumes:
      - ./data:/app/data
      - ./config.yaml:/app/config.yaml
```

**Step 4：验证**
```bash
docker compose up --build
# 期望：服务器在 3001 端口启动
curl http://localhost:3001/api/health
```

**Step 5：提交**
```bash
git add Dockerfile docker-compose.yml
git commit -m "feat(D2): add Dockerfile and docker-compose.yml for one-click startup"
```

---

### Task D3：配置文档完善

**目标：** 完善 `.env.example` 和 `config.yaml` 注释，确保新用户可以轻松配置。

**文件：**
- 修改: `.env.example`
- 修改: `config.yaml`（添加注释）

**Step 1：更新 .env.example**

```bash
# === Required ===
ANTHROPIC_API_KEY=sk-ant-xxxxx      # Anthropic API key (required)
# OPENAI_API_KEY=sk-xxxxx           # OpenAI API key (if using openai provider)

# === Server ===
# OCTO_HOST=127.0.0.1               # Server host (default: 127.0.0.1)
# OCTO_PORT=3001                    # Server port (default: 3001)
# OCTO_DB_PATH=./data/octo.db      # SQLite database path

# === Provider ===
# LLM_PROVIDER=anthropic            # Provider: anthropic (default) or openai

# === Logging ===
# RUST_LOG=info                     # Log level: error/warn/info/debug/trace
# OCTO_LOG_FORMAT=json              # Log format: text (default) or json
```

**Step 2：提交**
```bash
git add .env.example config.yaml
git commit -m "docs(D3): update .env.example and config.yaml with complete comments"
```

---

### Task D4：发布 Checklist 验证

**目标：** 运行所有发布门槛检查，确认 v1.0 可发布。

**Step 1：后端测试**
```bash
cargo test --workspace
# 期望：所有测试 PASS，零失败
```

**Step 2：TypeScript 类型检查**
```bash
cd web && npx tsc --noEmit
# 期望：零错误
```

**Step 3：前端构建**
```bash
cd web && npx vite build
# 期望：成功，零错误
```

**Step 4：Docker 构建**
```bash
docker compose up --build
# 期望：镜像构建成功，服务启动
```

**Step 5：端到端三模式验证**
```bash
bash tests/e2e/test_three_modes.sh
# 期望：所有模式 PASS
```

**Step 6：打 v1.0 tag**
```bash
git tag -a v1.0.0 -m "octo-workbench v1.0.0 release"
# (确认所有检查通过后再执行)
```

**Step 7：最终提交**
```bash
git add -A
git commit -m "chore(D4): v1.0 release checklist - all checks passed"
```

---

## 快速参考：问题对应修复

| Bug ID | 症状 | 修复位置 | 对应 Task |
|--------|------|----------|-----------|
| S1 | stop_primary 不终止 Executor | `runtime.rs:stop_primary()` | A1 |
| S2 | MCP 热插拔无效 | `executor.rs:tools 类型` + `runtime.rs:start_primary()` | A2 |
| S3 | run_now 假执行 | `scheduler/mod.rs:run_now()` | A3 |
| S4 | WorkingMemory 全局共享 | `runtime.rs:start_primary()` memory 参数 | A4 |
| S5 | MCP 子进程未清理 | `main.rs:shutdown_signal()` | A5 |
| S6 | Provider 无重试 | **已实现**（`loop_.rs:271-307` + `retry.rs`） | A6 |
| C1 | enable_parallel 无效 | `loop_.rs` 工具执行段 | B1 |
| O4 | turns.total 永远为 0 | `loop_.rs:for round` 开始处 | B4 |
| O5 | 无 JSON 日志 | `main.rs` logging 初始化 | B5 |

## 注意事项

1. **A2 修改影响范围广**：`executor.rs` 的 `tools` 类型变化会影响 `AgentExecutor::new()` 的所有调用方，确保全部更新
2. **A3 需先读 execute_scheduled_task 签名**：确认参数类型后再修改 run_now
3. **Phase C 前确认 API 端点**：查看 `api/mod.rs` 的实际路由，前端 fetch 路径要与后端一致
4. **S6 无需修改**：Provider 重试通过 `RetryPolicy::default()` 在 loop_.rs 中已完整实现

---

## Deferred（暂缓项）

> 本阶段已知但暂未实现的功能点。每次开始新 Task 前先检查此列表。

| ID | 内容 | 前置条件 | 状态 |
|----|------|---------|------|
| D1 | AgentExecutor observability 事件转发（`runtime.rs:86` TODO） | Observer wiring 添加完成后 | ✅ 已补 |
| D2 | 旧构造函数 `AgentRuntime::new_legacy()` 移除（`runtime.rs:244` unimplemented!） | 新构造函数完全验证通过后 | ✅ 已补 |
| D3 | 认证中间件实现（`octo-server/src/auth/middleware.rs:50` NOT_IMPLEMENTED） | JWT/OAuth2 集成设计后 | ✅ 已补 |
| D4 | WebSocket JSON 序列化 `.unwrap()` 风险（`ws.rs:119,134,219`） | 替换为 Result 处理 | ✅ 已补 |
| D5 | API Handler `.unwrap()` 风险（`tools.rs:19` 已修复, `mcp_servers.rs:252` 实际安全） | 替换为 Result/Option 处理 | ✅ 已补 |
