// Wrapping MCP middleware server. T1 ships the type; T3 wires
// tools/call interception + ADR-V2-006 §2/§3 envelope dispatch.
pub struct Server;

impl Server {
    pub fn new() -> Self {
        Self
    }
}
